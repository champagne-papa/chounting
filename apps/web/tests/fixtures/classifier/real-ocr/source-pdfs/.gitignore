# Real personal/business documents (founder self-dogfooding corpus).
# Never commit the binaries — only the captured OCR line-text in
# ../corpus.ts is committed (founder-authorized; CRA business-records
# retention). Also excludes Windows NTFS *:Zone.Identifier artifacts.
*
!.gitignore
!README.md

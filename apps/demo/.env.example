# Demo-app environment variables.
#
# The demo app is a design surface — no Supabase, no auth, no
# secrets. Add demo-only keys here as they appear (e.g. a
# NEXT_PUBLIC_ANTHROPIC_API_KEY for in-browser AI calls in
# isolated component demos).

# Phase 7 Chunk 7.2 Brief — Classifier + dedup integration + org_settings substrate-add migration

- **Date:** 2026-05-19
- **Phase:** Phase 7 (substantively-new-phase cycle; Tier 2 extraction pipeline)
- **Chunk:** 7.2 of 3 (chunk 7.1 SPLIT to 7.1a + 7.1b orchestrator + Modal sidecar; chunk 7.2 classifier + org_settings substrate; chunk 7.3 extractor + matcher + proposal + UI consumer wires)
- **Path C disposition:** **SINGLE-CHUNK** at brief-grade per Path C MODERATE-but-settled-substrate analysis at §1.3 (cycle-close §7.2 LOW probability framing adjusted to MODERATE at brief-grade based on file-count walk evidence; SPLIT NOT WARRANTED because settled-substrate framing keeps framing-revisit count low)
- **Status:** brief-drafting (Session 34; this artifact) → chunk 7.2 implementation (Session 38 per cycle-close §9.5 sequencing post chunk 7.1a + 7.1b implementations at Sessions 36-37)
- **Session shape:** Single-session brief drafting per Phase 7 chunk 7.1 precedent (592 LOC); chunk 7.2 brief expected at ~600-940 LOC per Session 33 corrected brief-artifact-band inheritance
- **Predecessor:** Phase 7 chunk 7.1 brief at `701dcd2` (`docs/09_briefs/phase-7/chunks/2026-05-19-phase-7-chunk-7-1.md`; 592 LOC; Path C SPLIT disposition)
- **Baseline at Session 34 onset:** HEAD = `701dcd2` on staging; `pnpm agent:validate` 26/26 green at session-onset

## §1 — Preamble

### §1.1 Brief context

This brief operationalizes Phase 7 chunk 7.2 at implementation-task grain. The cycle-close artifact at `b964798` locked all 27 sub-questions; chunk 7.1 brief at `701dcd2` shipped Path C SPLIT disposition for chunk 7.1 (chunk 7.1a + chunk 7.1b inline). This brief converts the chunk 7.2 cycle-close locks into a numbered task list with per-task acceptance criteria + dependency annotations + concrete value picks including Sub-Q27 column-shape adjudication for all 11 reserved org_settings columns.

**Inheritance posture.** The cycle-close artifact + chunk 7.1 brief are authoritative for *what* chunk 7.2 ships (Stage 3 classification active per ADR-0014 §7; org_settings substrate-add migration; ExceptionReasonSchema enum extension; per-document-type rule + prompt + schema modules per Sub-Q6/Q9/Q13 locks; AI fallback mock harness per Sub-Q24). This brief is authoritative for *how* chunk 7.2 builds it (per-task substrate decomposition; Sub-Q27 column-shape per-column value picks; Tier A rule heuristics; Tier C system prompt content; AI fallback mock harness shape). The cycle-close brief + chunk 7.1 brief win on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

**Brief-drafting cycle vs implementation cycle distinction (inherited from chunk 7.1 brief §1.1).** Per cycle-close §9.5 sequencing:

- **Brief-drafting cycle:** Sessions 33 (chunk 7.1 brief) → 34 (chunk 7.2 brief; this artifact) → 35 (chunk 7.3 brief). Preserved regardless of Path C split outcomes.
- **Implementation cycle:** Sessions 36 (chunk 7.1a impl) → 37 (chunk 7.1b impl) → 38 (chunk 7.2 impl per this brief) → 39+ (chunk 7.3 impl; possibly split). Path C splits expand implementation cycle session count.

Single-Session-34 brief artifact ships chunk 7.2 task decomposition; no Path C split (see §1.3 disposition).

### §1.2 Session-onset divergence absorption

**Divergence (α) — ADR-0019 reserved org_settings column substrate verification clarification.** Phase A step 7 ADR-0019 read enumerates the 6 reserved org_settings columns explicitly per §2 lines 399-404:

| Column | Surface | Default |
|---|---|---|
| `confidence_threshold_vendor_invoice` | 3.1 | NULL-default |
| `confidence_threshold_receipt` | 3.2 | NULL-default |
| `confidence_threshold_payment_confirmation` | 3.3 | NULL-default |
| `confidence_threshold_ambiguity_margin` | 3.5 | NULL-default |
| `calibration_cadence` | governance config | NULL-default |
| `calibration_test_set_version` | governance config | NULL-default |

Cycle-close §1.4 noted "ADR-0019 substrate-scope expansion finding: ADR-0019 commits 6 reserved org_settings columns NULL-default at v1 (distinct from ADR-0014's 5 columns)" but did NOT enumerate column names or shape commitments at cycle-close grade. The Session 34 directive anti-drift framing (Refinement note 2) flagged the verification requirement; Phase A step 7 outcome resolves the gap. Bank as **option-space-framing-against-substrate sub-shape: directive-grade-citation-against-substrate sub-grain second instance** at chunk-brief-drafting grade (parallels Session 33 (α) Phase 5.1 chunk 5.1a LOC citation correction; same sub-grain firing at different surface).

Cumulative banking: directive-grade-citation-against-substrate sub-grain graduates to **N=2** at chunk-brief-drafting grade.

**Divergence (β) — Path C probability re-evaluation at brief-grade.** Cycle-close §7.2 forecast Path C LOW probability ("substrate is well-bounded; no novel external dependencies analogous to Modal sidecar"). Phase B §1.3 walk-grade analysis surfaces MODERATE probability based on file-count evidence (~15-18 files exceeds chunk-3-Phase-4's 8-file bound) — but SETTLED-SUBSTRATE framing (Sub-Q6/Q9/Q10/Q13/Q26 locks at scope-lock-cycle grade; minimal framing-revisit risk) keeps disposition at single-chunk. The cycle-close §7.2 framing presumed file-count-doesn't-matter-without-novelty — empirically incomplete framing. Brief-grade walks against multi-axis evaluation (file count + framing count + novelty + settled-substrate-stability) for tighter Path C disposition.

Bank as **catalog-substrate-characterization drift sub-grain second instance** (parallels Sub-Q21 F-J-14 catalog-grain-characterization drift at Round 4; same sub-grain firing at different catalog — Path C probability heuristic characterization vs F-J-14 grain catalog). Cumulative banking: catalog-substrate-characterization sub-grain graduates to **N=2** at brief-drafting grade.

**No other divergences at session-onset.** Substrate citation corrections from Rounds 1-3 + Session 33 inherited cleanly.

### §1.3 Path C invocation evaluation at brief-drafting grade — MODERATE probability; SINGLE-CHUNK disposition

Per F-J-14 Grain 1 brief-draft prospective firing conditions + chunk-3-Phase-4 empirical bound + cycle-close §7.2 forecast LOW probability inheritance + Phase A substrate verification outcomes:

**Volume estimators at brief-drafting grade:**

| Estimator | Chunk 7.2 walk-grade | Chunk-3-Phase-4 bound | Status |
|---|---|---|---|
| Source files touched (TypeScript) | ~3-4 (documentExceptionService extension + ingestDocument.ts Stage 3 wiring + types.ts extension + classifier subdirectory entry) | 8 | BELOW |
| Source files created (TypeScript) | ~12-14 (3 per-document-type rule modules + tierCoordination + aiFallback + 3 per-document-type prompts + 3 per-document-type Zod schemas + 2-3 test files) | (composes with modified) | (composes) |
| Source files combined (TS) | ~15-18 | 8 | EXCEEDS |
| Migrations | 1 (org_settings substrate-add) | 1 | EQUAL |
| Generated types.ts | 0 new (extension only) | 1 | BELOW |
| Framing-revisits at brief-grade | 0-2 (settled substrate from cycle-close locks; anti-drift framings pre-emptively framed at directive grade) | 5 | WELL BELOW |
| Cross-phase blast radius (services) | 1 (documentExceptionService extension; existing service) | (case-by-case) | LOW |

**Path C invocation criteria evaluation:**

- **Volume estimators sum exceeds single-session reliable delivery band:** PARTIAL. TS file count (15-18) exceeds 8 bound; migration + types.ts at or below bound; net volume estimator is MODERATE.
- **Scope-lock surfaces N≥3 framings:** NO. Settled-substrate dominance — Sub-Q6 (classifier module structure) + Sub-Q9 (prompt versioning) + Sub-Q10 (exception payload) + Sub-Q13 (schema location) + Sub-Q26 (substrate-placement) all locked at scope-lock-cycle grade. Framing-revisit count expected ~0-2.
- **Substantively-novel-logic scope:** NO. No Modal-equivalent novel substrate. Per-document-type rules + Tier coordination + AI fallback module + per-document-type prompts + per-document-type Zod schemas are mechanical authorship against well-defined contracts (ADR-0014 §7 + §8 + agent_architecture_policy.md §2.1 + ADR-0011 §6).

**Disposition: Path C MODERATE probability with SINGLE-CHUNK disposition.**

The chunk-3-Phase-4 empirical bound (5 framings + 8 source files + 1 migration + 1 types.ts) pushed chunk-3 over single-session reliable delivery via the COMBINATION of HIGH framing-revisit count AND HIGH file count AND HIGH novelty. Chunk 7.2's substrate fires HIGH on file count alone but LOW on framing-revisit count + LOW on novelty + ZERO on types.ts. The COMBINATION doesn't reach chunk-3-Phase-4 threshold for SPLIT invocation.

**Walk-grade refinement of cycle-close §7.2 framing:** cycle-close §7.2 LOW probability framing was based on novelty/complexity heuristic alone; brief-grade walks against MULTI-AXIS evaluation (file count + framing count + novelty + settled-substrate-stability) for tighter Path C disposition. The refinement is precision-grade (LOW → MODERATE-single-chunk), not disposition-grade (SPLIT not warranted).

Bank at §6 carry-forward observations as **multi-axis Path C probability evaluation discipline** — pattern: brief-grade Path C probability calibrates against file-count + framing-count + novelty + settled-substrate-stability axes rather than single-axis novelty heuristic. N=1 banking at chunk 7.2 brief-grade; codification threshold N=3+ for retrospective grade.

### §1.4 Cross-references

- **Cycle-close artifact:** `docs/09_briefs/phase-7/2026-05-19-phase-7-scope-lock-cycle-close.md` (`b964798`) — §3 sub-question lock consolidation; §5.4 chunk 7.2 acceptance criteria framework; §6 inter-chunk dependency map; §7.2 chunk 7.2 substrate-load expectation.
- **Chunk 7.1 brief:** `docs/09_briefs/phase-7/chunks/2026-05-19-phase-7-chunk-7-1.md` (`701dcd2`) — Path C SPLIT precedent at brief-grade (chunk 7.1a + chunk 7.1b inline); Stage 3 wiring at chunk 7.2 replaces chunk 7.1a Stage 3 stub.
- **Phase 5.1 chunk 5.1a brief:** `docs/09_briefs/phase-5.1/chunks/2026-05-19-phase-5-1-chunk-5-1a.md` — chunk-brief structural template + Postgres DDL convention precedent (text + CHECK constraint vs new ENUM types).
- **Phase 6.5 chunk 1 brief:** `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` — chunk-brief structural template.
- **ADR-0014:** §6 (dedup-by-hash; chunk 7.1a Stage 0 substrate inherited) + §7 (classifier tier strategy: Tier A high-precision-low-recall + Tier C Claude Sonnet AI fallback + Tier D unknown fallback; per-document-type confidence thresholds 0.85/0.80/0.85) + §8 (AI fallback contract + trace propagation pipeline_trace.input_hash includes system prompt version; max 2 fallback calls per source document budget) + §11 (proposal routing; not exercised at chunk 7.2 — Stages 6-7 stub remain) + §12 (failure classification; Stage 3 classifier inherits §12.1 + §12.3 audit event shapes).
- **ADR-0019** (`docs/07_governance/adr/0019-confidence-calibration-policy.md`) — Ratified 2026-05-04; calibration governance + 6 reserved org_settings columns at §2 lines 399-404 (per §1.2 (α) absorption).
- **ADR-0010** — substrate-now-enforcement-later discipline; chunk 7.2 substrate-add migration applies at Sub-Q26 placement.
- **ADR-0011 §6** — v1-active document_type enum (vendor_invoice + receipt + payment_confirmation + unknown); chunk 7.2 classifier output drives document_type assignment.
- **ADR-0007 Q28 + Q30 + Q31** — Tier 2 safety contract + Logic Receipt reproducibility + LLM-orchestration prohibition.
- **agent_architecture_policy.md §2.1** — per-document-type field re-verification matrix (per Round 3 Sub-Q12 anti-drift override; per-field thresholds NOT inherited from classification thresholds).
- **F-J-14 catalog** at `docs/04_engineering/conventions/session/scope-lock.md` §Session-budget-feasibility verification + Path C invocation conditions (RI-7).
- **Existing project substrate:**
  - `apps/web/src/services/document-platform/documentExceptionService.ts` — ExceptionReasonSchema enum at `exceptionQueueEntry.schema.ts`; chunk 7.2 extends with `ai_fallback_validation_failed` value.
  - `supabase/migrations/` — latest migration `20240156000000_phase_5_1_vendor_credits_substrate.sql`; chunk 7.2 substrate-add migration at `20240157000000_phase_7_org_settings_substrate.sql`.
  - DDL convention precedent: `text NOT NULL DEFAULT '<value>' + CHECK constraint` for enum-like fields (no new ENUM types at chunk-grade); `numeric(P,S) NOT NULL DEFAULT <value>` for money/threshold; `timestamptz NOT NULL DEFAULT now()` for timestamps.
  - `apps/web/src/shared/schemas/` — per-domain organization (accounting, canvas, document-platform, organization, spend, user); chunk 7.2 seeds `extraction/` subdirectory.

## §2 — Scope

### §2.1 What chunk 7.2 ships

- **`org_settings` substrate-add migration** at `supabase/migrations/20240157000000_phase_7_org_settings_substrate.sql`:
  - CREATE TABLE `org_settings` with `org_id uuid PRIMARY KEY REFERENCES organizations(org_id) ON DELETE CASCADE`.
  - 5 ADR-0014 reserved columns (v1-active NOT NULL DEFAULT):
    - `classification_fallback_order text[] NOT NULL DEFAULT ARRAY['tier_a', 'tier_c', 'tier_d']::text[]` (per §4 value pick).
    - `ai_fallback_budget integer NOT NULL DEFAULT 2`.
    - `vendor_match_threshold numeric(3,2) NOT NULL DEFAULT 0.80`.
    - `gc_cadence text NOT NULL DEFAULT 'daily' CHECK (gc_cadence IN ('daily', 'hourly'))`.
    - `gc_threshold_hours integer NOT NULL DEFAULT 24`.
  - 6 ADR-0019 reserved columns (NULL-default substrate-now-enforcement-later):
    - `confidence_threshold_vendor_invoice numeric(3,2) DEFAULT NULL`.
    - `confidence_threshold_receipt numeric(3,2) DEFAULT NULL`.
    - `confidence_threshold_payment_confirmation numeric(3,2) DEFAULT NULL`.
    - `confidence_threshold_ambiguity_margin numeric(3,2) DEFAULT NULL`.
    - `calibration_cadence integer DEFAULT NULL` (months).
    - `calibration_test_set_version text DEFAULT NULL`.
  - `created_at timestamptz NOT NULL DEFAULT now()` + `updated_at timestamptz NOT NULL DEFAULT now()`.
  - RLS policies per Phase 4+ multi-tenant org-scoped pattern.
  - Backfill: INSERT INTO org_settings (org_id) SELECT org_id FROM organizations ON CONFLICT DO NOTHING (per row-per-org v1-active discipline).
- **`ExceptionReasonSchema` enum extension** at `apps/web/src/shared/schemas/document-platform/exceptionQueueEntry.schema.ts`:
  - Add `'ai_fallback_validation_failed'` to existing 6-value enum (`manual_route` + `low_confidence_classification` + `unknown_document_type` + `unmatched_router_candidate` + `multi_candidate_ambiguity` + `invariant_violation` + NEW `ai_fallback_validation_failed`).
  - Update CHECK constraint in existing exception_queue substrate migration via ALTER (or new migration if existing migration has been applied).
- **Classifier subdirectory + per-document-type rule modules** at `apps/web/src/agent/orchestrator/extraction/classifier/`:
  - `vendorInvoiceRules.ts` — Tier A heuristics for vendor_invoice (Invoice/Bill header patterns; vendor identity patterns; total/subtotal/tax field patterns; filename "invoice"/"bill" heuristics).
  - `receiptRules.ts` — Tier A heuristics for receipt (receipt-shape patterns: terminal-style line layout; total at bottom; payment-method line; filename "receipt" heuristics).
  - `paymentConfirmationRules.ts` — Tier A heuristics for payment_confirmation (payment language patterns: "payment received" / "thank you for your payment"; confirmation number patterns; filename heuristics).
  - `tierCoordination.ts` — Tier A → Tier C → Tier D fallback ordering logic per Sub-Q7 (highest-confidence-first) + Sub-Q8 (binary match-or-no-match short-circuit) locks.
  - `aiFallback.ts` — Tier C Claude Sonnet AI fallback per ADR-0014 §8 contract (Claude SDK invocation + system prompt assembly + Zod-validation gate + confidence-threshold gate + max 2 calls per source document budget).
  - `index.ts` — classifier module entrypoint; exports `classifyDocumentType(ocrArtifact, traceId, ctx): Promise<ClassificationResult>`.
- **Per-document-type prompt files** at `apps/web/src/agent/orchestrator/extraction/classifier/prompts/`:
  - `vendor_invoice.prompt.ts` — Tier C system prompt for vendor_invoice classification + field-extraction targets.
  - `receipt.prompt.ts` — Tier C system prompt for receipt classification + field-extraction targets.
  - `payment_confirmation.prompt.ts` — Tier C system prompt for payment_confirmation classification + field-extraction targets.
  - Each prompt file exports `{ content: string; contentHash: string }` where contentHash is SHA-256 of content per Sub-Q9 lock; build-time script computes contentHash from content (or runtime computation cached at module load).
- **Per-document-type Zod schemas** at `apps/web/src/shared/schemas/extraction/`:
  - `vendorInvoice.schema.ts` — Tier C AI fallback output schema for vendor_invoice (classification + extracted fields per ADR-0014 §8 + agent_architecture_policy.md §2.1).
  - `receipt.schema.ts` — Tier C AI fallback output schema for receipt.
  - `paymentConfirmation.schema.ts` — Tier C AI fallback output schema for payment_confirmation.
  - `index.ts` — schema module entrypoint; exports `ClassificationOutputSchema` discriminated union over document_type.
- **Stage 3 classification active wiring** at `apps/web/src/agent/orchestrator/extraction/ingestDocument.ts`:
  - Replace chunk 7.1a Stage 3 stub with active classifier invocation: `const classification = await classifyDocumentType(ocrArtifact, traceId);`.
  - Preserve Stages 0-2 active (chunk 7.1a + chunk 7.1b inheritance) + Stages 4-7 stub.
- **pipeline_trace JSONB emission for Stage 3** per ADR-0014 §8 trace propagation:
  - Tier A path: `(stage_name = 'classification_tier_a', input_hash = SHA-256(ocrArtifact.lines), output_hash = SHA-256(classificationResult), model = null, timestamp)`.
  - Tier C path: `(stage_name = 'ai_fallback_classify', input_hash = SHA-256(ocrArtifact.lines || prompt.contentHash), output_hash = SHA-256(validatedJSON), model = 'claude-sonnet-<version>', timestamp)`.
  - Tier D path: `(stage_name = 'classification_tier_d', input_hash = SHA-256(ocrArtifact.lines), output_hash = SHA-256({document_type: 'unknown'}), model = null, timestamp)`.
- **Failure-class audit events for Stage 3** per ADR-0014 §12:
  - `pipeline_transient_retry` + `pipeline_transient_exhausted` (Anthropic API rate limit; classifier service brief unavailability).
  - `pipeline_unavailable` (Anthropic API auth failure; Claude SDK config missing; ANTHROPIC_API_KEY missing).
  - `ai_fallback_validation_failed` audit event (AI output non-validating per Sub-Q10 + this brief substrate extension).
  - `extraction_failed` with `failure_reason = 'confidence_below_threshold'` (Tier C output below per-document-type threshold).
  - `extraction_failed` with `failure_reason = 'ai_fallback_validation_failed'` (Tier C output Zod-validation failure).
- **AI fallback mock harness** at `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/mockAiFallback.ts` per Sub-Q24 per-chunk-incremental test infra:
  - `mockClaudeFallback(input: ClassificationInput, mockResponse?: ClassificationOutput | Error): Promise<ClassificationOutput>` — local mock for Tier C Claude SDK testing; supports response injection + failure injection (timeout + auth failure + non-validating output).
- **Per-document-type rule unit tests** at `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/`:
  - `vendorInvoiceRules.test.ts` — Tier A heuristic match tests (match + no-match + multi-match precedence).
  - `receiptRules.test.ts` — Tier A heuristic match tests.
  - `paymentConfirmationRules.test.ts` — Tier A heuristic match tests.
  - `tierCoordination.test.ts` — Tier A → Tier C → Tier D fallback ordering + binary short-circuit behavior tests.
  - `aiFallback.test.ts` — Tier C invocation tests with mockAiFallback (golden path + confidence-below-threshold + Zod-validation failure + max 2 calls budget).
- **Classifier integration test** at `apps/web/src/agent/orchestrator/extraction/__tests__/classifier.integration.test.ts`:
  - End-to-end Stage 3 with Stages 0-2 active (chunk 7.1a + chunk 7.1b dependency); verifies Tier A → Tier C → Tier D fallback paths with synthetic OCR artifacts.

### §2.2 What chunk 7.2 does NOT ship

Deferred to **chunk 7.3** (extractor + matcher + proposal + UI consumer wires):

- Stage 4 field extraction (per-document-type extractor modules: `vendorInvoiceExtractor.ts` + `receiptExtractor.ts` + `paymentConfirmationExtractor.ts` at `agent/orchestrator/extraction/`).
- Stage 5 vendor matching (`vendorService.matchVendor` extension at `services/spend/vendorService.ts` per Sub-Q14.b lock).
- Stage 6 relationship candidate (via documentRouterService.completeCandidate per Phase 4 substrate).
- Stage 7 proposal building (ProposedMutation / ProposedMutationBundle / ProposedAttachment routing per ADR-0014 §11 + Sub-Q15 bifurcated render per Sub-Q18 inheritance).
- canvasDirective extension with new `proposed_attachment_card` member at v1 (snake_case) per Sub-Q19 lock.
- DocumentCard per-card state machine extension per Sub-Q20 lock.

## §3 — Substrate touchpoints

Verify-from-disk at session-onset (Session 38 implementation) before consuming any line/path reference below. All counts/paths confirmed at Session 34 brief-drafting against HEAD `701dcd2`.

### §3.1 Files modified

- `supabase/migrations/` — adds new migration `20240157000000_phase_7_org_settings_substrate.sql` (per Task 7.2.1).
- `apps/web/src/shared/schemas/document-platform/exceptionQueueEntry.schema.ts` — ExceptionReasonSchema enum extension (per Task 7.2.2). Touch grain: small (~5-10 LOC addition).
- `apps/web/src/services/document-platform/documentExceptionService.ts` — verify enum extension type-propagates cleanly (per Task 7.2.2).
- `apps/web/src/agent/orchestrator/extraction/ingestDocument.ts` — replace chunk 7.1a Stage 3 stub with active classifier invocation (per Task 7.2.12). Touch grain: small (~10-20 LOC modification).
- `apps/web/src/agent/orchestrator/extraction/types.ts` — extend with `ClassificationResult` + Tier-A/Tier-C/Tier-D output types (per Task 7.2.3). Touch grain: small (~30-50 LOC addition).

### §3.2 Files created

- `supabase/migrations/20240157000000_phase_7_org_settings_substrate.sql` — ~80-120 LOC estimated.
- `apps/web/src/agent/orchestrator/extraction/classifier/` (new subdirectory).
- `apps/web/src/agent/orchestrator/extraction/classifier/index.ts` — classifier module entrypoint; ~60-100 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/vendorInvoiceRules.ts` — Tier A heuristics; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/receiptRules.ts` — Tier A heuristics; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/paymentConfirmationRules.ts` — Tier A heuristics; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/tierCoordination.ts` — fallback ordering logic; ~60-100 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/aiFallback.ts` — Tier C Claude Sonnet invocation; ~120-180 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/prompts/` (new subdirectory).
- `apps/web/src/agent/orchestrator/extraction/classifier/prompts/vendor_invoice.prompt.ts` — ~80-120 LOC (prompt content + contentHash export).
- `apps/web/src/agent/orchestrator/extraction/classifier/prompts/receipt.prompt.ts` — ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/prompts/payment_confirmation.prompt.ts` — ~80-120 LOC.
- `apps/web/src/shared/schemas/extraction/` (new subdirectory).
- `apps/web/src/shared/schemas/extraction/index.ts` — schema module entrypoint; ~30-50 LOC.
- `apps/web/src/shared/schemas/extraction/vendorInvoice.schema.ts` — ~60-100 LOC.
- `apps/web/src/shared/schemas/extraction/receipt.schema.ts` — ~60-100 LOC.
- `apps/web/src/shared/schemas/extraction/paymentConfirmation.schema.ts` — ~60-100 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/mockAiFallback.ts` — ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/vendorInvoiceRules.test.ts` — ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/receiptRules.test.ts` — ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/paymentConfirmationRules.test.ts` — ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/tierCoordination.test.ts` — ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/classifier/__tests__/aiFallback.test.ts` — ~120-180 LOC.
- `apps/web/src/agent/orchestrator/extraction/__tests__/classifier.integration.test.ts` — ~150-200 LOC.

**Total new + modified files at chunk 7.2 impl-grade: ~18-22 files.** LOC estimate at impl-grade: ~1500-2200 LOC including tests (above cycle-close §7.2 750-1350 forecast band; brief-grade walk evidence revises upward; single-chunk feasibility holds per settled-substrate framing).

### §3.3 Files NOT modified

- Existing `apps/web/src/agent/orchestrator/extraction/` files from chunk 7.1a (ingestDocument.ts modified for Stage 3 wiring only; other files preserved).
- Chunk 7.1b Modal sidecar substrate at `sidecar-ocr/` — chunk 7.2 doesn't touch.
- `apps/web/src/services/document-platform/storageProviderService.ts` — Phase 6 byte-storage substrate; consumed at Stage 1 byte-fetch via chunk 7.1a inheritance.
- `apps/web/src/shared/schemas/document-platform/documentArtifact.schema.ts` — Phase 6 substrate; pipeline_trace JSONB column shape inherited (Phase 7 owner per Round 1 §2.1 finding); Stage 3 emits trace records via existing shape.

### §3.4 Service layer scope

Chunk 7.2 service-layer changes: `documentExceptionService.ts` ExceptionReasonSchema enum extension (1 enum value addition + verify type propagation). No new service functions; no new mutation surfaces. The classifier substrate at `agent/orchestrator/extraction/classifier/` is agent-tier per ADR-0020 §3 import boundary (agent calls services via existing interfaces; doesn't invert).

## §4 — Tasks

Tasks numbered sequentially with per-task acceptance criteria + dependency annotations + concrete partial-information value picks.

### Task 7.2.1 — Author org_settings substrate-add migration

**Scope:** Create `supabase/migrations/20240157000000_phase_7_org_settings_substrate.sql` per §2.1 substrate enumeration. Sub-Q27 column-shape resolutions inline.

**Acceptance criteria:**

- Migration file at canonical numbered location.
- CREATE TABLE org_settings with `org_id uuid PRIMARY KEY REFERENCES organizations(org_id) ON DELETE CASCADE`.
- All 11 reserved columns ship per Sub-Q27 value picks (§4 below).
- RLS policies per multi-tenant org-scoped pattern.
- Backfill INSERT INTO org_settings (org_id) SELECT FROM organizations.
- Migration applies cleanly via `supabase migration up`.
- `pnpm db:reset && pnpm db:seed:all` succeeds with new migration.

**Dependencies:** None (Task 1 entry point).

**Sub-Q27 column-shape value picks (per Phase A step 16 verification sequence outcome):**

- **`classification_fallback_order text[] NOT NULL DEFAULT ARRAY['tier_a', 'tier_c', 'tier_d']::text[]`** — text[] preserves order (sequence-sensitive per ADR-0014 §7 fallback ordering); text[] aligns with Postgres array native operations; DEFAULT initializes to v1-fixed ordering. Alternative jsonb (array of strings) considered but text[] simpler for ordered iteration at consumer-grade.
- **`ai_fallback_budget integer NOT NULL DEFAULT 2`** — integer per DEFAULT value 2; matches ADR-0014 §8 commitment "NOT NULL DEFAULT to the v1-fixed value (2)".
- **`vendor_match_threshold numeric(3,2) NOT NULL DEFAULT 0.80`** — numeric(3,2) (3 total digits; 2 decimal places) accommodates 0.00-9.99 range with 2-decimal precision; matches ADR-0014 §9 commitment "NOT NULL DEFAULT to the v1-fixed value (`0.80`)".
- **`gc_cadence text NOT NULL DEFAULT 'daily' CHECK (gc_cadence IN ('daily', 'hourly'))`** — text + CHECK constraint per Phase 5.1 chunk 5.1a DDL convention (no new ENUM types at chunk-grade; ENUM creation deferred to post-v1 if needed); v1-active values 'daily' + 'hourly' reserved for future GC cadence adjustment.
- **`gc_threshold_hours integer NOT NULL DEFAULT 24`** — integer per DEFAULT value 24; matches ADR-0014 §10 commitment.
- **`confidence_threshold_vendor_invoice numeric(3,2) DEFAULT NULL`** — numeric(3,2) consistent with vendor_match_threshold; NULL-default per ADR-0019 substrate-now-enforcement-later discipline.
- **`confidence_threshold_receipt numeric(3,2) DEFAULT NULL`** — same.
- **`confidence_threshold_payment_confirmation numeric(3,2) DEFAULT NULL`** — same.
- **`confidence_threshold_ambiguity_margin numeric(3,2) DEFAULT NULL`** — same; per ADR-0019 §2 Surface 3.5.
- **`calibration_cadence integer DEFAULT NULL`** — integer in months per ADR-0019 §4 "6 calendar months" semantics; NULL-default allows runtime fallthrough to system-fixed cadence.
- **`calibration_test_set_version text DEFAULT NULL`** — text version identifier per ADR-0019 §11 calibration test-set artifact format; NULL-default allows runtime fallthrough to platform-shipped version.
- **Common columns:** `created_at timestamptz NOT NULL DEFAULT now()` + `updated_at timestamptz NOT NULL DEFAULT now()`.

### Task 7.2.2 — Extend ExceptionReasonSchema enum

**Scope:** Add `'ai_fallback_validation_failed'` to ExceptionReasonSchema at `apps/web/src/shared/schemas/document-platform/exceptionQueueEntry.schema.ts`. Update CHECK constraint via ALTER migration if needed.

**Acceptance criteria:**

- Zod enum updated to 7 values (existing 6 + NEW).
- TypeScript type propagates cleanly through documentExceptionService.ts.
- Integration test coverage for new value at enqueueException call site.
- ALTER migration (or inline in Task 7.2.1 migration) updates Postgres CHECK constraint on `exception_queue_entries.exception_reason` column.

**Dependencies:** Task 7.2.1 (migration).

**Partial-information value picks:**

- **Enum value name `'ai_fallback_validation_failed'`** — matches ADR-0014 §12 audit event `failure_reason` enum value verbatim; consistent naming across ExceptionReasonSchema + ADR-0014 §12 audit events.
- **ALTER strategy:** inline in Task 7.2.1 migration (combine substrate-add + ExceptionReasonSchema CHECK update in single migration) for atomicity.

### Task 7.2.3 — Extend extraction types.ts with ClassificationResult

**Scope:** Extend `apps/web/src/agent/orchestrator/extraction/types.ts` from chunk 7.1a with classifier-related types.

**Acceptance criteria:**

- `ClassificationResult` type exported: `{ documentType: 'vendor_invoice' | 'receipt' | 'payment_confirmation' | 'unknown'; confidence: number; rationale: string; tier: 'A' | 'C' | 'D' }`.
- `TierAOutput` + `TierCOutput` + `TierDOutput` types exported per per-tier shape (Tier A is binary match-or-no-match per Sub-Q8; Tier C is full ClassificationResult; Tier D is `{ documentType: 'unknown' }`).
- `ClassificationInput` type exported: `{ ocrArtifact: DocumentArtifact; traceId: string }`.
- `pnpm typecheck` green.

**Dependencies:** Task 7.2.1 (no dependency; can run parallel).

### Task 7.2.4 — Seed classifier subdirectory + index.ts

**Scope:** Create `apps/web/src/agent/orchestrator/extraction/classifier/` subdirectory. Author `index.ts` classifier module entrypoint exporting `classifyDocumentType(ocrArtifact, traceId, ctx): Promise<ClassificationResult>`.

**Acceptance criteria:**

- Subdirectory created.
- `index.ts` exports `classifyDocumentType` function; invokes tierCoordination internally.
- `pnpm typecheck` green.

**Dependencies:** Task 7.2.3 (types.ts extension).

### Task 7.2.5 — Author Tier A rule modules (vendorInvoiceRules + receiptRules + paymentConfirmationRules)

**Scope:** Author per-document-type Tier A rule modules with heuristics per ADR-0014 §7 specifications.

**Acceptance criteria:**

- `vendorInvoiceRules.ts` exports `evaluateVendorInvoice(ocrArtifact): TierAOutput` — checks Invoice/Bill header patterns + vendor identity patterns + total/subtotal/tax field patterns + filename heuristics; returns binary match (with intrinsic confidence ≥ 0.85 per ADR-0014 §7) or no-match.
- `receiptRules.ts` exports `evaluateReceipt(ocrArtifact): TierAOutput` — checks receipt-shape patterns + filename heuristics; returns binary match (with intrinsic confidence ≥ 0.80) or no-match.
- `paymentConfirmationRules.ts` exports `evaluatePaymentConfirmation(ocrArtifact): TierAOutput` — checks payment language patterns + confirmation number patterns + filename heuristics; returns binary match (with intrinsic confidence ≥ 0.85) or no-match.
- Per-module unit tests cover match + no-match + multi-match scenarios.

**Dependencies:** Task 7.2.4 (subdirectory + types).

**Partial-information value picks (Tier A heuristic specifics):**

- **vendor_invoice patterns:** case-insensitive regex match for "Invoice" or "Bill" or "Statement" in document_artifacts.lines text + filename contains "invoice" or "bill" or "statement". Intrinsic confidence 0.90 (above 0.85 threshold).
- **receipt patterns:** filename contains "receipt" + document_artifacts.lines text contains "Total" near bottom + "Subtotal" / "Tax" / "Cash" / "Credit Card" payment-method line. Intrinsic confidence 0.85 (above 0.80 threshold).
- **payment_confirmation patterns:** document_artifacts.lines text contains "payment received" or "thank you for your payment" or "confirmation number" or "transaction ID" + filename contains "payment" or "confirmation". Intrinsic confidence 0.90.
- **NOTE:** specific heuristics calibrate against chunk 7.1b actual OCR output substrate at chunk 7.2 IMPL grade onset (chunk 7.1b ships at Session 37; chunk 7.2 impl at Session 38 has actual OCR output evidence). Brief-grade specifies heuristic intent + initial pattern set; impl-grade refines against actual OCR evidence.

### Task 7.2.6 — Author tierCoordination.ts

**Scope:** Author Tier A → Tier C → Tier D fallback ordering logic per Sub-Q7 + Sub-Q8 locks.

**Acceptance criteria:**

- `tierCoordination.ts` exports `coordinateTiers(ocrArtifact, traceId, ctx): Promise<ClassificationResult>`.
- Calls evaluateVendorInvoice + evaluateReceipt + evaluatePaymentConfirmation per Tier A; if multiple matches, highest-confidence-first per Sub-Q7.
- If any Tier A match with binary match-and-above-threshold: returns ClassificationResult with tier='A'.
- If no Tier A match: falls through to aiFallback per Sub-Q8.
- If Tier C returns valid output above threshold: returns ClassificationResult with tier='C'.
- If Tier C returns invalid output or below threshold: returns ClassificationResult with documentType='unknown' + tier='D'.
- pipeline_trace JSONB emission per ADR-0014 §8 contract for each tier evaluation.
- Unit tests cover tier-fallback paths + multi-match precedence.

**Dependencies:** Tasks 7.2.5 + 7.2.7.

### Task 7.2.7 — Author aiFallback.ts (Tier C Claude Sonnet invocation)

**Scope:** Author Tier C AI fallback module invoking Claude Sonnet per ADR-0014 §8 contract.

**Acceptance criteria:**

- `aiFallback.ts` exports `runAiFallback(ocrArtifact, traceId, ctx): Promise<TierCOutput>`.
- Reads ocrArtifact.lines + ocrArtifact.pages content; NEVER raw image bytes per ADR-0014 §8 Q30 discipline.
- Assembles system prompt from per-document-type prompt files (vendor_invoice + receipt + payment_confirmation).
- Invokes Claude Sonnet via Anthropic SDK at `apps/web/src/agent/extraction/sidecar/` (or analogous existing Claude SDK call-site pattern).
- Zod-validates response against ClassificationOutputSchema (discriminated union over document_type).
- Confidence-below-threshold gate: returns Tier D 'unknown' if confidence below per-document-type threshold (vendor_invoice 0.85; receipt 0.80; payment_confirmation 0.85 per ADR-0014 §7).
- Max 2 fallback calls per source document budget per ADR-0014 §8.
- pipeline_trace JSONB emission per ADR-0014 §8: `(stage_name = 'ai_fallback_classify', input_hash = SHA-256(OCR text || prompt.contentHash), output_hash = SHA-256(validatedJSON), model = 'claude-sonnet-<version>', timestamp)`.
- Audit event `ai_fallback_validation_failed` emitted via canonical audit-log writer per ADR-0011 §1 on Zod-validation failure.

**Dependencies:** Tasks 7.2.4 + 7.2.8 + 7.2.9 (prompts + schemas).

**Partial-information value picks:**

- **Claude SDK invocation pattern:** inherits from existing Claude SDK usage at `apps/web/src/agent/orchestrator/callClaude.ts` (chat-orchestrator); chunk 7.2 extracts shared Claude SDK invocation helper or reuses callClaude pattern.
- **System prompt assembly:** loads per-document-type prompts at module load (or per-call); concatenates with extraction targets per agent_architecture_policy.md §2.1.
- **Anthropic model selection:** `claude-sonnet-4-5` (or current generation per project standard); model version flows through pipeline_trace.model field for reproducibility per ADR-0007 Q30.

### Task 7.2.8 — Author per-document-type prompts at classifier/prompts/

**Scope:** Author Tier C system prompts per Sub-Q9 lock (per-document-type prompt files + content-hash versioning).

**Acceptance criteria:**

- `vendor_invoice.prompt.ts` exports `{ content: string; contentHash: string }` where contentHash is SHA-256(content).
- `receipt.prompt.ts` exports same shape.
- `payment_confirmation.prompt.ts` exports same shape.
- Each prompt content names the v1-active document_type enum + field-extraction targets per agent_architecture_policy.md §2.1 + ADR-0014 §8.
- contentHash computed at module load (or build-time script if performance warrants).

**Dependencies:** Task 7.2.4 (subdirectory).

**Partial-information value picks (Tier C prompt content):**

- **vendor_invoice prompt:** system prompt instructs Claude to classify the document as one of `[vendor_invoice, receipt, payment_confirmation, unknown]` + extract fields per agent_architecture_policy.md §2.1.1 vendor_invoice row + return JSON matching VendorInvoiceClassificationSchema.
- **receipt prompt:** analogous for receipt; extracts fields per §2.1.1 receipt row.
- **payment_confirmation prompt:** analogous for payment_confirmation; extracts fields per §2.1.1 payment_confirmation row.
- **Prompt length target:** ~500-800 tokens per prompt (concise instructions + field schema reference).
- **contentHash computation:** runtime at module load via Node crypto SHA-256 (no build-time script at v1; defer to post-v1 if performance warrants).

### Task 7.2.9 — Author per-document-type Zod schemas at shared/schemas/extraction/

**Scope:** Author per-document-type Zod schemas per Sub-Q13 lock; per-field shape adjudication per agent_architecture_policy.md §2.1 + ADR-0014 §8.

**Acceptance criteria:**

- `vendorInvoice.schema.ts` exports `VendorInvoiceClassificationSchema` Zod schema for Tier C AI fallback output.
- `receipt.schema.ts` exports `ReceiptClassificationSchema`.
- `paymentConfirmation.schema.ts` exports `PaymentConfirmationClassificationSchema`.
- `index.ts` exports `ClassificationOutputSchema` discriminated union over `document_type` field.
- Per-field shape adjudicated against agent_architecture_policy.md §2.1 per-document-type rows.
- `pnpm typecheck` green.

**Dependencies:** Task 7.2.4 (subdirectory).

**Partial-information value picks (per-field Zod schema shape):**

- **VendorInvoiceClassificationSchema:** `document_type: z.literal('vendor_invoice')` + `confidence: z.number().min(0).max(1)` + `rationale: z.string()` + `fields: { vendor_name?, vendor_tax_id?, invoice_number?, issue_date?, due_date?, currency?, amount_total?, amount_subtotal?, tax_amount?, line_items?: array(...) }` per §2.1.1 vendor_invoice row.
- **ReceiptClassificationSchema:** `document_type: z.literal('receipt')` + same confidence/rationale + `fields: { merchant_name?, receipt_date?, total?, payment_method?, ... }` per §2.1.1 receipt row.
- **PaymentConfirmationClassificationSchema:** `document_type: z.literal('payment_confirmation')` + same + `fields: { payment_amount?, payment_date?, confirmation_number?, payment_method?, payer_name?, payee_name?, ... }` per §2.1.1 payment_confirmation row.

### Task 7.2.10 — Author AI fallback mock harness

**Scope:** Author `mockAiFallback.ts` at `agent/orchestrator/extraction/classifier/__tests__/` per Sub-Q24 per-chunk-incremental test infra.

**Acceptance criteria:**

- `mockClaudeFallback(input, mockResponse?)` returns mockResponse (if provided) or synthetic ClassificationOutput.
- Supports failure injection (timeout + auth failure + non-validating output).
- Used by aiFallback.test.ts + tierCoordination.test.ts + classifier.integration.test.ts.

**Dependencies:** Task 7.2.9 (schemas).

### Tasks 7.2.11.A-E — Per-module unit tests

**Scope:** Unit tests for vendorInvoiceRules + receiptRules + paymentConfirmationRules + tierCoordination + aiFallback per §3.2 test file enumeration.

**Acceptance criteria:**

- Each test file covers golden-path + edge-case + failure scenarios per per-module behavior.
- `pnpm test` includes new unit tests; full suite green.

**Dependencies:** Tasks 7.2.5 + 7.2.6 + 7.2.7 + 7.2.10.

### Task 7.2.12 — Wire Stage 3 active at orchestrator

**Scope:** Replace chunk 7.1a Stage 3 stub at `ingestDocument.ts` with active `classifyDocumentType` invocation.

**Acceptance criteria:**

- Stage 3 stub deleted; active invocation calls `classifyDocumentType(ocrArtifact, traceId, ctx)` per Task 7.2.4 entrypoint.
- pipeline_trace records flow through orchestrator per ADR-0014 §8.
- Stages 0-2 active (chunk 7.1a + chunk 7.1b inheritance) + Stage 3 active + Stages 4-7 stub.
- Orchestrator integration test still passes (Task 7.1a.7 inheritance updated).

**Dependencies:** Tasks 7.2.4 + 7.2.6.

### Task 7.2.13 — Classifier integration test

**Scope:** Author `classifier.integration.test.ts` per §3.2.

**Acceptance criteria:**

- End-to-end Stage 3 with mock OCR artifact (synthetic document_artifacts row per chunk 7.1b mock pattern).
- Tier A match path + Tier C fallback path + Tier D unknown path all covered.
- Failure-class audit events verified for ai_fallback_validation_failed + confidence_below_threshold + extraction_failed.
- `pnpm test:integration` includes new test.

**Dependencies:** Tasks 7.2.7 + 7.2.10 + 7.2.12.

### Chunk 7.2 close gate

- `pnpm agent:validate` 26/26 green.
- `pnpm test` full vitest suite green (including new chunk 7.2 tests).
- `pnpm typecheck` green.
- `pnpm db:reset && pnpm db:seed:all` succeeds with new migration applied.
- Stage 3 classification active per ADR-0014 §1 + §7 verified via classifier integration test.
- org_settings substrate-add migration applied verified via DDL inspection + RLS policy verification.
- ExceptionReasonSchema enum extension verified via type-check + integration test.
- Per-document-type rule modules + Tier coordination + Tier C AI fallback wired into orchestrator Stage 3 verified via integration test.
- Per-document-type prompt files + content-hash versioning verified via unit test.
- Per-document-type Zod schemas verified via type-check + schema-validation test.
- AI fallback mock harness verified via mock-harness unit test.
- pipeline_trace JSONB column writes for Stage 3 verified via trace-emission test.
- Failure-class audit events per ADR-0014 §12 verified via failure-injection test (Stage 3 classifier-confidence-below-threshold + AI-fallback-validation-failed cases).

## §5 — Dependencies + sequencing

Task ordering:

1. **Task 7.2.1** (migration) — entry point; substrate-first.
2. **Tasks 7.2.2 + 7.2.3** parallel (ExceptionReasonSchema enum + types.ts extension); both depend on Task 7.2.1 (types.ts independent).
3. **Task 7.2.4** (classifier subdirectory + index.ts) depends on Task 7.2.3.
4. **Tasks 7.2.5 + 7.2.8 + 7.2.9** parallel (Tier A rules + prompts + Zod schemas); all depend on Task 7.2.4.
5. **Task 7.2.10** (mock harness) depends on Task 7.2.9.
6. **Task 7.2.7** (aiFallback.ts) depends on Tasks 7.2.4 + 7.2.8 + 7.2.9.
7. **Task 7.2.6** (tierCoordination.ts) depends on Tasks 7.2.5 + 7.2.7.
8. **Tasks 7.2.11.A-E** (unit tests) depend on Tasks 7.2.5 + 7.2.6 + 7.2.7 + 7.2.10.
9. **Task 7.2.12** (Stage 3 wiring) depends on Tasks 7.2.4 + 7.2.6.
10. **Task 7.2.13** (integration test) depends on Tasks 7.2.7 + 7.2.10 + 7.2.12.

## §6 — Acceptance criteria

Per §4 task-level criteria + §3 close gate. Summary close gates per cycle-close §5.4 chunk 7.2 acceptance criteria framework:

- `pnpm agent:validate` 26/26 green ✓
- Stage 3 classification active per ADR-0014 §1 + §7 ✓
- org_settings substrate-add migration ✓ (11 reserved columns: 5 ADR-0014 + 6 ADR-0019)
- ExceptionReasonSchema enum extension ✓
- Per-document-type rule + prompt + Zod schema modules ✓
- AI fallback mock harness ✓
- pipeline_trace JSONB writes for Stage 3 ✓
- Failure-class audit events for Stage 3 ✓

## §7 — Path C invocation outcome banking

**Path C SINGLE-CHUNK disposition at brief-grade per multi-axis evaluation (file count MODERATE-HIGH; framing-revisit count LOW; novelty LOW; settled-substrate dominance HIGH).**

- **Cycle-close §7.2 framing adjustment:** LOW → MODERATE-single-chunk (precision-grade refinement; not disposition-grade override).
- **Forecast at impl-grade:** ~1500-2200 LOC including tests (revised upward from cycle-close §7.2 750-1350 forecast band; brief-grade walk evidence accommodates ~18-22 files with tests).
- **Single-chunk feasibility holds** per settled-substrate framing — Sub-Q6/Q9/Q10/Q13/Q26 locks at scope-lock-cycle grade minimize framing-revisit risk; file count high but author-time compressed for mechanical authorship against well-defined contracts.
- **Reactive Path C contingency reserved** per F-J-14 Grain 2 (Phase-A-close prospective at impl-onset) + Grain 3 (mid-impl reactive) if chunk 7.2 impl-grade evidence surfaces unforeseen framing-revisits.

**F-J-14 catalog Phase 7 third-instance graduation does NOT fire at chunk 7.2** (chunk 7.1 prospective firing at Session 33 remains the second F-J-14 prospective instance; Phase 7 chunk 7.3 brief-grade evaluation at Session 35 may produce additional prospective firings).

## §8 — Carry-forward observations

### §8.1 Anti-drift discipline prospective firings at chunk 7.2 brief-drafting grade

**Two anti-drift firings at Session 34 brief-drafting:**

1. **Divergence (α) — ADR-0019 reserved org_settings column substrate verification clarification** (cycle-close §1.4 noted 6 columns NULL-default at v1 but did NOT enumerate column names + shape commitments; Phase A step 7 verification resolved with explicit enumeration per ADR-0019 §2 lines 399-404). Banks at option-space-framing-against-substrate sub-shape catalog at **directive-grade-citation-against-substrate sub-grain SECOND instance** (Session 33 (α) Phase 5.1 chunk 5.1a LOC citation; Session 34 (α) ADR-0019 column enumeration). Sub-grain graduates to **N=2** at chunk-brief-drafting grade.
2. **Divergence (β) — Path C probability re-evaluation at brief-grade** (cycle-close §7.2 LOW probability framing based on novelty heuristic alone; brief-grade walks multi-axis evaluation revealing MODERATE probability with single-chunk disposition holding). Banks at **catalog-substrate-characterization sub-grain SECOND instance** (Round 4 Sub-Q21 F-J-14 catalog; Session 34 Path C probability heuristic). Sub-grain graduates to **N=2** at brief-drafting grade.

**Cumulative banking at Session 34 close:**

- Option-space-framing-against-substrate sub-shape: **N=11 instances** (9 cycle-close cumulative + 1 chunk 7.1 brief-drafting + 2 chunk 7.2 brief-drafting) + **8 sub-grains** (no new sub-grain at Session 34; existing sub-grains continued firing).
- Anti-drift prospective-firing sub-discipline: **N=9 cumulative firings** (7 through Session 33 close + 2 Session 34).
- Codification candidate further strengthening at Phase 7 retrospective grade.

### §8.2 Directive-authoring multi-iteration refinement sub-grain N=6 banking

Session 34's three-iteration cycle (initial draft → refinement notes → refined directive) is the SIXTH consecutive instance per Sessions 29 + 30 + 31 + 32 + 33 + 34. Pattern firing strengthens cross-directive-grade generalization evidence:

- Scope-lock-cycle-round directive grade: Sessions 29 + 30 + 31 + 32 (N=4).
- Chunk-brief-drafting directive grade: Sessions 33 + 34 (N=2).
- **Cumulative: N=6 across grades.**

The pattern's substantive substance per Session 33 banking holds: directive-authoring multi-iteration refinement is NOT directive-grade-specific; it's directive-complexity-specific. **Codification candidate at Phase 7 retrospective grade strongly supported** at N=6 cross-directive-grade banking.

### §8.3 Multi-axis Path C probability evaluation discipline (NEW sub-discipline)

Session 34 §1.3 surfaces a new sub-discipline at chunk-brief-drafting grade: **multi-axis Path C probability evaluation**. Pattern: brief-grade Path C probability calibrates against file-count + framing-count + novelty + settled-substrate-stability axes rather than single-axis novelty heuristic (which cycle-close §7.2 framing used).

The discipline catches catalog-substrate-characterization drift at heuristic-grade — cycle-close §7.2 LOW probability framing was based on novelty alone; brief-grade walks multi-axis evaluation for tighter precision.

**N=1 banking at chunk 7.2 brief-grade**; codification threshold N=3+ per existing pattern. Future-cycle-watch at chunk 7.3 brief-grade (Session 35) + post-v1 chunks for analogous multi-axis evaluation surfaces.

### §8.4 Substrate-density-compresses-LOC observation at chunk-brief-drafting grade

Chunk 7.2 brief LOC at this artifact: TBD at session close.

Per cycle-close §4.2 flagging + Session 33 OBSERVATION-NOT-YET-APPLICABLE framing: chunk-brief-drafting LOC is different sub-curve than scope-lock-cycle LOC. Phase 7 chunk 7.1 at 592 LOC + chunk 7.2 brief target at ~600-940 LOC per corrected band. Brief-drafting cycle sub-curve characterization accumulating across Sessions 33 + 34 + 35.

**OBSERVATION-NOT-YET-APPLICABLE per cycle-close §4.2 flagging continued.** Track for sub-curve characterization at chunk 7.3 brief-drafting close (Session 35).

### §8.5 Next operational fire

Next operational fire: **Session 35 — chunk 7.3 brief-drafting** per cycle-close §9.5 sequencing. Brief-drafting cycle continues at Session 35 (chunk 7.3); chunk implementations begin at Session 36 (chunk 7.1a impl); chunk 7.2 implementation at Session 38 (post-chunk-7.1a + 7.1b implementations).

---

**Chunk 7.2 brief drafting complete at Session 34.** Path C SINGLE-CHUNK disposition at brief-grade with precision-grade adjustment of cycle-close §7.2 LOW → MODERATE-single-chunk framing per multi-axis evaluation. Sub-Q27 column-shape adjudication resolved per-column for all 11 reserved org_settings columns (5 ADR-0014 v1-active NOT NULL DEFAULT + 6 ADR-0019 NULL-default substrate-now-enforcement-later). 13 tasks (Tasks 7.2.1 through 7.2.13) enumerated with per-task acceptance criteria + dependency annotations + concrete partial-information value picks.

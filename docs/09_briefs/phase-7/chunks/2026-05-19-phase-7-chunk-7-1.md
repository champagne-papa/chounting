# Phase 7 Chunk 7.1 Brief — Orchestrator skeleton + Modal sidecar (PATH C SPLIT to 7.1a + 7.1b)

- **Date:** 2026-05-19
- **Phase:** Phase 7 (substantively-new-phase cycle; Tier 2 extraction pipeline)
- **Chunk:** 7.1 of 3 (chunk 7.1 orchestrator + Modal sidecar; chunk 7.2 classifier + dedup + org_settings substrate-add migration; chunk 7.3 extractor + matcher + proposal + UI consumer wires)
- **Path C disposition:** **SPLIT to chunk 7.1a (orchestrator skeleton + Stages 0+1 active) + chunk 7.1b (Modal sidecar + Stage 2 active)** at brief-grade per F-J-14 Grain 1 prospective; fault line = TypeScript orchestrator substrate vs Python-sidecar-deployment substrate
- **Status:** brief-drafting (Session 33; this artifact) → chunk 7.1a implementation (Session 36 per cycle-close §9.5) → chunk 7.1b implementation (Session 37)
- **Session shape:** Single-session brief drafting per Phase 5.1 chunk 5.1a + Phase 6.5 chunk 1 precedent; brief artifact carries chunk 7.1a + chunk 7.1b task decompositions inline (no separate sub-chunk briefs)
- **Predecessor:** Phase 7 scope-lock cycle close at `docs/09_briefs/phase-7/2026-05-19-phase-7-scope-lock-cycle-close.md` (commit `b964798`; 652 LOC)
- **Baseline at Session 33 onset:** HEAD = `b964798` on staging; `pnpm agent:validate` 26/26 green at session-onset

## §1 — Preamble

### §1.1 Brief context

This brief operationalizes Phase 7 chunk 7.1 at implementation-task grain. The cycle-close artifact at `b964798` locked all 27 sub-questions across Rounds 1-4 + ratified chunk decomposition + 3-chunk shipping order + Modal sidecar at chunk 7.1 placement (Sub-Q5 Path A founder-decision; Sub-Q23 inheritance). This brief converts those locks into a numbered task list with per-task acceptance criteria, dependency annotations, and concrete value picks for cycle-close §7.1 partial-information items.

**Inheritance posture.** The cycle-close artifact is authoritative for *what* chunk 7.1 ships (Stages 0-2 active per ADR-0014 §1; Modal sidecar deployment; schema-bound TS↔Python boundary per ADR-0014 §3; pipeline_trace JSONB emission per ADR-0014 §8; failure classification per ADR-0014 §12). This brief is authoritative for *how* chunk 7.1 builds it (per-stage substrate decomposition; Python sidecar repo location; Modal config shape; HMAC secret management pattern; schema-bound boundary translation pipeline; per-stage unit test patterns). The cycle-close brief wins on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

**Brief-drafting cycle vs implementation cycle distinction (per Refinement 1).** Per cycle-close §9.5 sequencing:

- **Brief-drafting cycle:** Sessions 33 (chunk 7.1 brief; this artifact) → 34 (chunk 7.2 brief) → 35 (chunk 7.3 brief). Preserved regardless of Path C split outcomes at any brief-grade.
- **Implementation cycle:** Sessions 36-N. Path C splits at brief-grade expand N (5 implementation sessions if both chunk 7.1 + chunk 7.3 split; 4 if one splits; 3 if neither). Chunk 7.1 Path C SPLIT disposition (see §1.3) places chunk 7.1a implementation at Session 36 + chunk 7.1b at Session 37; chunks 7.2/7.3 implementations follow at Sessions 38+.

The single-Session-33 brief artifact carries BOTH chunk 7.1a + chunk 7.1b task decompositions inline; no separate sub-chunk briefs at Sessions 34-35. Sessions 34-35 are dedicated to chunk 7.2 + chunk 7.3 brief-drafting per cycle-close §9.5.

### §1.2 Session-onset divergence absorption

**Divergence (α) — Phase 5.1 chunk 5.1a LOC citation correction.** Cycle-close §7.1 cited "Phase 5.1 chunk 5.1a (629 LOC after Round 4 reference)" but verify-from-disk at Session 33 Phase A step 3 confirms Phase 5.1 chunk 5.1a brief at **938 LOC** (including post-implementation Amendment section ~330 LOC; pre-amendment brief ~605 LOC). The cycle-close framing presumed the wrong LOC reference. Chunk-brief artifact LOC band is actually ~620-940 LOC (Phase 6.5 chunk 1 at 623 LOC + Phase 5.1 chunk 5.1a at 605 pre-amendment / 938 with amendment) rather than the cycle-close §7.1 inferred ~600-900 LOC.

Bank as **option-space-framing-against-substrate sub-shape instance at cycle-close-artifact-authoring grade**. Specifically: directive-grade-citation-against-substrate sub-grain (the cycle-close artifact cited Phase 5.1 chunk 5.1a LOC against incomplete substrate verification; walk-grade verify-from-disk caught the divergence at chunk-brief-drafting Phase A). Graduates the catalog further at Phase 7 retrospective grade if subsequent citations surface analogous instances.

**Divergence (β) — Modal substrate verification outcome refinement (per Refinement 2).** Phase A step 13 verification sequence outcomes:

- (a) Modal references: zero substantive substrate in apps/ code; only docs/ mentions. **Modal account + config: fully greenfield.**
- (b) Python sidecar substrate: zero `.py` files; no pyproject.toml/requirements.txt/setup.py. **Python sidecar: fully greenfield.**
- (c) Schema-bound substrate: `zod-to-json-schema@3.25.2` IS installed at `apps/web/package.json` (extending-existing); Pydantic substrate is greenfield.

Overall outcome: **mostly (d) fully-greenfield** with partial (e) extending-existing for zod-to-json-schema package only. Path C HIGH probability framing from cycle-close §7.1 forecast holds — minor zod-to-json-schema compression does not materially shift LOC forecast band (1400-2200 LOC at impl-grade per cycle-close §7.1 walk evidence).

**No other divergences at session-onset.** Substrate citation corrections from Rounds 1-3 inherited cleanly: VFD-2 (original_content_hash) + VFD-5 (pipeline_trace JSONB column on document_artifacts) + VFD-11 (PendingDocumentsView path) + VFD-13 (SplitScreenLayout path).

### §1.3 Path C invocation evaluation at brief-drafting grade — SPLIT FIRES

Per F-J-14 Grain 1 brief-draft prospective firing conditions + chunk-3-Phase-4 empirical bound (8 files + 1 migration + 1 types.ts + 5 framings) + cycle-close §7.1 forecast inheritance + Phase A step 13 Modal substrate verification outcome:

**Volume estimators at brief-drafting grade:**

| Estimator | Chunk 7.1 (combined) | Chunk-3-Phase-4 bound | Status |
|---|---|---|---|
| Source files touched (TypeScript) | ~10-12 (orchestrator + 3 stages + types + tests + Modal mock harness + .env.example update) | 8 | EXCEEDS |
| Source files created (Python) | ~3-5 (sidecar PaddleOCR wrapper + Pydantic schemas + HMAC middleware + Modal config + deployment manifest) | 0 (chunk-3 TS-only) | EXCEEDS |
| Migrations | 0 (document_artifacts substrate from Phase 6) | 1 | BELOW |
| Generated types.ts | 1 (extraction types) | 1 | EQUAL |
| Framing-revisits at brief-grade | 4+ (Modal substrate authorship grain + HMAC secret management + schema-bound boundary + Python sidecar repo authorship + deployment pipeline) | 5 | NEAR-OR-ABOVE |
| Cross-phase blast radius (services) | 2 (storageProviderService + future paymentService/billService at Stages 6-7 stub) | (case-by-case) | MODERATE |

**Path C invocation criteria evaluation:**

- **Volume estimators sum exceeds single-session reliable delivery band:** YES. TS file count (10-12) exceeds 8; Python substrate adds 3-5 net-new files outside chunk-3-Phase-4's TS-only scope.
- **Scope-lock surfaces N≥3 framings:** YES at brief-grade. Modal substrate authorship grain + HMAC secret management + schema-bound boundary + Python sidecar repo authorship + deployment pipeline = 5 framings minimum at brief-drafting grade.
- **Substantively-novel-logic scope:** YES. Python sidecar substrate is fundamentally different operational shape than TypeScript orchestrator substrate (different language; different deployment platform; different test harness; different secret management).

**Disposition: Path C SPLIT FIRES at brief-grade per Grain 1 prospective discipline.**

**Fault line declaration:** TypeScript orchestrator substrate (chunk 7.1a) vs Python-sidecar-deployment substrate (chunk 7.1b).

**Sub-chunk decomposition:**

- **Chunk 7.1a — Orchestrator skeleton + Stages 0+1 active.** Pure TypeScript substrate at `agent/orchestrator/extraction/` subtree. Stages 0 (dedup-by-hash) + 1 (byte fetch) active; Stage 2 (OCR) STUB returning placeholder (e.g., synthetic document_artifacts row for testing); Stages 3-7 STUB. Validates orchestrator skeleton + dedup-by-hash + byte-fetch + pipeline_trace emission + failure classification for Stages 0-1 + per-stage unit tests + integration test stubs.
- **Chunk 7.1b — Modal sidecar deployment + Stage 2 active.** Python sidecar substrate (PaddleOCR wrapper + Pydantic schemas + HMAC verification middleware) + Modal config + deployment pipeline + HMAC secret management + schema-bound TS↔Python boundary (zod-to-json-schema → Pydantic generation pipeline) + Stage 2 wiring at orchestrator (replaces 7.1a stub) + Modal sidecar mock harness + end-to-end test against deployed Modal sidecar.

**Each sub-chunk preserves wiring-with-tests pairing + validation-gate-green at commit boundary** per Path C invocation discipline.

**Path C disposition contingent on Phase A step 13 outcome (per Refinement 2):** outcome verified at §1.2 (β) — mostly (d) fully-greenfield substrate confirms HIGH probability framing. If outcome had surfaced (e) extending-existing substrate with material compression, Path C disposition would have shifted to single-chunk-7.1 with brief-grade reactive Path C deferred to Grain 2 (Phase-A-close prospective) at impl-onset. Outcome (d) confirms HIGH; Path C SPLIT fires at brief-grade as expected.

### §1.4 Cross-references

- **Cycle-close artifact:** `docs/09_briefs/phase-7/2026-05-19-phase-7-scope-lock-cycle-close.md` (`b964798`) — §3 sub-question lock consolidation; §5 chunk decomposition + acceptance criteria framework; §6 inter-chunk dependency map; §7.1 chunk 7.1 substrate-load expectation; §9.4 directive prompt inputs.
- **Phase 5.1 chunk 5.1a brief:** `docs/09_briefs/phase-5.1/chunks/2026-05-19-phase-5-1-chunk-5-1a.md` — chunk-brief structural template precedent (amendment-cycle grade; 938 LOC including post-impl Amendment).
- **Phase 6.5 chunk 1 brief:** `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` — chunk-brief structural template precedent (substantively-new-phase cycle grade; 623 LOC).
- **ADR-0014:** §1 orchestrator stage sequence + §2 OCR engine PaddleOCR + §3 sidecar topology Modal + HMAC + schema-bound boundary + §8 trace propagation + §12 failure classification.
- **ADR-0007:** Q30 Logic Receipt reproducibility + Q31 LLM-orchestration prohibition.
- **ADR-0013:** storage substrate; Stage 1 byte-fetch consumer contract.
- **F-J-14 catalog** at `docs/04_engineering/conventions/session/scope-lock.md` §Session-budget-feasibility verification + Path C invocation conditions (RI-7) + friction-journal F-J-14 entries — Grain 1 brief-draft prospective firing precedent.
- **Existing project substrate:**
  - `apps/web/src/services/document-platform/ingestionService.ts` — Phase 6 ingestion substrate; `handleDragDropUpload` aggregate-batch return shape `{ ingest_batch_id: string; document_count: number }`.
  - `apps/web/src/services/document-platform/storageProviderService.ts` — Phase 6 byte-storage; chunk 7.1a Stage 1 byte-fetch consumer.
  - `apps/web/src/agent/orchestrator/` directory (5 existing chat-orchestrator files; chunk 7.1a seeds `extraction/` subdirectory).
  - `apps/web/.env.example` — secret management precedent (`POSTMARK_INBOUND_WEBHOOK_SECRET` shared-secret pattern; chunk 7.1b HMAC inherits).
  - `apps/web/package.json` — `zod-to-json-schema@3.25.2` already installed (chunk 7.1b schema-bound boundary inheritance).
  - `supabase/migrations/` — latest migration `20240156000000_phase_5_1_vendor_credits_substrate.sql` (chunk 7.2 substrate-add migration `20240157000000` reserved).

## §2 — Scope

### §2.1 What chunks 7.1a + 7.1b ship

**Chunk 7.1a ships (Session 36 implementation):**

- **Orchestrator skeleton** at `apps/web/src/agent/orchestrator/extraction/ingestDocument.ts` — deterministic TypeScript function calling stages in fixed sequence per ADR-0014 §1 + ADR-0007 Q31.
- **Stage 0 dedup-by-hash** active — short-circuit on `document_artifacts.original_content_hash` match per ADR-0014 §6 (dedup-by-hash idempotency).
- **Stage 1 byte fetch** active — invokes `storageProviderService` (or analogous Phase 6 byte-fetch contract) to retrieve source document bytes.
- **Stage 2 OCR** STUB returning placeholder `document_artifacts`-shaped row for downstream-stub testing (Modal sidecar wiring deferred to chunk 7.1b).
- **Stages 3-7** STUB returning typed placeholder values per ADR-0014 §1 stage signature contract (classification, extraction, vendor matching, relationship candidate, proposal building).
- **pipeline_trace JSONB emission** for Stages 0-1 per ADR-0014 §8 trace propagation contract: `(stage_name, input_hash, output_hash, model, timestamp)` records appended to `document_artifacts.pipeline_trace` JSONB column.
- **Failure classification** for Stages 0-1 per ADR-0014 §12: transient retryable (max 3 attempts; 500ms base; exponential factor 2x; ±20% jitter; ~3.5s budget per-stage) + persistent unavailable (no retry; route to exception queue) + permanent malformed (no retry; route to exception queue with `extraction_failed` audit event).
- **Audit events** per ADR-0014 §12 for Stages 0-1 failures: `pipeline_transient_retry` + `pipeline_transient_exhausted` + `pipeline_unavailable` + `extraction_failed`.
- **Per-stage unit tests** for Stages 0-1 with mock storageProviderService + mock document_artifacts substrate.
- **Integration test stubs** for full orchestrator skeleton (Stages 0-7 end-to-end with Stages 2-7 stubbed; validates orchestrator harness even at stub-grade).

**Chunk 7.1b ships (Session 37 implementation):**

- **Python sidecar repo** at `sidecar-ocr/` (repo root subdirectory per §4.B1 partial-information value pick) — PaddleOCR wrapper module + Pydantic schemas + HMAC verification middleware.
- **Modal config** at `sidecar-ocr/modal.yaml` (or analogous) — Modal deployment manifest + Python dependencies + runtime configuration.
- **Deployment pipeline** — Modal CLI workflow documentation + deployment script + account-setup runbook.
- **HMAC secret management** — `MODAL_OCR_HMAC_SECRET` environment variable added to `apps/web/.env.example` (inherits POSTMARK_INBOUND_WEBHOOK_SECRET shared-secret pattern); secret-generation script + .env propagation pattern + rotation policy v1 (manual rotation per founder-grade decision; documented at sidecar-ocr/README.md).
- **Schema-bound TS↔Python boundary** — zod-to-json-schema export pipeline at `apps/web/src/agent/orchestrator/extraction/sidecar/schemas.ts` (extends existing zod-to-json-schema@3.25.2 package; emits JSON Schema for Pydantic consumption); Pydantic schema generation pipeline at `sidecar-ocr/schemas/` (greenfield; Pydantic models mirror Zod input/output contracts).
- **Stage 2 OCR active wiring** — orchestrator Stage 2 stub at 7.1a replaced with Modal sidecar HTTP request; X-Trace-Id header propagation; HMAC request signing.
- **Modal sidecar mock harness** at `apps/web/src/agent/orchestrator/extraction/sidecar/__tests__/mockSidecar.ts` — local mock implementation matching deployed Modal sidecar contract for unit + integration testing.
- **pipeline_trace JSONB emission** for Stage 2 per ADR-0014 §8 trace propagation: `(stage_name = 'ocr', input_hash = SHA-256 of byte content, output_hash = SHA-256 of document_artifacts.lines/pages JSON, model = 'paddleocr', engine_version, timestamp)`.
- **Failure classification** for Stage 2 per ADR-0014 §12: transient (Modal cold-start exceeded budget) + persistent (Modal endpoint not configured, HMAC secret missing) + permanent (OCR produces empty output).
- **End-to-end test** against deployed Modal sidecar (manual deployment + integration smoke test).

### §2.2 What chunks 7.1a + 7.1b do NOT ship

Deferred to **chunk 7.2** (classifier + dedup integration + org_settings substrate-add migration):

- Stage 3 classification (Tier A rule-based + Tier C Claude Sonnet AI fallback + Tier D unknown fallback) per ADR-0014 §7.
- Per-document-type Tier A rule modules at `agent/orchestrator/extraction/classifier/` subdirectory.
- Per-document-type prompt files at `agent/orchestrator/extraction/classifier/prompts/` with content-hash (SHA-256) versioning per Sub-Q9 lock.
- Per-document-type Zod schemas at `apps/web/src/shared/schemas/extraction/` per Sub-Q13 lock.
- `org_settings` substrate-add migration (~11 reserved columns: 5 ADR-0014 v1-active NOT NULL DEFAULT + 6 ADR-0019 NULL-default) at migration `20240157000000_phase_7_chunk_7_2_substrate.sql`.
- `ExceptionReasonSchema` enum extension with `ai_fallback_validation_failed` value per Sub-Q10 lock.
- Sub-Q27 column-shape adjudication for 11 reserved columns at chunk 7.2 brief grade.

Deferred to **chunk 7.3** (extractor + matcher + proposal + UI consumer wires):

- Stage 4 field extraction (per-document-type extractor modules: `vendorInvoiceExtractor.ts` + `receiptExtractor.ts` + `paymentConfirmationExtractor.ts` at `agent/orchestrator/extraction/`).
- Stage 5 vendor matching (`vendorService.matchVendor` extension at `services/spend/vendorService.ts` per Sub-Q14.b lock).
- Stage 6 relationship candidate (via documentRouterService.completeCandidate per Phase 4 substrate).
- Stage 7 proposal building (ProposedMutation / ProposedMutationBundle / ProposedAttachment routing per ADR-0014 §11 + Sub-Q15 bifurcated render per Sub-Q18 inheritance).
- canvasDirective extension with new `proposed_attachment_card` member at v1 (snake_case) per Sub-Q19 lock.
- DocumentCard per-card state machine extension per Sub-Q20 lock.

## §3 — Substrate touchpoints

Verify-from-disk at session-onset (Session 36 implementation for chunk 7.1a; Session 37 implementation for chunk 7.1b) before consuming any line/path reference below. All counts/paths confirmed at Session 33 brief-drafting against HEAD `b964798`.

### §3.1 Files modified

**Chunk 7.1a (Session 36):**

- `apps/web/src/services/document-platform/ingestionService.ts` — extend `handleDragDropUpload` (or analogous orchestrator-invocation hook) to invoke `ingestDocument(orgId, sourceDocumentId, traceId)` post-ingestion-commit per cycle-close §6.2 service dependency framing. Touch grain: small (~20-40 LOC addition). Preserve: existing DragDropUploadResult return shape; existing Phase 6 substrate consumer contracts.

**Chunk 7.1b (Session 37):**

- `apps/web/.env.example` — add `MODAL_OCR_HMAC_SECRET` variable + comment block analogous to existing POSTMARK_INBOUND_WEBHOOK_SECRET pattern. Touch grain: small (~5-10 LOC).
- `apps/web/src/agent/orchestrator/extraction/ingestDocument.ts` — replace Stage 2 stub with Modal sidecar HTTP invocation; preserve Stages 0-1 active behavior + Stages 3-7 stub.

### §3.2 Files created

**Chunk 7.1a (Session 36):**

- `apps/web/src/agent/orchestrator/extraction/ingestDocument.ts` — orchestrator entry function; ~150-250 LOC estimated.
- `apps/web/src/agent/orchestrator/extraction/types.ts` — PipelineStageRecord + ingestDocument input/output types; ~80-120 LOC estimated.
- `apps/web/src/agent/orchestrator/extraction/stages/dedupByHash.ts` — Stage 0 substrate; ~80-120 LOC estimated.
- `apps/web/src/agent/orchestrator/extraction/stages/byteFetch.ts` — Stage 1 substrate; ~80-120 LOC estimated.
- `apps/web/src/agent/orchestrator/extraction/stages/runOCR.stub.ts` — Stage 2 stub returning synthetic document_artifacts row; ~40-60 LOC estimated (replaced at chunk 7.1b).
- `apps/web/src/agent/orchestrator/extraction/__tests__/dedupByHash.test.ts` — Stage 0 unit tests; ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/__tests__/byteFetch.test.ts` — Stage 1 unit tests; ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/__tests__/ingestDocument.integration.test.ts` — orchestrator skeleton integration test (Stages 0-7 with Stages 2-7 stubbed); ~150-200 LOC.

**Chunk 7.1b (Session 37):**

- `sidecar-ocr/` (new repo-root subdirectory; greenfield Python sidecar).
- `sidecar-ocr/modal.yaml` — Modal deployment config; ~30-50 LOC.
- `sidecar-ocr/requirements.txt` — Python dependencies (paddleocr + modal + pydantic + hmac); ~10-20 LOC.
- `sidecar-ocr/main.py` — Modal entrypoint + PaddleOCR wrapper; ~150-250 LOC.
- `sidecar-ocr/schemas/extraction.py` — Pydantic schemas mirroring Zod input/output contracts; ~80-120 LOC.
- `sidecar-ocr/middleware/hmac.py` — HMAC verification middleware; ~50-80 LOC.
- `sidecar-ocr/README.md` — deployment runbook + account setup + secret rotation policy; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/sidecar/schemas.ts` — zod-to-json-schema export pipeline; ~60-100 LOC.
- `apps/web/src/agent/orchestrator/extraction/sidecar/client.ts` — Modal sidecar HTTP client with HMAC signing; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/sidecar/__tests__/mockSidecar.ts` — local mock harness; ~80-120 LOC.
- `apps/web/src/agent/orchestrator/extraction/stages/runOCR.ts` — Stage 2 active (replaces stub from 7.1a); ~100-150 LOC.
- `apps/web/src/agent/orchestrator/extraction/__tests__/runOCR.test.ts` — Stage 2 unit tests with mock sidecar; ~120-180 LOC.
- `apps/web/src/agent/orchestrator/extraction/__tests__/sidecar.e2e.test.ts` — end-to-end test against deployed Modal sidecar (manual deployment gate); ~80-120 LOC.

### §3.3 Files NOT modified

- `apps/web/src/services/document-platform/storageProviderService.ts` — Phase 6 byte-storage substrate; consumed at Stage 1 byte-fetch via existing signature.
- `apps/web/src/shared/schemas/document-platform/documentArtifact.schema.ts` — Phase 6 substrate; pipeline_trace JSONB column shape inherited (`z.unknown()` per Phase 7 owner per Round 1 §2.1 finding).
- `supabase/migrations/` — no migrations at chunks 7.1a + 7.1b (org_settings substrate-add migration at chunk 7.2; document_artifacts substrate from Phase 6 holds).
- Existing `apps/web/src/agent/orchestrator/` files (`buildSystemPrompt.ts` + `callClaude.ts` + `index.ts` + `loadOrCreateSession.ts` + `toolsForPersona.ts`) — chat-orchestrator substrate preserved; chunk 7.1a seeds `extraction/` subdirectory without touching chat-orchestrator files.

### §3.4 Service layer scope

**Chunk 7.1a:** `apps/web/src/services/document-platform/ingestionService.ts` adds orchestrator invocation hook (single-call extension; Pattern B external-wrap inheritance per Sub-Q16+Q17 commit-grade routing locks — though commits don't fire at chunks 7.1a/7.1b since Stages 6-7 are stubbed; commit-grade routing activates at chunk 7.3).

**Chunk 7.1b:** No service-layer touches at chunk 7.1b. Modal sidecar substrate is external (Python repo + Modal deployment); TS-side substrate at `agent/orchestrator/extraction/sidecar/` is agent-tier per ADR-0020 §3 import boundary (agent calls services; services don't call agent).

## §4 — Tasks

**Strict-sequence chunks 7.1a → 7.1b** per Path C fault line + sequential dependency. Within each sub-chunk: tasks numbered sequentially. Per-task: scope + acceptance criteria + dependency annotations + partial-information value picks.

### Chunk 7.1a tasks (Session 36 implementation)

#### Task 7.1a.1 — Seed `agent/orchestrator/extraction/` subdirectory + types.ts substrate

**Scope:** Create `apps/web/src/agent/orchestrator/extraction/` subdirectory. Author `types.ts` with PipelineStageRecord + IngestDocumentInput + IngestDocumentOutput + per-stage typed contracts per ADR-0014 §1 + §8 trace propagation contract.

**Acceptance criteria:**

- Subdirectory created at `apps/web/src/agent/orchestrator/extraction/` (sibling to existing chat-orchestrator files at `agent/orchestrator/`).
- `types.ts` exports `PipelineStageRecord` type with `(stage_name, input_hash, output_hash, model, timestamp)` shape per ADR-0014 §8.
- `types.ts` exports `IngestDocumentInput` (orgId + sourceDocumentId + traceId) + `IngestDocumentOutput` (proposal + pipeline_trace) types.
- `types.ts` exports per-stage typed contracts (`Stage0Input` + `Stage0Output` + ... + `Stage7Input` + `Stage7Output`).
- `pnpm typecheck` green.
- `pnpm test:unit` green (types.ts has no runtime behavior; passes by default).

**Dependencies:** None (Task 1 entry point).

**Partial-information value picks:**

- **PipelineStageRecord.timestamp shape:** `string` ISO-8601 (matches existing `audit_log.created_at` pattern at Phase 6 substrate).
- **PipelineStageRecord.model field cardinality:** required for AI-fallback stages (Stage 3 Tier C); optional for non-AI stages (Stages 0-2 OCR + Stages 4-7 extraction). Type as `string | null` with null at non-AI stages; brief-grade decision.

#### Task 7.1a.2 — Author orchestrator entry function

**Scope:** Author `agent/orchestrator/extraction/ingestDocument.ts` orchestrator entry function calling Stages 0-7 in fixed sequence per ADR-0014 §1. Stages 0-1 active; Stages 2-7 stub returning synthetic typed outputs.

**Acceptance criteria:**

- `ingestDocument(input: IngestDocumentInput): Promise<IngestDocumentOutput>` exported function.
- Sequential stage invocation per ADR-0014 §1 stage order.
- `trace_id` propagation through `ServiceContext` per Service Communication Rule 5.
- Stages 0-1 active via Task 7.1a.3 + Task 7.1a.4.
- Stages 2-7 stub returning synthetic typed outputs (Stage 2 returns synthetic `document_artifacts` row; Stages 3-7 return typed placeholder values).
- `pnpm typecheck` green.

**Dependencies:** Task 7.1a.1 (types.ts).

**Partial-information value picks:**

- **Filename `ingestDocument.ts`** per ADR-0014 §1 illustrative orchestrator shape. Function name matches filename.

#### Task 7.1a.3 — Implement Stage 0 dedup-by-hash

**Scope:** Author `agent/orchestrator/extraction/stages/dedupByHash.ts` — Stage 0 short-circuit on `document_artifacts.original_content_hash` match per ADR-0014 §6.

**Acceptance criteria:**

- `dedupByHash(orgId, sourceDocumentId, traceId): Promise<DedupResult>` where `DedupResult = { shortCircuited: true; existingProposal } | { shortCircuited: false }`.
- Reads `source_documents.original_content_hash` (per VFD-2 substrate citation correction; NOT `hash`); queries `document_artifacts` for prior row matching the hash.
- On match: returns existingProposal; orchestrator short-circuits (returns from Task 7.1a.2 ingestDocument early).
- On no-match: returns `shortCircuited: false`; orchestrator proceeds to Stage 1.
- pipeline_trace JSONB emission per ADR-0014 §8: `(stage_name = 'dedup_by_hash', input_hash = sourceDocumentId, output_hash = SHA-256(dedupResult), model = null, timestamp)`.
- Unit tests at `agent/orchestrator/extraction/__tests__/dedupByHash.test.ts` covering: match path + no-match path + Postgres query error path (transient retryable per ADR-0014 §12.1).
- `pnpm test` includes new unit tests; integration test suite preserves.

**Dependencies:** Task 7.1a.2 (orchestrator entry).

**Partial-information value picks:**

- **Stage name string `'dedup_by_hash'`** for pipeline_trace + audit events (matches ADR-0014 §1 illustrative orchestrator shape).
- **Query pattern:** direct Supabase query via `ServiceContext.supabase` client (Pattern B inheritance per Phase 5.1 services).

#### Task 7.1a.4 — Implement Stage 1 byte fetch

**Scope:** Author `agent/orchestrator/extraction/stages/byteFetch.ts` — Stage 1 byte fetch via `storageProviderService` per ADR-0013 storage substrate.

**Acceptance criteria:**

- `byteFetch(sourceDocumentId, ctx): Promise<Uint8Array>` invokes `storageProviderService.fetch(sourceDocumentId, ctx)` per Phase 6 byte-storage consumer contract.
- pipeline_trace JSONB emission: `(stage_name = 'byte_fetch', input_hash = sourceDocumentId, output_hash = SHA-256(bytes), model = null, timestamp)`.
- Unit tests covering: successful fetch + storage error (persistent unavailable per ADR-0014 §12.2) + malformed bytes (permanent malformed per ADR-0014 §12.3).
- `pnpm test` includes new unit tests.

**Dependencies:** Task 7.1a.2 (orchestrator entry).

**Partial-information value picks:**

- **Stage name string `'byte_fetch'`** for pipeline_trace + audit events.

#### Task 7.1a.5 — Implement Stage 2 stub

**Scope:** Author `agent/orchestrator/extraction/stages/runOCR.stub.ts` — synthetic `document_artifacts` row returner for testing Stages 3-7 stubs in chunk 7.1a (replaced at chunk 7.1b Task 7.1b.6).

**Acceptance criteria:**

- `runOCR(bytes, traceId): Promise<DocumentArtifact>` returns synthetic typed `DocumentArtifact` (engine='paddleocr', engine_version='stub-v1', lines/pages JSON containing single placeholder line, quality_flags=['stub']).
- pipeline_trace JSONB emission: `(stage_name = 'ocr', input_hash = SHA-256(bytes), output_hash = SHA-256(syntheticArtifact), model = 'paddleocr-stub', timestamp)`.
- File explicitly named `runOCR.stub.ts` to flag stub-status; chunk 7.1b Task 7.1b.6 renames to `runOCR.ts` upon Stage 2 active wiring.

**Dependencies:** Task 7.1a.2 (orchestrator entry).

**Partial-information value picks:**

- **Stub filename `runOCR.stub.ts`** explicitly suffixed (rather than runOCR.ts with stub flag); rename pattern at chunk 7.1b.

#### Task 7.1a.6 — Implement failure classification for Stages 0-1

**Scope:** Author per-stage failure classification per ADR-0014 §12 for Stages 0-1: transient retryable (max 3 attempts; exponential backoff; ~3.5s budget per-stage) + persistent unavailable + permanent malformed.

**Acceptance criteria:**

- Stages 0-1 wrap throw paths in failure classification logic per ADR-0014 §12.
- Transient retryable class: retry with exponential backoff (500ms base; 2x factor; ±20% jitter); after max 3 attempts → typed ServiceError with `code = 'PIPELINE_TRANSIENT_EXHAUSTED'`.
- Persistent unavailable class: no retry; immediate route to exception queue with `pipeline_unavailable` audit event.
- Permanent malformed class: no retry; immediate route to exception queue with `extraction_failed` audit event + `failure_reason` field.
- Audit events emitted via canonical audit-log writer per ADR-0011 §1.
- Unit tests covering all three failure classes for Stages 0-1.

**Dependencies:** Tasks 7.1a.3 + 7.1a.4.

**Partial-information value picks:**

- **ServiceErrorCode `'PIPELINE_TRANSIENT_EXHAUSTED'`** per ADR-0014 §12.1 verbatim.
- **`failure_reason` enum values:** `document_corrupted` + `ocr_empty_output` + `ai_fallback_validation_failed` + `confidence_below_threshold` per ADR-0014 §12.3 verbatim.

#### Task 7.1a.7 — Integration test for orchestrator skeleton

**Scope:** Author `agent/orchestrator/extraction/__tests__/ingestDocument.integration.test.ts` — orchestrator skeleton integration test against test database; Stages 0-1 active; Stages 2-7 stubbed.

**Acceptance criteria:**

- Test invokes `ingestDocument` against test database; verifies Stages 0-1 actual behavior + Stages 2-7 stub passthrough.
- Test covers: golden path (Stages 0-7 with stubs) + dedup-by-hash short-circuit + Stage 0 failure path + Stage 1 failure path.
- pipeline_trace JSONB column read-back verifies all stage records emitted.
- Test included in `pnpm test:integration` suite.

**Dependencies:** Tasks 7.1a.2 + 7.1a.3 + 7.1a.4 + 7.1a.5 + 7.1a.6.

**Partial-information value picks:**

- **Test database seed:** existing `source_documents` + `document_artifacts` fixtures per Phase 6 integration test patterns.

#### Task 7.1a.8 — Orchestrator invocation hook at ingestionService

**Scope:** Extend `apps/web/src/services/document-platform/ingestionService.ts` to invoke `ingestDocument` post-ingestion-commit per cycle-close §6.2 service dependency framing.

**Acceptance criteria:**

- `ingestionService.handleDragDropUpload` (or analogous hook) invokes `ingestDocument(orgId, sourceDocumentId, traceId)` post-Phase-6-ingestion-commit per Sub-Q2 sync v1 invocation lock.
- Sync invocation; no queue substrate at v1.
- `pnpm typecheck` green.
- `pnpm test` preserves Phase 6 ingestionService tests.

**Dependencies:** Task 7.1a.7.

**Partial-information value picks:**

- **Invocation site:** post-ingestion-commit within `handleDragDropUpload` body (after document_jobs row INSERT; before return). Brief-grade decision pending Phase 6 substrate review at impl-onset.

#### Chunk 7.1a close gate

- `pnpm agent:validate` 26/26 green.
- `pnpm test` full vitest suite green (including new chunk 7.1a tests).
- `pnpm typecheck` green.
- `pnpm test:e2e` Playwright harness green (golden-path manual-walkable verification).
- Stages 0-1 active per ADR-0014 §1 verified via integration test.
- pipeline_trace JSONB writes for Stages 0-1 verified via trace-emission test.
- Failure-class audit events per ADR-0014 §12 for Stages 0-1 verified via failure-injection test.

### Chunk 7.1b tasks (Session 37 implementation)

#### Task 7.1b.1 — Author Python sidecar repo at sidecar-ocr/

**Scope:** Create `sidecar-ocr/` subdirectory at repo root. Author `requirements.txt` + `main.py` + `schemas/extraction.py` + `middleware/hmac.py` + `README.md`.

**Acceptance criteria:**

- `sidecar-ocr/requirements.txt` lists Python dependencies: `paddleocr`, `modal`, `pydantic`, `hmac` (stdlib), `hashlib` (stdlib).
- `sidecar-ocr/main.py` exports Modal entrypoint with `@modal.web_endpoint` decorator; PaddleOCR wrapper + HMAC verification middleware + Pydantic input/output validation.
- `sidecar-ocr/schemas/extraction.py` defines Pydantic models mirroring Zod input/output contracts (OCRRequest + OCRResponse + DocumentArtifact).
- `sidecar-ocr/middleware/hmac.py` implements HMAC-SHA256 request signature verification using shared MODAL_OCR_HMAC_SECRET.
- `sidecar-ocr/README.md` documents deployment runbook + Modal account setup + secret rotation policy v1.

**Dependencies:** None (chunk 7.1b entry point; sequenced after chunk 7.1a close).

**Partial-information value picks:**

- **Repo location `sidecar-ocr/`** at repo root subdirectory (not under `apps/web/src/`) per Python-substrate-outside-TypeScript-app discipline.
- **PaddleOCR model selection:** PaddleOCR v3 PP-OCRv4 (default; documented at sidecar-ocr/README.md).
- **HMAC algorithm:** SHA-256 (matches POSTMARK_INBOUND_WEBHOOK_SECRET pattern at Phase 6 chunk 6.3a).
- **Secret rotation policy v1:** manual rotation; runbook documents rotation steps; no automated rotation at v1.

#### Task 7.1b.2 — Modal config + deployment pipeline

**Scope:** Author `sidecar-ocr/modal.yaml` Modal deployment config + Modal CLI workflow + deployment script.

**Acceptance criteria:**

- `sidecar-ocr/modal.yaml` defines Modal app + Python dependencies + runtime configuration.
- Modal CLI workflow documented at `sidecar-ocr/README.md` (`modal deploy` invocation + account-setup steps).
- Deployment script at `sidecar-ocr/deploy.sh` invokes Modal CLI with appropriate auth.
- Manual deployment validates Modal endpoint accessible via HTTP.

**Dependencies:** Task 7.1b.1.

**Partial-information value picks:**

- **Modal app name `chounting-ocr-sidecar`** per project namespace convention.
- **Modal endpoint URL pattern:** Modal auto-generates per app + endpoint name; documented at sidecar-ocr/README.md.

#### Task 7.1b.3 — HMAC secret management

**Scope:** Add `MODAL_OCR_HMAC_SECRET` to `apps/web/.env.example` + secret-generation script + .env propagation pattern.

**Acceptance criteria:**

- `apps/web/.env.example` adds `MODAL_OCR_HMAC_SECRET=your-modal-ocr-hmac-secret` with comment block analogous to existing POSTMARK_INBOUND_WEBHOOK_SECRET pattern.
- Secret-generation script at `sidecar-ocr/generate-secret.sh` produces 32-byte random secret + outputs to stdout for manual .env propagation.
- `sidecar-ocr/README.md` documents secret propagation pattern: generate secret → set MODAL_OCR_HMAC_SECRET in apps/web/.env.local → set same value in Modal secret store via `modal secret create`.

**Dependencies:** Task 7.1b.1.

**Partial-information value picks:**

- **Secret variable name `MODAL_OCR_HMAC_SECRET`** (snake-case caps per .env convention).
- **Secret length 32 bytes (256 bits)** per HMAC-SHA256 security best practice.

#### Task 7.1b.4 — Schema-bound TS↔Python boundary

**Scope:** Author `apps/web/src/agent/orchestrator/extraction/sidecar/schemas.ts` — zod-to-json-schema export pipeline from Zod TS schemas to JSON Schema files consumed by Pydantic.

**Acceptance criteria:**

- `apps/web/src/agent/orchestrator/extraction/sidecar/schemas.ts` exports Zod schemas (OCRRequestSchema + OCRResponseSchema) matching Pydantic models at sidecar-ocr/schemas/extraction.py.
- Build-time script (or runtime export) emits JSON Schema files at `sidecar-ocr/schemas/json/` for Pydantic synchronization verification.
- Schema-roundtrip test verifies Zod-to-JSON-Schema-to-Pydantic boundary preserves shape.

**Dependencies:** Task 7.1b.1 (Pydantic schemas at sidecar-ocr/schemas/extraction.py).

**Partial-information value picks:**

- **JSON Schema location `sidecar-ocr/schemas/json/`** (sibling to Pydantic schemas).
- **Sync mechanism:** manual sync at v1 (developer runs build-time script after Zod schema changes); automated sync at post-v1 if drift surfaces.

#### Task 7.1b.5 — Modal sidecar HTTP client at TS-side

**Scope:** Author `apps/web/src/agent/orchestrator/extraction/sidecar/client.ts` — TS HTTP client invoking Modal sidecar with HMAC signing + X-Trace-Id header propagation.

**Acceptance criteria:**

- `invokeSidecar(bytes, traceId, ctx): Promise<OCRResponse>` invokes Modal sidecar HTTP endpoint with HMAC-SHA256 signature header + X-Trace-Id header.
- Reads MODAL_OCR_HMAC_SECRET from environment at invocation time.
- Response validated against OCRResponseSchema per Task 7.1b.4.
- Failure classification per ADR-0014 §12 wraps HTTP errors: transient (Modal cold-start timeout) + persistent (HMAC secret missing; endpoint unreachable) + permanent (response Zod-validation fails).

**Dependencies:** Task 7.1b.4.

**Partial-information value picks:**

- **HTTP client library:** native Next.js `fetch` (no additional dependency).
- **Timeout budget:** 10s per-request (Modal cold-start may take 5-8s; budget per ADR-0014 §12.1 transient class).

#### Task 7.1b.6 — Stage 2 active wiring at orchestrator

**Scope:** Replace chunk 7.1a Stage 2 stub at `agent/orchestrator/extraction/stages/runOCR.stub.ts` with active Stage 2 wiring at `agent/orchestrator/extraction/stages/runOCR.ts`.

**Acceptance criteria:**

- `agent/orchestrator/extraction/stages/runOCR.ts` invokes `invokeSidecar` from Task 7.1b.5 with byte content + traceId.
- Stage 2 returns `DocumentArtifact` from sidecar response.
- pipeline_trace JSONB emission: `(stage_name = 'ocr', input_hash = SHA-256(bytes), output_hash = SHA-256(documentArtifact), model = 'paddleocr', engine_version = response.engine_version, timestamp)`.
- chunk 7.1a's `runOCR.stub.ts` deleted (Stage 2 stub no longer needed).
- Orchestrator entry function unchanged (calls `runOCR` per same signature).
- Unit tests at `agent/orchestrator/extraction/__tests__/runOCR.test.ts` cover active path with mock sidecar (Task 7.1b.7).

**Dependencies:** Task 7.1b.5.

**Partial-information value picks:**

- **Filename rename `runOCR.stub.ts` → `runOCR.ts`** via delete + create pattern (or git mv if cleaner at impl-onset).

#### Task 7.1b.7 — Modal sidecar mock harness

**Scope:** Author `apps/web/src/agent/orchestrator/extraction/sidecar/__tests__/mockSidecar.ts` — local mock implementation matching deployed Modal sidecar contract for testing.

**Acceptance criteria:**

- `mockSidecar(request: OCRRequest): OCRResponse` returns synthetic OCR output matching Pydantic schema contract.
- Mock supports configurable failure injection (transient timeout + persistent unavailable + permanent malformed) for failure-classification testing.
- Used in Task 7.1b.8 unit tests + Task 7.1b.9 integration tests.

**Dependencies:** Tasks 7.1b.4 + 7.1b.5.

**Partial-information value picks:**

- **Mock implementation pattern:** synchronous function returning Promise; supports failure-injection via constructor parameter.

#### Task 7.1b.8 — Unit tests for Stage 2 active

**Scope:** Author `agent/orchestrator/extraction/__tests__/runOCR.test.ts` — Stage 2 unit tests using mockSidecar.

**Acceptance criteria:**

- Tests cover: golden-path OCR + HMAC signature failure (persistent) + Modal cold-start timeout (transient retryable; max 3 retries) + sidecar response Zod-validation failure (permanent malformed).
- pipeline_trace JSONB emission verified per failure class.
- `pnpm test` includes new unit tests.

**Dependencies:** Task 7.1b.6 + 7.1b.7.

#### Task 7.1b.9 — End-to-end test against deployed Modal sidecar

**Scope:** Author `agent/orchestrator/extraction/__tests__/sidecar.e2e.test.ts` — manual deployment gate + integration smoke test against deployed Modal sidecar.

**Acceptance criteria:**

- Test invokes `ingestDocument` against test database with real Modal sidecar deployment.
- Test gated behind MODAL_OCR_HMAC_SECRET environment variable presence (skipped if absent; runs if present).
- Test verifies Stage 2 OCR active path end-to-end with deployed Modal sidecar.
- `pnpm test:e2e` includes new E2E test (or analogous gated suite).

**Dependencies:** Tasks 7.1b.1 + 7.1b.2 + 7.1b.3 + 7.1b.5 + 7.1b.6.

**Partial-information value picks:**

- **Test gate:** environment variable presence (skip if absent); allows CI to run without Modal credentials.

#### Chunk 7.1b close gate

- `pnpm agent:validate` 26/26 green.
- `pnpm test` full vitest suite green (including new chunk 7.1b unit tests).
- `pnpm typecheck` green.
- `pnpm test:e2e` Playwright harness green (golden-path manual-walkable verification with deployed Modal sidecar).
- Stage 2 OCR active per ADR-0014 §1 verified via integration test against deployed Modal sidecar.
- Schema-bound TS↔Python boundary verified via schema-roundtrip test.
- HMAC signature verification working (failure-injection test passes).
- pipeline_trace JSONB column writes for Stage 2 verified via trace-emission test.
- Failure-class audit events per ADR-0014 §12 for Stage 2 verified via failure-injection test.

## §5 — Path C invocation outcome banking

**Path C SPLIT disposition fired at brief-grade per F-J-14 Grain 1 prospective discipline.**

- **Fault line:** TypeScript orchestrator substrate (chunk 7.1a) vs Python-sidecar-deployment substrate (chunk 7.1b).
- **Sub-chunk forecast at impl-grade:**
  - Chunk 7.1a: ~700-1100 LOC (orchestrator skeleton + Stages 0-1 active + Stages 2-7 stubs + unit tests + integration test stub + ingestionService extension).
  - Chunk 7.1b: ~700-1100 LOC (Python sidecar + Modal config + HMAC management + TS-side sidecar client + Stage 2 active wiring + unit tests + E2E test).
  - Combined: ~1400-2200 LOC (matches cycle-close §7.1 forecast inheritance).
- **Sub-chunk shipping order:** chunk 7.1a → chunk 7.1b per sequential dependency (Stage 2 active at 7.1b requires Stages 0-1 substrate at 7.1a).
- **Wiring-with-tests pairing preserved at each commit boundary per Path C invocation discipline.**
- **Validation-gate-green at each commit non-negotiable** per RI-7 + F-J-14 catalog.

**Banking entry — F-J-14 third instance at Phase 7 brief-grade.** Per F-J-14 catalog (Phase 4 chunk 3 reactive + Phase 6 chunk 6.2a prospective second-instance graduation), Phase 7 chunk 7.1 invocation is the THIRD instance — prospective at brief-grade (Grain 1). Strengthens the catalog's prospective-at-brief-grade pattern at N=2 cumulative prospective firings (Phase 6 chunk 6.2a + Phase 7 chunk 7.1).

## §6 — Carry-forward observations

### §6.1 Anti-drift discipline prospective firings at brief-drafting grade

**Two anti-drift firings at Session 33 brief-drafting:**

1. **Divergence (α) — Phase 5.1 chunk 5.1a LOC citation correction** (cycle-close §7.1 cited 629 LOC; actual is 938 LOC including amendment / 605 pre-amendment). Banks at option-space-framing-against-substrate sub-shape catalog at directive-grade-citation-against-substrate sub-grain (NEW sub-grain at brief-drafting grade).
2. **Divergence (β) — Modal substrate verification outcome refinement** (Phase A step 13 confirmed mostly fully-greenfield + partial extending-existing for zod-to-json-schema only; Path C HIGH probability framing held; outcome (e) extending-existing did NOT fire materially). Anti-drift framing at directive §1.3 (per Refinement 2) correctly contingentized Path C disposition on verification outcome; walk-grade evidence confirmed (d) outcome.

**Cumulative banking at Session 33 close:**

- Option-space-framing-against-substrate sub-shape: N=9 instances (8 cycle-close cumulative + 1 brief-drafting at Session 33) + 7 sub-grain catalog (6 cycle-close cumulative + 1 NEW at brief-drafting — directive-grade-citation-against-substrate).
- Anti-drift prospective-firing sub-discipline: N=7 cumulative firings (6 cycle-close cumulative + 1 brief-drafting Session 33).
- Codification candidate strengthening at Phase 7 retrospective grade.

### §6.2 Directive-authoring multi-iteration refinement sub-grain N=5 banking

Session 33's three-iteration cycle (initial draft → refinement notes [Path C split sequencing + Modal verification → §1.3 disposition linkage] → refined directive) is the FIFTH consecutive instance per Sessions 29 + 30 + 31 + 32 + 33. The pattern extends from scope-lock-cycle-round directive grade (Sessions 29-32) to chunk-brief-drafting directive grade (Session 33) — empirically validated across two distinct directive grades.

**Generalization observation (per founder note):** Complex governance-critical work at any directive grade (scope-lock-cycle-round OR chunk-brief-drafting) benefits from explicit three-iteration refinement cycle. The pattern is NOT directive-grade-specific; it's directive-complexity-specific.

**Codification candidate at Phase 7 retrospective grade strongly supported** at N=5 with cross-directive-grade generalization evidence.

### §6.3 Substrate-density-compresses-LOC observation at chunk-brief-drafting grade

Chunk 7.1 brief LOC at this artifact: TBD at session close.

Per cycle-close §4.2 flagging: chunk-brief-drafting LOC is a different sub-curve than scope-lock-cycle LOC. Phase 5.1 chunk 5.1a brief at 938 LOC (605 pre-amendment) + Phase 6.5 chunk 1 at 623 LOC establish chunk-brief-drafting band at ~600-940 LOC range. Phase 7 chunk 7.1 brief targets ~700-900 LOC band per chunk-brief-drafting precedent inheritance.

**OBSERVATION-NOT-YET-APPLICABLE at brief-drafting grade per cycle-close §4.2 flagging.** Track for future evidence at chunks 7.2 + 7.3 brief-drafting + chunk implementations.

### §6.4 F-J-14 catalog Phase 7 third-instance graduation

Phase 7 chunk 7.1 Path C invocation at brief-grade is the **third instance** in the F-J-14 catalog per cycle-close §10.6 framing (Phase 4 chunk 3 reactive first instance; Phase 6 chunk 6.2a prospective second-instance graduation; Phase 7 chunk 7.1 prospective third instance). Strengthens the catalog's prospective-at-brief-grade pattern.

Bank at Phase 7 retrospective grade as F-J-14 prospective-at-brief-grade sub-pattern N=2 firing (Phase 6 chunk 6.2a + Phase 7 chunk 7.1).

### §6.5 Next operational fire

Next operational fire: **Session 34 — chunk 7.2 brief-drafting** per cycle-close §9.5 sequencing. Brief-drafting cycle continues at Sessions 34 (chunk 7.2) → 35 (chunk 7.3). Chunk implementations begin at Session 36 (chunk 7.1a implementation).

---

**Chunk 7.1 brief drafting complete at Session 33.** Path C SPLIT disposition fired at brief-grade per F-J-14 Grain 1 prospective; chunk 7.1 ships as chunk 7.1a (orchestrator skeleton + Stages 0+1 active) + chunk 7.1b (Modal sidecar + Stage 2 active) at sequential implementation grade per cycle-close §9.5 sequencing (Sessions 36 + 37). Brief artifact carries both sub-chunk task decompositions inline per single-Session-33 brief-drafting discipline.

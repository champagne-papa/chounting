# Phase 6.5 Chunk 3 Brief — Chat-drop + staged attachments + PendingDocumentsView + DocumentIntakeRail deletion

- **Date:** 2026-05-16
- **Phase:** Phase 6.5 (amendment cycle per Phase 2.5 precedent)
- **Chunk:** 3 of 3 (chunk 1 shipped at `5a9492b`; chunk 2 shipped at two-commit chain `94b0411` + `c5d7e89` per Path C Arc β Phase-A-close-prospective; chunk 3 is terminal Phase 6.5 implementation chunk)
- **Status:** brief-drafting (Session 11) → scope-lock or implementation prep (Session 12, may collapse per Session 6 chunk-1 and Session 9 chunk-2 precedents) → implementation + chunk-close ceremony (Session 13)
- **Session shape:** three-session Shape A per v3 §7 Steps 7-8 with operational flex.
- **Predecessors:** cycle closeout brief at `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`); v3 proposal at `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`); amended `ui_architecture.md` at `docs/03_architecture/ui_architecture.md` (commit `9e606f6`); chunk 1 brief (commit `1c65cd1`) + chunk 1 implementation (commit `5a9492b`); chunk 2 brief (commit `05b478b`) + chunk 2 implementation chain (commits `94b0411` + `c5d7e89`).
- **Baseline at Session 11 onset:** HEAD = `c5d7e89` on staging; `pnpm test` 1146/1146 (verified at Session 11 onset re-run); `pnpm agent:validate` 26/26; `pnpm typecheck` green.

## 1. Preamble

Phase 6.5 chunk 3 ships the terminal Phase 6.5 implementation surface per v3 §5.1 chunk-3 decomposition + cycle closeout brief §5.3 acceptance criteria. The cycle closeout brief at `79a6ceb` locked the substantive decisions across Sub-Q4 (DocumentIntakeRail two-step removal verification-then-delete) + Sub-Q9 (staged-attachments behavior; tray-above-input; unified Send; session-only persistence) + Sub-Q10 (three-moment composite drop acknowledgment; failure-path persist-until-dismissed; multi-file batch one-tab) + Sub-Q11 EC3.β (one-tab-per-batch via `drop_session_id`). This brief operationalizes those locks at implementation-task grain.

**Inheritance posture.** Cycle closeout brief is authoritative for *what* chunk 3 ships (Cut 1 Flow (a) substrate-unchanged + Sub-Q4 two-step removal + Sub-Q9.a.β / .b.α / .c.α / .d.α + Sub-Q10 three-moment composite + Pattern γ Rule 1 + EC3.β + Cut 9 multi-tab integration). This brief is authoritative for *how* chunk 3 builds it (component decomposition; drop handler shape; staged tray data shape; Send refactor; toast lifecycle; tab title derivation; DocumentIntakeRail-to-PendingDocumentsView state-machine port mechanics; two-commit ceremony shape). Cycle brief wins on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

Chunk 3 is the most **substrate-coupled** Phase 6.5 chunk: it touches the ingestion service surface inheritance (via existing `ingestionService.handleDragDropUpload` with no new service-layer wrapping), extends the `CanvasDirective` discriminated union (first new directive member in Phase 6.5 — RI-1 strict per consumer-with-substrate), and consumes both chunk-1 and chunk-2 substrates atomically (Region 7.2 Billing nav extension + Pattern γ Rule 1 routing via `canvasTabRouting.routeNewTab` without `focusExistingExactMatch`). It is also the most **substrate-light** chunk relative to the prior two: no DB schema changes, no migrations, no service-layer additions, no new Zod schemas — all volume goes into UI affordances.

### 1.1 Path C invocation evaluation

Per RI-7 (Phase 4 retrospective codification) + cycle closeout brief §6 Path C risk surface. Cycle pre-disposes Path-C-negative for chunk 3 based on bounded substrate touchpoints + small forecast (~500-700 LOC per v3 §5.1) + well-defined two-commit verification-then-delete shape that is itself NOT a Path C session-split (it is an implementation-grain commit boundary per Sub-Q4 lock, not a session boundary per RI-7).

**Volume forecast (brief-draft re-evaluation against disk):**

- `PendingDocumentsView.tsx` (new component): port of `DocumentIntakeRail`'s `idle_with_recent_cards` + `showing_batch` state-machine (311-line source per disk; mostly copy of state shape + mount-fetch + post-drop fetch + DocumentCard rendering; minus the drop affordance which moves to AgentChatPanel; minus the section-header "Recent" / "Last drop" rendered shape which may simplify in a tab context). Estimate: ~200-280 LOC.
- `AgentChatPanel.tsx` drop handler + staged tray + Send refactor + toast lifecycle + beforeunload (~880 lines pre-chunk-3 post-`c5d7e89`): touch grain ~150-220 LOC added.
- `SplitScreenLayout.tsx` `handleDropEvent` fourth per-source handler + AgentChatPanel `onDropEvent` Prop fire (~159 lines pre-chunk-3): touch grain ~30-50 LOC added.
- `canvasDirective.ts` `pending_documents` member: ~3-5 LOC.
- `tabTitle.ts` `pending_documents` case: ~3 LOC.
- `canvasContextSuffix.ts` `describeDirective` `pending_documents` case: ~3-5 LOC.
- `ContextualCanvas.tsx` `renderDirective` switch `pending_documents` case: ~3-5 LOC.
- `Zone1ConsolidatedPanel.tsx` Billing nav 9th item (`pending_documents`): ~5-8 LOC (constant addition + `navItemToDirective` case).
- Tests: ~400-600 LOC (10-15 unit + 5-8 integration + 3-5 E2E + DocumentIntakeRail test-coverage migration).
- File-top comment updates per CLAUDE.md staleness review: ~20-40 LOC across touched files.
- `DocumentIntakeRail.tsx` (311 lines) + any DocumentIntakeRail-specific test files: deletion (Commit 2).

**Total estimate: ~850-1300 LOC (Commit 1) + deletion (Commit 2).** Sits at HIGH end of cycle band (500-700) — drivers are Phase 6 chunk-6.2b's empirical observation that test-file LOC ran 53 LOC/test vs forecast 30-40, plus the three-moment acknowledgment composite (Sub-Q10) bringing more operational density than the cycle forecast anticipated (cycle forecast pre-dated the chunk-6.2b LOC-undercount finding). Brief flags this for Phase 6.5 retrospective drafting input: chunk-3 LOC at impl close may further calibrate RI-7's empirical bounds (chunk-6.2b's 97%-over-upper-bound was banked at N=3 observation-grain per chunks 6.1 + 6.2a + 6.2b).

**Framing-revisits at brief-draft.** Cycle closeout brief locked all chunk-3-relevant sub-Qs (Sub-Q4 + Sub-Q9 + Sub-Q10 + Sub-Q11 EC3.β). Verify-from-disk at Session 11 onset confirmed substrate touchpoints are stable post-chunk-2: SplitScreenLayout per-source callback structure ready for fourth handler addition; `canvasTabRouting.routeNewTab` Pattern γ Rule 1 + 2 pure function ready for consumer wiring; ContextualCanvas `renderDirective` switch exhaustive over `CanvasDirective` ready for new case; chunk-1 Region 7.2 Billing nav constant `BILLING_NAV_ITEMS` ready for 9th-item insertion at the workflow slot. No framing-revisits surface at brief-draft. Framing-revisit risk at Session 13 implementation grain is bounded by: (a) PendingDocumentsView port being well-scoped per Session A §A.10 reference; (b) drop handler reusing the existing `/api/orgs/[orgId]/documents/ingest/drag-drop` endpoint without route changes; (c) the three-moment composite being a layered UI surface composition with no deep cross-component coordination beyond toast lifecycle.

**Substantively-novel-logic scope.** Three substrate areas are genuinely greenfield at chunk 3:

1. **AgentChatPanel drop affordance** — first drag-drop affordance at the Bridge chat input grain (DocumentIntakeRail had drag-drop but lived on the canvas as a separate rail; chunk-6.2b precedent for native HTML5 drag-drop UI patterns inherits cleanly).
2. **Three-moment acknowledgment composite** (tray + toast + canvas-tab open) — new UI orchestration pattern combining three concurrent affordance grains with timing-overlap handling.
3. **Two-commit verification-then-delete ceremony** (Sub-Q4) — implementation-grain commit boundary discipline; chunk-3-precedent for verify-then-delete two-step removal.

All three are substantively-novel-logic per RI-7 criterion 3. None require Path C session-split at the brief-drafting grain — single-session implementation is feasible with the two-commit ceremony at implementation grain.

**Path C invocation disposition at brief grain: NEGATIVE. Single-session Session 13 implementation with two-commit ceremony.**

If Session 13 implementation surfaces budget friction at any task boundary OR framing-revisit (e.g., DocumentIntakeRail state machine port complexity beyond Session A §A.10's "mostly copy" implication; or three-moment composite timing-coordination overrun), reactive Path C session-split per Phase 4 chunk 3 precedent. Substrate-coherent fault line for reactive split: **Commit 1's pre-deletion verification gate** (Tasks 1-8 vs Task 9). Tasks 1-8 ship PendingDocumentsView port + all dependent surfaces + tests + screenshot gate; Task 9 fires Commit 2 deletion post-verification. The verification gate itself is the natural reactive Path C split surface.

**Session 12 disposition (operational flex per Session 6 chunk 1 + Session 9 chunk 2 precedents):** If Session 11 brief-drafting closes with all substrate decisions locked + no framing-revisits + Path C negative, Session 12 may collapse into Session 13 implementation directly. Brief drafter does not pre-commit Session 12 shape; Session 13 onset adjudicates.

### 1.2 Inheritance + carry-forwards from chunks 1 + 2

**Chunk 1 substrate carry (RESOLVED at chunk 1 close).**

- Zone 1 three-region rendering (Region 7.1 workspace tabs + 7.2 nav items + 7.3 footer + 7.4 hidden) intact at HEAD.
- Region 7.2 Billing nav `BILLING_NAV_ITEMS` constant at `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx` lines 72-81 (8 items: New Bill primary + Open Bills → AP Aging → Vendor Balance → Payment Approval Queue → Pending Approvals → Active Payments → Paid Bills History). Chunk 3 inserts `pending_documents` as 9th per memory pointer `project_phase_6_5_chunk_1_billing_nav_ordering.md` lean (between New Bill and Open Bills — "incoming intake → outstanding bills" workflow ordering).
- `useShellState` + `useWorkspace` hook patterns established at `apps/web/src/hooks/`; localStorage keys `chounting:shell:zone1Collapsed` / `chounting:shell:zone2Collapsed` / `chounting:shell:activeWorkspace`.
- Chunk-1 screenshot gate fired full 5/5 empirical pass; no deferred verification carry.

**Chunk 2 substrate carry (TWO carries — RESOLVED + DEFERRED).**

Resolved carry-forwards (verified at chunk 2 close):

- Tab data model state lift at SplitScreenLayout: `tabsState: { tabs, activeTabId }` + `Tab = { tabId, directive, selectedEntity?, history, historyIndex }`.
- `canvasTabRouting.ts` six pure functions: `routeNewTab` (Rules 1+2) + `routeReplaceActive` (Rule 3) + `routeStayInActive` (Rule 4) + `findExistingExactMatch` (EC2.β) + `closeTab` (Sub-Q11.d.close.α with tabs-zero handling) + `switchTab` (Sub-Q11.d.switch.α).
- `CanvasTabStrip.tsx` browser-tab-shape UI with overflow scroll + close-on-hover.
- `tabTitle.ts` exhaustive `tabTitleForDirective` mapping (38 cases at chunk 2 close; chunk 3 adds `pending_documents` → 39 cases).
- Per-source callback structure at SplitScreenLayout: `handleMainframeNavigate` + `handleAgentDirective` + `handleCanvasDrillDown` + `handleCloseTab` + `handleSwitchTab` + `handleSelectEntity` + `handleGoBack` + `handleGoForward`. Chunk 3 adds `handleDropEvent` as fourth Pattern γ per-source handler.
- `canvasContext` active-tab binding per Sub-Q18.α (singleton-shape preserved at TYPE level); zero touch to `canvasContext.ts` / `canvasContext.schema.ts` / `canvasContextSuffix.ts` type surface (chunk 3 only extends `describeDirective` switch with new case alongside the directive union extension).

Deferred-empirical-verification carry (UNRESOLVED at chunk 2 close):

Chunk 2 screenshot gate fired partial — 2 of 6 shots empirically verified (Shot 1 single-tab default state; Shot 6 close-to-1-tab state), 4 of 6 shots DEFERRED pending an adjacent-substrate bug fix unrelated to chunk 2 itself:

- Shot 2 (multi-tab 3 tabs)
- Shot 3 (multi-tab 5 tabs / overflow threshold)
- Shot 4 (multi-tab 6+ tabs / overflow shadow indicators)
- Shot 5 (EC1.β prompt fires on Zone 1 nav)

**Root cause of chunk 2 deferral.** Multi-tab states (Shots 2-4) were unreachable empirically due to a pre-existing agent session-context bug. The agent's `respondToUser` tool construction recurrently produces `canvas_directive` payloads where `orgId` fails Zod UUID validation, gracefully degrading to text-only "I cannot access orgId in this session" responses. Pattern γ Rule 2 routeNewTab path doesn't fire because canvas_directives aren't emitted. This is a **Phase-6.5-revealed pre-existing bug** in agent orgId session-context construction, NOT a chunk 2 regression. Chunk 2 code paths are correct (canvasTabRouting unit tests pass; SplitScreenLayout typecheck green; the bug surfaces only because Phase 6.5 makes more navigation flow through agent emissions).

**Chunk 3 incidental-verification opportunity.** Chunk 3's drop-event-driven canvas tab open exercises Pattern γ Rule 1 routeNewTab (via `SplitScreenLayout.handleDropEvent` → `canvasTabRouting.routeNewTab` without `focusExistingExactMatch`, per EC3.β one-tab-per-batch). This path is **INDEPENDENT of agent emissions** — drop event fires user-driven via AgentChatPanel's drag-drop affordance; staged tray populates from user file selection; Send fire triggers ingest via the existing `/api/orgs/[orgId]/documents/ingest/drag-drop` endpoint (orgId from URL path, NOT from agent session context); ingest completion fires `onDropEvent` → `routeNewTab` which opens canvas tab with `pending_documents` directive + PendingDocumentsView component.

Operational implication: **chunk 3 screenshot gate is verification-double-duty** — it verifies chunk 3 surfaces AND incidentally verifies chunk 2 surfaces by exercising the same routeNewTab path the deferred Shots 2-4 prescribed. If a user fires 3+ sequential drop batches, each opens its own canvas tab per EC3.β; the resulting multi-tab states empirically reach Shots 2-4's prescribed multi-tab UI scenes. Brief §6.1 explicitly enumerates the dual-purpose scope.

Shot 5 (EC1.β prompt on Zone 1 nav) is independent of both chunk 2's deferred-substrate bug AND chunk 3's drop flow; brief §8 includes it as part of chunk 3 screenshot gate scope for operational coherence (verifying chunk 2's deferred EC1.β surface in the same gate firing).

**Phase 6.5 retrospective drafting input (cumulative).**

- **Agent orgId session-context bug** as Phase-6.5-revealed pre-existing finding; post-Phase-6.5 remediation session candidate; NOT chunk 3 territory. If chunk 3 surfaces additional manifestations (e.g., drop event triggers agent invocation downstream and the bug recurs in that path), flag in chunk-3-close friction-journal entry; otherwise carry as cumulative phase-level finding.
- **Screenshot gate verification surface should be independent of upstream broken substrate**: chunk 2 gate prescribed multi-tab states reachable only via agent emissions; when agent failed, gate became unfireable. Codification candidate at Phase 6.5 retrospective: screenshot gates should prescribe surfaces reachable via the chunk's own substrate, not via cross-substrate dependencies that may fail independently.
- **Verification-asymmetry across chunks 1/2/3**: chunk 1 full empirical pass (5/5); chunk 2 partial (2/6); chunk 3 expected full pass (drop interactions don't depend on agent emissions). Retrospective evaluates whether asymmetry warrants per-chunk verification-shape checklist OR gate-surface-independence codification.
- **ADR-0010 substrate-now-enforcement-later UI-layer instances catalog** at Phase 6.5 retrospective drafting input. The chunk 2 brief §2 enumerated 4 UI-layer instances; chunk 2 commit body added EC1.β v1-default-prompt-mechanism as a fifth instance. Disk reality: instance #4 (chunk 2 brief's "onDropEvent Prop additive at chunk 2 with chunk 3 consumer") did NOT ship at chunk 2 (verify-from-disk on AgentChatPanel + SplitScreenLayout confirms no `onDropEvent` Prop present); chunk 3 ships **both** the Prop signature AND consumer atomically per SplitScreenLayout file-top comment lines 41-44. The "fourth UI-layer instance" reclassifies — chunk 3's onDropEvent add is structurally an **atomic substrate-with-consumer ship** (RI-1 strict pattern at Prop-API grain), not a substrate-now-enforcement-later cross-chunk deferral. Phase 6.5 retrospective adjudicates: (i) does the chunk 2 brief §2 "onDropEvent" instance stay in the ADR-0010 catalog, OR is it reclassified as RI-1 strict at Prop-API grain? (ii) what's the canonical N after chunk 3 close — N=4 if onDropEvent reclassifies, N=5 if it stays. This brief positions onDropEvent as RI-1 strict atomic at chunk 3 (the safer disposition pending retro adjudication).
- **Path C invocation grain catalog** for retrospective codification: brief-draft prospective at chunk 3 (negative; this brief) + Phase-A-close-prospective at chunk 2 (positive; chunk 2 ratification at Session 10 Arc β) + mid-impl-reactive at Phase 4 chunk 3 (positive; F-J-14 first instance). Three-grain N=1-1-1 catalog stays codification-pending at Phase 6.5 retrospective.

### 1.3 Cross-references

- Cycle closeout brief at `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`) — §5.3 chunk 3 acceptance criteria 21-item checklist + §7.3 chunk 3 partial-information carry-forwards (11 items) + §6 inter-chunk dependency map (chunk 3 is terminal; consumes chunks 1+2 outputs).
- v3 proposal at `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`) — §5.1 chunk 3 decomposition + §3 Cut 1 Flow (a) substrate exclusively at v1 + §3 Cut 9 Pattern γ Rule 1 (drop event → new tab).
- Amended `ui_architecture.md` (commit `9e606f6`) — § The Three-Path Entry Model § Path 2 Chat amendment notation (Phase 6.5 chunk 3 drag-drop + paste + "+" button affordances + staged-with-explicit-ingest + unified Send + PendingDocumentsView canvas tab open on ingest completion per Cut 9 Pattern γ).
- Chunk 1 brief (commit `1c65cd1`) + chunk 1 implementation (commit `5a9492b`) — Zone 1 Region 7.2 Billing `BILLING_NAV_ITEMS` substrate; useShellState + useWorkspace hook patterns; localStorage namespace.
- Chunk 2 brief (commit `05b478b`) + chunk 2 implementations (commits `94b0411` 10a substrate + `c5d7e89` 10b feature) — tab data model state lift; `canvasTabRouting.routeNewTab` Pattern γ Rule 1 + 2 pure function; per-source callback structure at SplitScreenLayout; `CanvasTabStrip.tsx` UI; `tabTitle.ts` mapping; `canvasContext` active-tab binding.
- Memory pointer `project_phase_6_5_chunk_1_billing_nav_ordering.md` — Billing nav 8-item canonical order; Pending Documents 9th-item insertion slot lean (between New Bill and Open Bills).
- Memory pointer `project_phase_6_5_full_page_routes_layout.md` — three-zone shell vs full-page route boundary; PendingDocumentsView renders in Zone 3 tab context (per Pattern γ Rule 1; not a full-page route).
- Session A preserved-evidence at `docs/09_briefs/phase-6/2026-05-16-session-a-preserved-evidence.md` (commit `7834a26`) — §A.6 AGENT panel substrate ownership inventory; §A.7 chat-message substrate shape (`agent_sessions.turns` JSONB; no `chat_messages` table); §A.10 DocumentIntakeRail state machine reference (`idle_with_recent_cards` + `showing_batch` + transitions).
- ADR-0010 substrate-now-enforcement-later (Region 7.4 hidden chunk 1; localStorage→DB chunk 1; session-only→IndexedDB chunk 3; EC1.β v1-default-prompt-mechanism chunk 2; chunk 3 onDropEvent reclassification at Phase 6.5 retro).
- ADR-0020 source-tree authority-layer enumeration (`shared/types/` + `components/canvas/` + `components/bridge/` subdirectories established).
- ADR-0022 §2 supersession workflow (DocumentIntakeRail two-step removal precedent — detach mount at chunk 1; delete file at chunk 3 Commit 2 post-verification).
- chunk-6.3a brief at `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-3a.md` — structural shape inheritance for sub-Q-resolution + verify-from-disk + reasoning chain pattern.
- Phase 4 chunk 3 brief at `docs/09_briefs/phase-4/chunks/2026-05-13-phase-4-chunk-3.md` + amended `c76d264` — Path C reactive split precedent (forward-pointer if Session 13 impl surfaces budget friction at Tasks 1-8 vs Task 9 boundary).
- Phase 4 retrospective RI-1 / RI-6 / RI-7 / RI-10: `docs/07_governance/retrospectives/phase-4-retrospective.md`.

## 2. Scope

**Two-commit chunk shape** per Sub-Q4 verification-then-delete + cycle closeout brief §5.3 Surface B MEDIUM reversibility + §7.3 carry-forward #11:

### Commit 1 — PendingDocumentsView port + dependent surfaces

What Commit 1 ships:

- **AgentChatPanel drop affordance.** Native HTML5 `onDragOver` / `onDrop` event handlers on the chat input region; paste handler (`onPaste`) on the input textarea; "+" button affordance inline (file picker via hidden `<input type="file" multiple>`). Three intake mechanisms per v3 §5.1; all funnel into the same staged-tray state.
- **Staged attachments tray** (Sub-Q9.b.α) — in-memory React state per Sub-Q9.d.α session-only; rendered above the chat input region (between chat history and input). Tray shape: array of File objects with derived display state (filename + size + type icon).
- **Unified Send refactor** (Sub-Q9.c.α) — Send button fires both ingest (if attachments present) + chat message (if input text present). Send disabled-rule extended from current `input.trim().length > 0` to `attachments.length > 0 || input.trim().length > 0`. Ingest-only-path: drop + Send with empty text → ingest fires; no chat turn appended. Send-with-attached-message-path: drop + type + Send → ingest fires AND chat turn appended (existing send flow).
- **beforeunload prompt** — defensive UX for staged-files-on-reload (Sub-Q9.d.α session-only persistence means file objects don't survive reload; beforeunload `window.beforeunload` handler warns user with native browser prompt when staged files exist and reload is attempted; auto-unregisters when staged tray is empty).
- **Three-moment acknowledgment composite** (Sub-Q10 lock):
  - **(I) Drop event → tray entry** (Sub-Q10.a.α at tray grain; no separate UI; tray IS drop acknowledgment per Sub-Q9.b.α).
  - **(II) Send fire → transient toast** (Sub-Q10.a.α at toast grain) "Ingesting N documents..." at bottom of chat panel; ~3 sec fade per Sub-Q10.b.α success-transient.
  - **(III) Ingest completion → new canvas tab opens** (Sub-Q10.a.δ leg) with `pending_documents` directive + PendingDocumentsView component (Pattern γ Rule 1 + EC3.β one-tab-per-batch via `canvasTabRouting.routeNewTab` without `focusExistingExactMatch`).
  - **Failure-path: toast persists until dismissed** (Sub-Q10.b.γ; NOT transient) with error styling + close button + user-click-outside dismissal.
  - **Toast/canvas-tab timing-overlap handling**: if canvas tab opens within toast fade window, toast either early-fades (250ms transition) OR suppresses (skip steady-state) per §5 carry-forward #4 operationalization.
- **Multi-document batch behavior** (Sub-Q10.c composite):
  - Per-file tray entries (Sub-Q10.c.α at tray grain).
  - Single batched toast (Sub-Q10.c.β at toast grain).
  - Single canvas tab per batch (Sub-Q10.c.β at tab grain) — EC3.β one-tab-per-batch via `drop_session_id` (mirrors DocumentIntakeRail line 132 pattern: `crypto.randomUUID()` client-generated per drop event; server trusts client value per chunk-6.2b Flag 1 lock).
- **New `PendingDocumentsView.tsx` component** at `apps/web/src/components/canvas/PendingDocumentsView.tsx` — ports DocumentIntakeRail's `idle_with_recent_cards` + `showing_batch` state-machine. The component receives `orgId` + optionally a batch's `ingest_batch_id` from the directive payload (Pattern γ Rule 1 emits the directive with the just-created batch id; PendingDocumentsView focuses on that batch on mount, falls through to recent-cards view if no batch id provided OR if the batch id is stale). Renders DocumentCard rows per `idle_with_recent_cards` shape. Drop affordance is REMOVED in the ported component (drop moved to AgentChatPanel); the section-header "Recent" / "Last drop" copy may simplify to single canonical heading in a tab context — Session 13 picks specific.
- **`CanvasDirective` extension**: new `pending_documents` member added to discriminated union at `apps/web/src/shared/types/canvasDirective.ts`. Member shape: `{ type: 'pending_documents'; orgId: string; ingestBatchId?: string }` (optional `ingestBatchId` so the directive can be emitted from drop completion OR from Zone 1 nav). RI-1 strict — consumer ships in same commit (Commit 1).
- **`tabTitle.ts` extension**: new `pending_documents` case mapping to `"Pending Documents"` (or shape per §5 carry-forward #10 operationalization — single specific label picked at brief grain below).
- **`canvasContextSuffix.ts` `describeDirective` extension**: new `pending_documents` case at `apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts` (exhaustive switch over `CanvasDirective` union; chunk 3 add alongside enum addition).
- **`ContextualCanvas.tsx` `renderDirective` switch extension**: new `pending_documents` case routes directive to `PendingDocumentsView` component (chunk 3 add alongside enum addition).
- **Zone 1 Billing Region 7.2 "Pending Documents" nav item with count badge** — `BILLING_NAV_ITEMS` extension at `Zone1ConsolidatedPanel.tsx` lines 72-81 (insert as 2nd item between New Bill primary action button and Open Bills per memory pointer lean); `navItemToDirective` switch case added. Count badge wiring: nav item displays count of `document_cases` rows in states `received` + `extracting` + `awaiting_review` (states reachable via Phase 6 ingest, pre-classification + pre-routing) for the active org. Badge data source: GET `/api/orgs/[orgId]/documents/cases?state=received,extracting,awaiting_review&count_only=true` OR a polling/realtime mechanism; brief is non-prescriptive on transport mechanism — Session 13 picks (likely polling on mount + on Pattern γ Rule 1 tab open events; full real-time deferred to post-v1).
- **`SplitScreenLayout.handleDropEvent`** — fourth per-source Pattern γ handler. Calls `setTabsState((prev) => routeNewTab(prev, directive))` (WITHOUT `focusExistingExactMatch` per EC3.β one-tab-per-batch; every drop opens a fresh tab). Passed to AgentChatPanel as `onDropEvent` Prop.
- **`AgentChatPanel.onDropEvent` Prop signature** — atomic ship with consumer wire. Prop signature: `onDropEvent?: (directive: CanvasDirective) => void`. AgentChatPanel invokes the Prop on ingest completion with `{ type: 'pending_documents', orgId, ingestBatchId: result.ingest_batch_id }`. **Disk-verify catch addressed**: chunk 2 brief §2 implied the Prop signature shipped at chunk 2; verify-from-disk at Session 11 onset confirmed neither Prop signature NOR consumer existed at HEAD `c5d7e89` (SplitScreenLayout file-top comment lines 41-44 + AgentChatPanel grep both confirm). Chunk 3 ships atomically per RI-1 strict at Prop-API grain (this is the correct disposition — optional Props without consumers compile cleanly but contribute no value to the codebase; landing both at the same commit avoids the cross-chunk substrate-drift surface).
- **Substrate-shape preservation**: Cut 1 Flow (a) ingestion substrate **unchanged** at chunk 3:
  - POST to `/api/orgs/[orgId]/documents/ingest/drag-drop` — endpoint unchanged.
  - `ingestionService.handleDragDropUpload` — service unchanged; chunk 3 reuses Pattern B external-wrap via `withInvariants(ingestionService.handleDragDropUpload)(input, ctx)` per the existing route handler.
  - DB writes (`ingest_batches` + `source_documents` + `document_cases` + `document_jobs` + 1 `audit_log` row at batch grain) — substrate identical to chunk-6.2b path.
  - No service-layer changes; no DB schema changes; no migrations; no new Zod schemas; no new ServiceErrorCodes.

What Commit 1 does NOT ship:

- New service-layer wrapping (chunk 3 consumes existing `withInvariants`-wrapped `ingestionService.handleDragDropUpload` via the existing route handler at `apps/web/src/app/api/orgs/[orgId]/documents/ingest/drag-drop/route.ts`).
- New DB schema (Cut 1 Flow (a) substrate identical to chunk-6.3a path; no migrations).
- chat_messages table writes (per Session A §A.7; no such table exists on disk).
- Chat-message-with-attachments substrate (Cut 1 Flow (b) deferred past v1).
- localStorage persistence for staged tray (Sub-Q9.d.α session-only; File objects not serializable; IndexedDB substrate-now-enforcement-later candidate at post-v1).
- Multi-batch tab behavior (per Sub-Q10.c.β: single tab per batch via `drop_session_id`; multiple sequential drops produce multiple separate tabs, each opened via routeNewTab without focusExistingExactMatch).
- Drag-drop on canvas surfaces (per Cut 1 Flow (a) exclusively at v1; chat-input drop is the v1 ingestion entry-point).
- Mobile / touch-device drag-drop (defer to post-v1 mobile responsiveness).
- Real-time count-badge update mechanism (v1 ships polling-on-mount + on-tab-open; full real-time post-v1).
- Agent orgId session-context bug fix (Phase-6.5-revealed pre-existing finding; post-Phase-6.5 remediation session candidate; NOT chunk 3 scope).

### Commit 2 — DocumentIntakeRail.tsx deletion

What Commit 2 ships:

- Delete `apps/web/src/components/canvas/DocumentIntakeRail.tsx` (311 lines per disk).
- Delete any DocumentIntakeRail-specific test files. Session 13 onset enumerates exhaustively via `grep -rln DocumentIntakeRail apps/web/`; expected ~1-3 files (test fixtures or unit test files); brief is non-prescriptive on exact filename list — Session 13 picks at impl-onset post-grep.
- If any code path besides itself imports DocumentIntakeRail at impl-onset (expected zero per Sub-Q4 verification-then-delete gate), refactor or delete in same commit.

**Pre-Commit-2 verification gate** (verification-then-delete discipline per Sub-Q4 lock):

- All Commit 1 acceptance criteria green per Task 8 cross-task verification (1146+ vitest baseline preserved + new unit + integration + E2E tests passing).
- PendingDocumentsView renders cleanly in dev environment (founder-grade spot-check at screenshot gate Shot 5 or equivalent).
- DocumentIntakeRail no longer mounted anywhere (chunk 1 detached the mount at SplitScreenLayout; chunk 3 Commit 1 doesn't re-mount).
- DocumentIntakeRail-related imports are zero across `apps/web/` source tree (grep returns zero results EXCEPT DocumentIntakeRail.tsx itself + any DocumentIntakeRail-specific test file slated for deletion).
- Existing 12 E2E specs preserved.

Gate firing protocol: Session 13 orchestrator (WSL-side) runs the gate checklist post-Commit-1; founder ratifies; Commit 2 fires only after ratification.

## 3. Substrate touchpoints

Verify-from-disk at Session 13 implementation onset before consuming any line/path reference below. All counts/paths confirmed at Session 11 brief-drafting against HEAD `c5d7e89`.

### Files modified

- **`apps/web/src/components/bridge/AgentChatPanel.tsx`** (~880 lines pre-chunk-3). Substantive extension: drop affordance (onDragOver + onDrop + onPaste + "+" button file picker); staged attachments tray state + render; Send refactor (unified ingest + chat message); toast lifecycle component (transient on success; persist-until-dismissed on failure); beforeunload registration; `onDropEvent` Prop fire on ingest completion. Touch grain: ~150-220 LOC added. Preserve: `collapsed` + `onExpand` + `onCollapse` + `onAssistantTurnAdded` Props (chunk 1 substrate); two-mode wrapper (OnboardingChat / ProductionChat); existing send / persona / canvasContext wiring. File-top comment update per CLAUDE.md staleness review (current comment lines 1-13 don't mention drop affordance; chunk 3 adds).
- **`apps/web/src/components/bridge/SplitScreenLayout.tsx`** (~358 lines post-chunk-2). Add `handleDropEvent` fourth per-source Pattern γ handler (Rule 1 routeNewTab without focusExistingExactMatch per EC3.β). Pass `handleDropEvent` to AgentChatPanel as `onDropEvent` Prop. Touch grain: ~30-50 LOC. Preserve: tabsState + activeTab derivation + all existing per-source handlers + keyboard shortcuts + memberships query + canvasContext derivation. File-top comment update: lines 41-44 (chunk 3 forward-pointer) become as-shipped notation per chunk 3 implementation; chunk 3 commit body documents the transition.
- **`apps/web/src/components/bridge/ContextualCanvas.tsx`** (~239 lines). Add `pending_documents` case to `renderDirective` switch (line 130+). Touch grain: ~3-5 LOC. Preserve: all existing cases + back/forward header + Props-driven render shape.
- **`apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx`** (~310 lines post-chunk-1). Add `pending_documents` to `BILLING_NAV_ITEMS` constant (insert as 2nd item per memory pointer lean); add `pending_documents` case to `navItemToDirective` function. Count badge wiring: new state (or hook) for count value + GET fetch from `/api/orgs/[orgId]/documents/cases?...&count_only=true`; badge rendered alongside nav item label. Touch grain: ~30-50 LOC. Preserve: Region 7.1 + 7.3 + 7.4 + collapsed mode rendering.
- **`apps/web/src/shared/types/canvasDirective.ts`** (38 active members; 64 lines). Add `| { type: 'pending_documents'; orgId: string; ingestBatchId?: string }` member to discriminated union. Touch grain: ~3 LOC.
- **`apps/web/src/shared/types/tabTitle.ts`** (38 cases). Add `case 'pending_documents': return 'Pending Documents';` (specific label per §5 carry-forward #10). Touch grain: ~3 LOC.
- **`apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts`** (~120 lines; `describeDirective` exhaustive switch over CanvasDirective). Add `case 'pending_documents': return 'the pending documents queue';` (or shape per agent prompt narrative convention — narrative phrasing per agent_interface.md voice + Session 13 picks specific). Touch grain: ~3-5 LOC.
- **Existing test files touched by Commit 2 deletion** (Session 13 enumerates via grep at impl-onset; brief estimate: 1-3 files).

### Files created

- **`apps/web/src/components/canvas/PendingDocumentsView.tsx`** (~200-280 LOC). New canvas-tab view component; ports DocumentIntakeRail's `idle_with_recent_cards` + `showing_batch` state machine minus the drop affordance. Component signature:

  ```typescript
  interface Props {
    orgId: string;
    ingestBatchId?: string;
  }
  export function PendingDocumentsView({ orgId, ingestBatchId }: Props) { ... }
  ```

  State machine:
  - `idle` — initial pre-mount-fetch state (transient).
  - `fetching_recent` — mount-time GET in flight (when no `ingestBatchId` Prop OR fallback).
  - `idle_with_recent_cards` — recent-cards-rendered state (when no `ingestBatchId` Prop OR after fallback).
  - `fetching_batch` — mount-time GET in flight for specific batch (when `ingestBatchId` Prop provided).
  - `showing_batch` — batch-cards-rendered state.
  - `error` — fetch failure.

  Mount behavior: if `ingestBatchId` Prop provided, fetch batch-specific cards via existing endpoint `/api/orgs/[orgId]/documents/cases?ingest_batch_id={id}`; on success → `showing_batch`; on failure → fallback to `fetching_recent` → `idle_with_recent_cards` (defensive: empirical batch may not yet be queryable due to RPC commit timing).

  If no `ingestBatchId` Prop, fetch recent N cards via `/api/orgs/{orgId}/documents/cases?limit=50` (Sub-Q10 lock per DocumentIntakeRail line 83); on success → `idle_with_recent_cards`; on failure → soft-fail (stay in `idle`; render empty state).

  Renders `DocumentCard` rows per existing component contract (preserved from DocumentIntakeRail import). No drop affordance.

- **Test files (~10-15 unit + 5-8 integration + 3-5 E2E):**
  - `apps/web/src/components/bridge/__tests__/AgentChatPanel.drop.test.tsx` — drop handler unit tests + staged tray mutations + Send disabled-rule + toast lifecycle (subject to vitest DOM-environment availability per chunk-6.2b inheritance; if gap persists, SKIP regime per chunk 1 A1-B disposition).
  - `apps/web/src/components/canvas/__tests__/PendingDocumentsView.test.tsx` — state machine transitions + Props-driven shape (same vitest-DOM-environment dependency).
  - `apps/web/tests/unit/handleDropEvent.test.ts` OR co-located within an existing canvasTabRouting test — Pattern γ Rule 1 routeNewTab integration with handleDropEvent (node-env compatible; pure-function path).
  - `apps/web/tests/integration/chatInputIngestionFlow.test.ts` — chat-input drop → ingestionService.handleDragDropUpload → ingest_batches + source_documents + document_cases + document_jobs DB writes asserted (Two Laws inheritance verification).
  - `apps/web/tests/integration/chatInputIngestionFailurePath.test.ts` — failure-path: ingestionService throws → toast persists; trace_id propagation under failure.
  - `apps/web/tests/integration/chatInputMultiFileBatch.test.ts` — multi-file batch: single `drop_session_id` → single `ingest_batch_id` → multiple `document_cases` rows.
  - `apps/web/tests/e2e/phase-6-5-chat-drop.spec.ts` — golden-path E2E: drop file → tray entry → Send → toast appears → tab opens → PendingDocumentsView renders → document card visible.
  - `apps/web/tests/e2e/phase-6-5-chat-drop-failure.spec.ts` — failure-path E2E: drop file → Send → ingest fails → toast persists → user dismisses.
  - `apps/web/tests/e2e/phase-6-5-chat-drop-multi-batch.spec.ts` — multi-batch E2E: drop batch 1 → tab 1 opens → drop batch 2 → tab 2 opens (chunk 2 deferred Shots 2-4 incidental verification; see §6.1).

### Files deleted (Commit 2)

- `apps/web/src/components/canvas/DocumentIntakeRail.tsx` (311 lines per disk).
- DocumentIntakeRail-specific test files identified at Session 13 impl-onset (estimate 1-3 files; specific paths per grep).

### Files unchanged (chunk 3 zero touch)

- **Service layer** — no service-layer changes; chunk 3 inherits existing `ingestionService.handleDragDropUpload` (Phase 6 chunk 6.2b ship; Pattern B external-wrap with `withInvariants` at route handler boundary).
- **DB schema / migrations** — none.
- **`canvasContext.ts` / `canvasContext.schema.ts`** — Sub-Q18.α singleton-shape preserved at TYPE level (chunk 2 ratified); chunk 3 zero touch.
- **`apps/web/src/app/api/orgs/[orgId]/documents/ingest/drag-drop/route.ts`** — chunk 6.2b endpoint reused without changes.
- **`canvasTabRouting.ts`** — chunk 2 pure functions reused without changes; chunk 3 consumes `routeNewTab` from `SplitScreenLayout.handleDropEvent`.
- **`CanvasTabStrip.tsx`** — chunk 2 tab strip preserved without changes.
- **Other Zone 1 surfaces** (Region 7.1 + 7.3 + 7.4) — unchanged at chunk 3.

## 4. Tasks

Strict-sequence per task dependencies (see §9 for parallelization shape):

```
Task 1 (PendingDocumentsView port)
  ↓
Task 2 (canvasDirective + ContextualCanvas + tabTitle + describeDirective extensions)
  ↓
  ├─ Task 3 (Zone 1 Billing nav item) ──┐
  ├─ Task 4 (AgentChatPanel drop + tray + beforeunload) ──┤
  └─ Task 7 (SplitScreenLayout handleDropEvent) ──┘
            ↓
       Task 5 (Unified Send refactor)
            ↓
       Task 6 (Three-moment acknowledgment composite)
            ↓
       Task 8 (Cross-task verification + tests + screenshot gate)
            ↓
       Task 9 (Commit 2 DocumentIntakeRail deletion)
```

### Task 1 — PendingDocumentsView component port

Port DocumentIntakeRail's `idle_with_recent_cards` + `showing_batch` state machine to a new canvas-tab view component. Drop affordance is NOT ported (moves to AgentChatPanel at Task 4).

**Files:**
- Create: `apps/web/src/components/canvas/PendingDocumentsView.tsx`
- Create: `apps/web/src/components/canvas/__tests__/PendingDocumentsView.test.tsx`

**Component contract** per §3 Files created.

**Mount behavior:**

```typescript
useEffect(() => {
  if (ingestBatchId) {
    fetchBatch(ingestBatchId); // → showing_batch on success; fallback to fetchRecent on failure
  } else {
    fetchRecent(); // → idle_with_recent_cards on success; soft-fail on error
  }
}, [orgId, ingestBatchId]);
```

**Acceptance criteria:**

- State machine transitions match the 6-state contract (idle → fetching_recent → idle_with_recent_cards → fetching_batch → showing_batch → error).
- `ingestBatchId` Prop fetches batch-specific cards on mount; falls through to recent-cards on batch-fetch failure.
- No `ingestBatchId` Prop fetches recent N cards on mount.
- Renders DocumentCard rows per existing component contract.
- No drop affordance (drop moved to AgentChatPanel).
- Section header: single canonical heading (e.g., "Pending Documents" or "Recent Documents" — Session 13 picks; brief non-prescriptive).
- Unit tests (subject to vitest DOM-environment per chunk 1 A1-B inheritance): batch-id-Prop branch + no-batch-id-Prop branch + soft-fail + error-state.

**Dependency:** none; Task 1 fires first.

**Parallelizable:** small component; not worth parallelizing within Task 1.

### Task 2 — canvasDirective + ContextualCanvas + tabTitle + describeDirective extensions

Atomic discriminated-union extension per RI-1 strict — directive enum + consumer all in same task. Test compile passes only when all four files updated together.

**Files:**
- Modify: `apps/web/src/shared/types/canvasDirective.ts`
- Modify: `apps/web/src/components/bridge/ContextualCanvas.tsx`
- Modify: `apps/web/src/shared/types/tabTitle.ts`
- Modify: `apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts`

**Changes:**

```typescript
// canvasDirective.ts — add member:
  | { type: 'pending_documents'; orgId: string; ingestBatchId?: string }

// ContextualCanvas.tsx renderDirective switch — add case:
  case 'pending_documents':
    return <PendingDocumentsView orgId={d.orgId} ingestBatchId={d.ingestBatchId} />;

// tabTitle.ts — add case:
  case 'pending_documents':
    return 'Pending Documents';

// canvasContextSuffix.ts describeDirective — add case (narrative phrasing):
  case 'pending_documents':
    return 'the pending documents queue';
```

**Acceptance criteria:**

- TypeScript compile passes with new member added (exhaustive-switch checks at compile time enforce all four cases).
- `pending_documents` directive renders PendingDocumentsView in ContextualCanvas.
- `tabTitleForDirective` returns 'Pending Documents' for the new case.
- `describeDirective` returns canonical narrative phrasing for the new case.
- No regression on existing 38 directive members.

**Dependency:** Task 1 (PendingDocumentsView exists for ContextualCanvas import).

**Parallelizable:** four-file atomic; single-subagent.

### Task 3 — Zone 1 Billing nav item with count badge

Add 9th item to `BILLING_NAV_ITEMS` constant per memory pointer lean (between New Bill primary action button and Open Bills). Wire count badge data source.

**Files:**
- Modify: `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx`

**Changes:**

```typescript
const BILLING_NAV_ITEMS: ReadonlyArray<NavItem> = [
  { id: 'bill_form', label: 'New Bill', icon: '\u{1F4C4}', primaryAction: true },
  { id: 'pending_documents', label: 'Pending Documents', icon: '\u{1F4E5}' }, // NEW
  { id: 'open_bills', label: 'Open Bills', icon: '\u{1F4DD}' },
  // ... rest unchanged ...
];

// navItemToDirective switch — add case:
case 'pending_documents':
  return { type: 'pending_documents', orgId };
```

**Count badge:**

- Data source: GET `/api/orgs/[orgId]/documents/cases?count_only=true&state=received,extracting,awaiting_review` (or similar; endpoint extension may be required IF current cases endpoint doesn't support `count_only`; Session 13 verifies at impl-onset and adds minimal endpoint extension if needed — flagged as scope-creep risk to surface back to brief if endpoint extension grows beyond ~20-30 LOC).
- Polling: on Zone 1 expanded mount + on Pattern γ Rule 1 drop-event tab open (event-driven refresh). Full real-time deferred to post-v1.
- Render: small numeric badge next to nav-item label (collapsed-mode: badge replaces icon? OR badge below icon? Session 13 picks specific visual).
- Count format: single integer when ≥1; hidden when 0.

**Acceptance criteria:**

- 9th item renders in BILLING_NAV_ITEMS between New Bill and Open Bills.
- Click fires `onNavigate({ type: 'pending_documents', orgId })`.
- Count badge displays current count of incomplete-state document_cases.
- Count refreshes on tab open via drop event.
- Reports workspace (3 items) unchanged.
- Region 7.3 footer (4 items) unchanged.

**Dependency:** Task 2 (canvasDirective member exists).

**Parallelizable:** independent of Task 4 + Task 7; can fire in parallel post-Task-2.

### Task 4 — AgentChatPanel drop handler + staged tray + beforeunload

Substantive extension to AgentChatPanel. Three intake mechanisms: drag-drop, paste, "+" button (file picker). All funnel into the staged tray.

**Files:**
- Modify: `apps/web/src/components/bridge/AgentChatPanel.tsx`
- Create: `apps/web/src/components/bridge/__tests__/AgentChatPanel.drop.test.tsx`

**State extension:**

```typescript
const [attachments, setAttachments] = useState<File[]>([]);
const [toastState, setToastState] = useState<ToastState>({ kind: 'idle' });

type ToastState =
  | { kind: 'idle' }
  | { kind: 'ingesting'; fileCount: number; appearedAt: number }
  | { kind: 'success'; appearedAt: number }
  | { kind: 'error'; code: string; message: string };
```

**Drop handler:**

```typescript
const handleDrop = useCallback((e: React.DragEvent<HTMLDivElement>) => {
  e.preventDefault();
  e.stopPropagation();
  setDragOver(false);
  const fileList = Array.from(e.dataTransfer.files);
  if (fileList.length === 0) return;
  setAttachments((prev) => [...prev, ...fileList]);
}, []);
```

**Paste handler:**

```typescript
const handlePaste = useCallback((e: React.ClipboardEvent) => {
  const fileList = Array.from(e.clipboardData?.files ?? []);
  if (fileList.length === 0) return;
  e.preventDefault();
  setAttachments((prev) => [...prev, ...fileList]);
}, []);
```

**"+" button affordance:**

```typescript
const fileInputRef = useRef<HTMLInputElement>(null);
const handleAddClick = () => fileInputRef.current?.click();
// <input ref={fileInputRef} type="file" multiple hidden onChange={...} />
```

**Staged tray render:** above chat input (between chat history and input region). Each entry: file icon + filename (truncated with ellipsis) + size badge + remove button (×). Per-file remove: `setAttachments((prev) => prev.filter((_, idx) => idx !== removedIdx))`.

**Tray operationalizations (§5 carry-forwards #7 + #8 + #9):**

- Max-height: 144px (≈4 file entries visible at ~36px each); `overflow-y: auto` beyond.
- Per-file entry height: 36px.
- Visual treatment: filename + size badge + remove button on hover; file-type icon leading (PDF / image / generic).
- Ordering: most-recently-added at bottom (preserve drop-order; matches natural reading order top-to-bottom).

**beforeunload prompt:**

```typescript
useEffect(() => {
  if (attachments.length === 0) return;
  const handler = (e: BeforeUnloadEvent) => {
    e.preventDefault();
    e.returnValue = ''; // Modern browsers display default prompt
  };
  window.addEventListener('beforeunload', handler);
  return () => window.removeEventListener('beforeunload', handler);
}, [attachments.length]);
```

**drag-drop-while-typing** (§5 carry-forward #6): drop fires regardless of typing state; text input cursor position preserved; drop doesn't focus chat input or interfere with typing.

**Acceptance criteria:**

- Drag-drop event populates staged tray with dropped files.
- Paste event with files in clipboard populates staged tray.
- "+" button opens file picker; selected files populate staged tray.
- Staged tray renders above chat input; max-height with scroll at N≥5 files.
- Per-file remove (×) deletes specific file from tray.
- beforeunload prompt fires when tray non-empty + user attempts reload; doesn't fire when tray empty.
- Drag-drop-while-typing preserves cursor position.
- No regression on existing AgentChatPanel surfaces (Onboarding mode, collapsed-rail, send path).

**Dependency:** Task 2 (canvasDirective extension provides directive type for downstream Task 7 wire).

**Parallelizable:** independent of Tasks 3 + 7; can fire in parallel.

### Task 5 — Unified Send refactor

Extend Send button disabled-rule + send path to handle both ingest + chat message.

**Files:**
- Modify: `apps/web/src/components/bridge/AgentChatPanel.tsx`

**Disabled-rule extension:**

```typescript
const sendDisabled =
  submitting ||
  (attachments.length === 0 && input.trim().length === 0);
```

**Send disabled visual state (§5 carry-forward #2):** button disabled-color (`bg-neutral-300 text-neutral-500 cursor-not-allowed`); tooltip on hover: "Add a message or attach a file to send".

**Send path (unified):**

```typescript
async function handleSend() {
  if (sendDisabled) return;
  setSubmitting(true);

  // Capture attachments + text before clearing UI state.
  const filesToIngest = attachments;
  const textToSend = input.trim();

  // Clear tray + input immediately (optimistic; toast reflects status).
  setAttachments([]);
  setInput('');

  // Branch on intake-mechanism:
  if (filesToIngest.length > 0) {
    // Ingest-fire path — fires for both ingest-only and send-with-attached-message.
    fireIngest(filesToIngest); // → toast transitions
  }
  if (textToSend.length > 0) {
    // Chat-message path — fires for both empty-attach and send-with-attached-message.
    await fireSend(textToSend); // existing chat send flow
  }

  setSubmitting(false);
}
```

**Ingest fire** (calls into Task 6 toast lifecycle):

```typescript
async function fireIngest(files: File[]) {
  const drop_session_id = crypto.randomUUID();
  const formData = new FormData();
  formData.append('drop_session_id', drop_session_id);
  for (const file of files) formData.append('files', file);

  setToastState({ kind: 'ingesting', fileCount: files.length, appearedAt: Date.now() });

  try {
    const res = await fetch(`/api/orgs/${orgId}/documents/ingest/drag-drop`, {
      method: 'POST',
      body: formData,
    });
    if (!res.ok) {
      const errBody = await res.json().catch(() => ({}));
      setToastState({
        kind: 'error',
        code: errBody.error ?? 'UNKNOWN_ERROR',
        message: errBody.message ?? `Ingest failed (HTTP ${res.status})`,
      });
      return;
    }
    const result = await res.json() as { ingest_batch_id: string; document_count: number };
    setToastState({ kind: 'success', appearedAt: Date.now() });
    onDropEvent?.({
      type: 'pending_documents',
      orgId: orgId!,
      ingestBatchId: result.ingest_batch_id,
    });
  } catch (err) {
    setToastState({
      kind: 'error',
      code: 'NETWORK_ERROR',
      message: err instanceof Error ? err.message : String(err),
    });
  }
}
```

**Acceptance criteria:**

- Send disabled when input empty + attachments empty.
- Send enabled when attachments present + input empty (ingest-only-path).
- Send enabled when input present + attachments empty (chat-only-path; existing behavior preserved).
- Send enabled when both present (send-with-attached-message-path).
- Ingest-only-path: Send fires ingest only; no chat turn appended.
- Send-with-attached-message-path: Send fires ingest AND chat turn appended.
- Tooltip on disabled Send: "Add a message or attach a file to send".

**Dependency:** Task 4 (staged tray state); Task 7 (onDropEvent Prop wiring).

**Parallelizable:** integrates with Task 4 + Task 6; not separately fired in practice.

### Task 6 — Three-moment acknowledgment composite (toast lifecycle)

Implement toast lifecycle: transient on success, persist-until-dismissed on failure, timing-overlap handling on canvas-tab open.

**Files:**
- Modify: `apps/web/src/components/bridge/AgentChatPanel.tsx` (or extract as `AgentChatPanel.IngestToast.tsx` subcomponent if Task 4's file LOC grows large — Session 13 adjudicates).

**Toast render:** positioned at bottom of chat panel (above chat input region; below chat history); enters via slide-up + fade-in (200ms); exits via fade-out (250ms).

**Lifecycle:**

- `kind: 'idle'` → no render.
- `kind: 'ingesting'` → "Ingesting {fileCount} documents..." + subtle spinner; renders until status transitions.
- `kind: 'success'` → "✓ Ingested" (or similar success copy); fades after ~3000ms (Sub-Q10.b.α transient).
- `kind: 'error'` → "✗ {code}: {message}" with error styling (red background + close button); persists until dismissed.

**Failure-path persistence + dismissal (§5 carry-forward #5):**

```typescript
function dismissToast() {
  if (toastState.kind === 'error') {
    setToastState({ kind: 'idle' });
  }
}
// Triggers: close button click; click-outside toast region; new ingest fire dismisses prior error toast.
```

**Toast/canvas-tab timing-overlap (§5 carry-forward #4):**

```typescript
// When onDropEvent fires (canvas tab opens), if toast is in steady-state success window, early-fade.
useEffect(() => {
  if (toastState.kind === 'success') {
    const elapsed = Date.now() - toastState.appearedAt;
    if (elapsed > 1500) {
      // Past mid-window; early-fade immediately.
      const t = setTimeout(() => setToastState({ kind: 'idle' }), 250);
      return () => clearTimeout(t);
    } else {
      // Pre-mid-window; standard fade at 3000ms.
      const t = setTimeout(() => setToastState({ kind: 'idle' }), 3000 - elapsed);
      return () => clearTimeout(t);
    }
  }
}, [toastState]);
```

Concrete operationalization: success-toast fade duration adapts based on whether canvas tab has opened. If onDropEvent already fired (tab open), toast early-fades at 250ms; if onDropEvent hasn't fired yet (slow ingest), toast holds at steady-state up to ~3000ms then fades. Brief is non-prescriptive on exact threshold; Session 13 implementation may simplify (e.g., always 3000ms standard fade) if behavioral testing surfaces no observable benefit from early-fade.

**Acceptance criteria:**

- Toast transitions idle → ingesting → success/error per send flow.
- Success toast auto-fades (~3000ms).
- Error toast persists until user dismisses.
- Error toast dismissal triggers: close button + click-outside + new ingest fire.
- Toast renders above chat input; no overlap with chat history scrollbar.
- Toast/canvas-tab timing-overlap handled (early-fade OR standard-fade per Session 13 operationalization).

**Dependency:** Task 4 (toastState state shape); Task 5 (fireIngest sets state); Task 7 (onDropEvent fires on tab open).

**Parallelizable:** integrates with Task 4 + Task 5; not separately fired in practice.

### Task 7 — SplitScreenLayout handleDropEvent + onDropEvent consumer wire

Add fourth per-source Pattern γ handler at SplitScreenLayout. Wire to AgentChatPanel via new `onDropEvent` Prop.

**Files:**
- Modify: `apps/web/src/components/bridge/SplitScreenLayout.tsx`
- Modify: `apps/web/src/components/bridge/AgentChatPanel.tsx` (Prop signature addition; Task 4 + Task 5 use it)

**Changes at SplitScreenLayout:**

```typescript
const handleDropEvent = useCallback((directive: CanvasDirective) => {
  // Pattern γ Rule 1 + EC3.β: new tab focused per batch; no focus-
  // existing (every drop batch is unique via drop_session_id).
  setTabsState((prev) => routeNewTab(prev, directive));
}, []);

// Pass to AgentChatPanel:
<AgentChatPanel
  // ... existing Props ...
  onDropEvent={handleDropEvent}
/>
```

**Changes at AgentChatPanel:**

```typescript
interface Props {
  // ... existing Props ...
  /**
   * Phase 6.5 chunk 3: invoked on ingest completion with the
   * pending_documents directive carrying the just-created
   * ingest_batch_id. SplitScreenLayout routes the directive
   * via Pattern γ Rule 1 + EC3.β (one-tab-per-batch).
   */
  onDropEvent?: (directive: CanvasDirective) => void;
}
```

**File-top comment updates** per CLAUDE.md staleness review:

- SplitScreenLayout lines 41-44 (chunk 3 forward-pointer) update to as-shipped notation.
- SplitScreenLayout lines 175-179 (forward-pointer comment inside handler section) remove; replace with as-shipped handleDropEvent doc.
- AgentChatPanel lines 1-13 update to mention drop affordance + staged tray + onDropEvent Prop.

**Acceptance criteria:**

- `handleDropEvent` consumes `routeNewTab` from canvasTabRouting without `focusExistingExactMatch` per EC3.β.
- Each drop event opens exactly one new tab (regardless of file count in the batch — EC3.β one-tab-per-batch).
- New tab focused per Sub-Q11.d.open.α.
- `AgentChatPanel.onDropEvent` Prop signature shipped.
- AgentChatPanel ingest-completion path invokes `onDropEvent` with `{ type: 'pending_documents', orgId, ingestBatchId }`.
- File-top comments updated.
- Existing per-source handlers unchanged.

**Dependency:** Task 2 (canvasDirective member exists); Task 4 (AgentChatPanel structure ready for Prop addition); Task 5 (fireIngest invokes Prop).

**Parallelizable:** independent of Task 3; can fire in parallel post-Task-2.

### Task 8 — Cross-task verification + tests + screenshot gate

Final integration check + validation gates + screenshot gate firing + Commit 1 ceremony.

**Files:**
- Create: test files per §3 Files created.
- Update file-top comments on all chunk-3-touched files (CLAUDE.md staleness review).

**Pre-commit validation gates:**

- `pnpm typecheck` — expected green.
- `pnpm test` — expected 1146 baseline + new unit + integration tests = ~1156-1175 (revised count at ceremony grain). Subject to vitest DOM-environment availability per chunk 1 A1-B inheritance; if gap persists, UI unit tests under SKIP regime (acknowledge at chunk-3-close friction-journal entry; Phase 6.5 retro candidate alongside chunk-6.2b + chunk 1 + chunk 2 inheritance).
- `pnpm agent:validate` — expected 26/26 preserved.
- `pnpm test:no-hardcoded-urls` — expected green.
- `pnpm test:e2e` (full) — expected 12 existing specs + 1 chunk 1 spec + 1 chunk 2 spec + 3-5 new chunk 3 specs all passing.

**RI-6 five-grain verification (load-bearing per cycle Round 6 Surface D):**

1. **Substrate-shape grain.** `CanvasDirective` extended with `pending_documents`; all four consumer surfaces (ContextualCanvas, tabTitle, describeDirective, Zone1ConsolidatedPanel) extended atomically per RI-1 strict. AgentChatPanel.onDropEvent Prop signature + consumer shipped atomically.
2. **Per-trigger semantic coverage grain.** Pattern γ Rule 1 (drop event → new tab) exercised via E2E test #1 golden path; EC3.β one-tab-per-batch exercised via E2E test #3 multi-batch. Pattern γ Rules 2/3/4 unchanged from chunk 2.
3. **Per-trigger × per-decision-outcome conformance grain.** Drop event × success → tab opens + PendingDocumentsView renders + DocumentCard rows visible; drop event × ingest failure → toast persists; drop event × network failure → toast persists with NETWORK_ERROR code.
4. **Idempotency-and-side-effect-contract conformance grain.** `routeNewTab` is pure (chunk 2 unit tests preserved); chunk 3 handleDropEvent is a thin wrapper; idempotency inherits.
5. **Fifth grain — existing-canvas-view-consumer-contract conformance grain.** PendingDocumentsView renders in tab context (chunk 2 fifth-grain pattern); existing 12 canvas-view E2E specs unaffected (chunk 3 adds a new view; doesn't modify existing views). DocumentIntakeRail removal at Commit 2 is OUT of canvas-view consumer set (it was a Zone-1-adjacent sidebar component, not a canvas-view per the chunk 2 fifth-grain enumeration; verified at chunk 1 detachment + chunk 2 close).

**Two Laws verification scope** (per Round 6 Surface D + cycle closeout brief §5.3 Surface D):

- **INV-SERVICE-001 (service-wrapper):** drop handler invokes existing `ingestionService.handleDragDropUpload` via the existing route handler at `apps/web/src/app/api/orgs/[orgId]/documents/ingest/drag-drop/route.ts` (Pattern B external-wrap with `withInvariants` per chunk 6.2b ship). Inheritance preserved; chunk 3 adds zero new service-layer wrapping.
- **INV-SERVICE-002 (ledger-truth-model / trace_id propagation):** chat-input drop event generates `drop_session_id` (UUID via `crypto.randomUUID()` per chunk 6.2b precedent line 132); `drop_session_id` flows through endpoint → ingestionService → ingest_batch + document_cases + document_jobs writes. Integration test `chatInputIngestionFlow.test.ts` asserts trace_id consistency.
- **INV-AUTH-001 (RLS isolation):** chat-input drop operates within authenticated user session; existing endpoint `/api/orgs/[orgId]/documents/ingest/drag-drop` is RLS-scoped to user's active org via `withInvariants` org_id-vs-memberships check. Verification: existing `crossOrgRlsIsolation` floor test (unmodified) + new integration test `chatInputIngestionFlow.test.ts` exercises chat-input drop entry-point.
- **INV-DOC-001:** Phase 5.1 substrate; NOT chunk 3 scope. Forward-compatibility note: chunk 3 writes `document_type='unknown'` (per Cut 1 Flow (a) substrate); Phase 7 classifier + Phase 5.1 INV-DOC-001 enforcement work against this same substrate; chunk 3 substrate-shape is forward-compatible.
- **Phase 1.1 audit framework UF-001 (transaction atomicity):** inherits from `ingestionService.handleDragDropUpload` existing transactional shape (per service file-top comment; chunk 6.2b ship); no new atomicity surface introduced.

**Screenshot gate per CLAUDE.md UI-session-screenshot-gate (~6-8 shots).** See §8 for full gate scope + dual-purpose verification scope (§6.1 chunk 2 deferred-shots incidental verification).

**Pre-Commit-1 ceremony:**

```
feat(bridge): Phase 6.5 chunk 3 Commit 1 — chat-drop + staged tray + PendingDocumentsView + Pattern γ Rule 1 wire

[5-7 paragraphs covering: chunk 3 scope; AgentChatPanel drop affordance
(drag-drop + paste + "+" button); staged attachments tray; unified Send
refactor + ingest-only vs send-with-attached-message paths; three-moment
acknowledgment composite (tray + transient toast + canvas tab open per
Pattern γ Rule 1 + EC3.β); failure-path persist-until-dismissed; canvasDirective
pending_documents member RI-1 strict atomic ship with consumer (ContextualCanvas
+ tabTitle + describeDirective + Zone 1 nav); onDropEvent Prop signature +
consumer atomic ship at chunk 3 (chunk 2 brief §2 internal tension resolved);
RI-6 five-grain verification outcome; pre-Commit-2 deletion gate]

Cross-references:
- Session 11 chunk 3 brief: docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-3.md (this commit's brief SHA)
- Cycle closeout brief: 79a6ceb
- Chunks 1 + 2: 5a9492b + 94b0411 + c5d7e89
- v3 proposal: b7cb081
- Amended ui_architecture.md: 9e606f6
```

**Acceptance criteria:**

- All five validation gates green.
- Screenshot gate passes (founder ratifies all 6-8 shots; dual-purpose chunk 2 incidental verification noted at gate firing).
- Commit 1 lands on staging.
- Pre-Commit-2 deletion gate checklist verified.
- Push readiness three-condition gate met for Commit 1.

**Dependency:** Tasks 1-7 complete.

**Parallelizable:** terminal task at Commit 1 grain; not parallelizable.

### Task 9 — Commit 2 DocumentIntakeRail.tsx deletion

Deletion ceremony post Commit-1-verification-gate.

**Files:**
- Delete: `apps/web/src/components/canvas/DocumentIntakeRail.tsx`.
- Delete: DocumentIntakeRail-specific test files (Session 13 enumerates at impl-onset via grep).

**Pre-deletion verification protocol:**

```bash
# 1. Grep for residual imports.
grep -rln "DocumentIntakeRail" apps/web/

# 2. Confirm zero imports outside of:
#    - apps/web/src/components/canvas/DocumentIntakeRail.tsx itself
#    - apps/web/src/components/canvas/__tests__/DocumentIntakeRail.test.tsx (or similar; deletion target)
#    Expected output: only these two file paths.

# 3. Run validation gates one more time post-Commit-1 to baseline pre-deletion.
pnpm typecheck && pnpm test && pnpm agent:validate
```

**Commit 2 ceremony:**

```
feat(bridge): Phase 6.5 chunk 3 Commit 2 — DocumentIntakeRail.tsx deletion (verification-then-delete gate fired)

[3-4 paragraphs covering: Sub-Q4 two-step removal completion; PendingDocumentsView
port verified at Commit 1; component deletion + test-file deletions; rollback
posture (revert restores from git history); Phase 6.5 chunk 3 close ceremony.]

Cross-references:
- Session 11 chunk 3 brief: docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-3.md
- Sub-Q4 two-step removal lock: cycle closeout brief 79a6ceb §3.1
- Commit 1: [Commit 1 SHA captured at Session 13]
- ADR-0022 §2 supersession workflow
```

**Acceptance criteria:**

- DocumentIntakeRail.tsx deleted from disk.
- DocumentIntakeRail-specific test files deleted from disk.
- Grep confirms zero residual imports.
- Validation gates green post-deletion.
- Commit 2 lands on staging.
- Phase 6.5 chunk 3 close ceremony complete; chunk 3 closes terminally.
- Push readiness three-condition gate met (Phase 6.5 retrospective drafting fires next per Sub-Q6.d).

**Dependency:** Task 8 (Commit 1 ratified).

**Parallelizable:** terminal task; not parallelizable.

## 5. §7.3 carry-forward operationalizations

Each cycle closeout brief §7.3 carry-forward item gets a specific value picked at brief grain:

1. **Ingest-only-path vs send-with-attached-message-path UX**: BOTH paths ship per Sub-Q9.c.α. Ingest-only = drop + Send with empty text input → ingest fires; no chat turn appended. Send-with-attached-message = drop + type + Send → ingest AND chat turn appended (text becomes user turn; attachments become ingest). Send fires both paths conditionally per text-input state + attachments-state.

2. **Send disabled-rule extension**: enabled when `attachments.length > 0 || input.trim().length > 0`. Disabled-state visual: `bg-neutral-300 text-neutral-500 cursor-not-allowed`; tooltip on hover: "Add a message or attach a file to send".

3. **beforeunload prompt**: native `window.beforeunload` event handler; default browser prompt (cannot be styled per browser security model). Registers on first attachment add; unregisters on Send fire or all-attachments-removed.

4. **Toast/canvas-tab timing-overlap**: success-toast adapts — if `onDropEvent` already fired by mid-window (~1500ms into 3000ms standard fade), toast early-fades at 250ms transition; otherwise standard 3000ms fade. Session 13 may simplify to standard 3000ms always if testing surfaces no observable benefit from adaptive behavior.

5. **Failure-path persist-until-dismissed**: toast persists with error styling (red background + close button); dismissal triggers: close-button click + click-outside-toast-region + new-ingest-fire-dismisses-prior-error. Error copy from `ingestionService` error response or fallback "Ingest failed; please retry".

6. **Drag-drop-while-typing**: drop event always fires regardless of typing state; file appears in tray; text input cursor position preserved. Typing doesn't affect drop; drop doesn't affect typing or steal focus.

7. **Tray max-height + scroll**: max-height 144px (≈4 file entries visible without scroll at 36px each); `overflow-y: auto` beyond.

8. **Tray visual treatment**: filename-only display (no file preview at v1; preview is post-v1 ergonomic-enhancement candidate). Per-file: file-type icon (PDF / image / generic) leading + filename (truncated with ellipsis) + size badge trailing + remove button (×) appearing on hover.

9. **Tray ordering**: drop-order with most-recently-added at bottom (top-to-bottom reading reflects chronological add order; matches natural attention flow).

10. **Tab title for multi-file batch**: simple `"Pending Documents"` static label per `tabTitleForDirective` mapping (file count omitted from title; count rendered separately in PendingDocumentsView's section header — e.g., "Recent · N cards" or "Last drop · N cards" per DocumentIntakeRail line-284 / 297 precedent). Static label keeps multi-file vs single-file tab visually identical; simpler than alternative "Pending Documents (N)" dynamic label.

11. **Two-commit verification-then-delete gate**: Commit 1 = PendingDocumentsView port + all functional surfaces + dependent surfaces. Pre-Commit-2 verification gate fires (all Task 8 acceptance criteria green + render verification spot-check + grep for residual DocumentIntakeRail imports + existing 12 E2E specs preserved). Commit 2 = DocumentIntakeRail.tsx deletion + DocumentIntakeRail-specific test file deletions. Gate is operationalized at the orchestrator level (Session 13 WSL-side runs the gate; founder ratifies; Commit 2 fires only post-ratification).

## 6. Cross-task verification + integration testing

### Floor preservation

1146/1146 vitest (chunk 2 baseline at HEAD c5d7e89) + 26/26 agent:validate + 12 existing E2E specs + 1 chunk 1 E2E spec + 1 chunk 2 E2E spec all preserved.

### New unit tests (~10-15)

- AgentChatPanel.drop.test.tsx: drop handler state mutations + paste handler + "+" button + staged tray mutations + Send disabled-rule logic + toast lifecycle state transitions + beforeunload registration (subject to vitest DOM-environment availability).
- PendingDocumentsView.test.tsx: state machine transitions for ingestBatchId Prop vs no-Prop branches + soft-fail recovery + error state (subject to vitest DOM-environment availability).
- handleDropEvent.test.ts (or co-located): Pattern γ Rule 1 routeNewTab integration via handleDropEvent (node-env compatible pure-function path; not blocked by DOM-environment gap).
- tabTitle.test.ts extension: `pending_documents` case maps to 'Pending Documents'.
- canvasDirective.ts compile-time exhaustive check: TypeScript verifies all consumer switches handle the new member.

### New integration tests (~5-8)

- `chatInputIngestionFlow.test.ts` — chat-input drop → `ingestionService.handleDragDropUpload` → DB writes (`ingest_batches` + `source_documents` + `document_cases` + `document_jobs` + `audit_log` row at batch grain); trace_id propagation asserted; INV-SERVICE-002 verified.
- `chatInputMultiFileBatch.test.ts` — multi-file batch: single `drop_session_id` → single `ingest_batch_id` → N `document_cases` rows; EC3.β one-tab-per-batch substrate verification.
- `chatInputIngestionFailurePath.test.ts` — failure-path: service throws (e.g., file fails Zod validation mid-batch) → all-or-nothing rejection; trace_id consistency under failure.
- `chatInputCrossOrgRls.test.ts` — RLS isolation: org-A user drops files; org-B user cannot see resulting ingest_batch / document_cases via cross-org queries; INV-AUTH-001 verified.

### New E2E tests (~3-5)

- `phase-6-5-chat-drop.spec.ts` — golden path: open Bridge → drop file in chat → file appears in tray → Send → toast appears ("Ingesting 1 document...") → toast transitions to success → canvas tab opens with "Pending Documents" title → PendingDocumentsView renders DocumentCard.
- `phase-6-5-chat-drop-failure.spec.ts` — failure path: drop file with invalid mime → Send → toast appears → toast transitions to error → toast persists → user clicks close button → toast dismisses.
- `phase-6-5-chat-drop-multi-batch.spec.ts` — multi-batch path (Shot 2-4 incidental verification per §6.1): drop batch 1 (3 files) → wait for tab 1 → drop batch 2 (3 files) → wait for tab 2 → drop batch 3 (3 files) → wait for tab 3 → verify 3 tabs in CanvasTabStrip; switch between tabs; verify each tab renders correct ingest_batch_id-specific PendingDocumentsView state.
- `phase-6-5-chat-drop-paste.spec.ts` — paste path: clipboard contains file → paste in chat → file appears in tray → Send → ingest fires (same downstream as drop).
- `phase-6-5-chat-drop-plus-button.spec.ts` — "+" button path: click + button → file picker opens → select files → files appear in tray → Send → ingest fires.

### DocumentIntakeRail test-coverage migration

Existing DocumentIntakeRail test files (Session 13 enumerates via grep at impl-onset) — migrate test coverage to PendingDocumentsView where the state machine behavior remains; delete tests covering the drop affordance (moved to AgentChatPanel; covered by AgentChatPanel.drop.test.tsx). The migration sits within Task 1 (PendingDocumentsView port) + Task 9 (Commit 2 deletion); brief estimate ~5-8 tests migrate, ~3-5 tests delete (specific counts at Session 13 grain post-grep).

### Test infrastructure additions

- Chat-input drop E2E fixture: helper to compose `DataTransfer` object with File entries (Playwright pattern: `page.evaluate((files) => { const dt = new DataTransfer(); ... })` OR via `setInputFiles` on hidden file input).
- Chat-input multi-batch fixture: helper to fire sequential drops with controlled `drop_session_id` (mock `crypto.randomUUID` if needed for deterministic E2E assertions).

### 6.1 Chunk 2 deferred-shots incidental verification scope

Chunk 3's screenshot gate exercises chunk 2's deferred multi-tab states as side effect of chunk 3 drop-to-tab flow:

- **Chunk 3 Shot N (single-tab post-drop)** — verifies chunk 2 single-tab → single-tab path (Shot 1 chunk 2 surface already passed; chunk 3 Shot N is incidental re-verification).
- **Chunk 3 Shot N+1 (multi-tab 3 tabs post-3-drops)** — incidentally verifies chunk 2 Shot 2 (multi-tab 3 tabs) because chunk 3's drop flow opens 3 separate tabs via 3 sequential drop batches (each batch via EC3.β = new tab).
- **Chunk 3 Shot N+2 (multi-tab 5 tabs post-5-drops)** — incidentally verifies chunk 2 Shot 3 (multi-tab 5 tabs / overflow threshold).
- **Chunk 3 Shot N+3 (multi-tab 6+ tabs post-6-drops)** — incidentally verifies chunk 2 Shot 4 (multi-tab 6+ tabs / overflow shadow indicators).
- **Chunk 3 Shot N+4 (EC1.β prompt on Zone 1 nav)** — directly verifies chunk 2 Shot 5 (independent of drop flow; verifiable as part of chunk 3 gate firing for operational coherence).

If chunk 3 screenshot gate fires cleanly (founder ratifies all 6-8 shots), **chunk 2 deferred-empirical-verification carry RESOLVES**. If chunk 3 gate surfaces issues at the multi-tab states (e.g., overflow rendering broken at N=6 in actual user workflow), retrospective evaluates whether the chunk 2 partial-pass was masked by the agent orgId bug AND whether chunk 3's incidental coverage is sufficient to ratify chunk 2 close, OR whether a chunk-2-amendment cycle is warranted.

Brief enumerates the explicit chunk 3 gate-firing protocol covering multi-batch scenarios (3-tab; 5-tab; 6+-tab); the firing aligns with chunk 3's natural drop-to-tab flow + EC3.β one-tab-per-batch behavior. Operationally, Session 13 founder fires sequential drops in the same dev session as chunk 3 gate capture; the multi-tab states accumulate naturally.

## 7. Two Laws verification scope

Per cycle closeout brief §5.3 Surface D + Round 6 chunk 3 Surface D:

- **INV-SERVICE-001 (service-wrapper inheritance):** drop handler invokes existing `ingestionService.handleDragDropUpload` (Pattern B external-wrap with `withInvariants` at route handler boundary per chunk 6.2b ship). Verification: existing route handler at `apps/web/src/app/api/orgs/[orgId]/documents/ingest/drag-drop/route.ts` lines 104-117 wraps the service call via `withInvariants(ingestionService.handleDragDropUpload)(input, ctx)`; chunk 3 adds zero new service-layer wrapping. Inheritance preserved.

- **INV-SERVICE-002 (ledger-truth-model / trace_id propagation):** trace_id flows from chat-input drop event → `drop_session_id` (UUID; chunk 6.2b precedent at DocumentIntakeRail.tsx line 132) → FormData → endpoint → ingestionService call → `ingest_batches` + `document_cases` + `document_jobs` rows + `audit_log` row at batch grain (single trace_id per Sub-Q8 lock + INV-AUDIT-001 leaf per chunk 6.2b file-top comment lines 55-56). Verification via `chatInputIngestionFlow.test.ts` integration test asserting trace_id consistency across the substrate write set.

- **INV-AUTH-001 (RLS isolation):** chat-input drop operates within authenticated user session; existing endpoint is RLS-scoped via `withInvariants` org_id-vs-memberships check (Invariants 1-3 per chunk 6.2b route handler file-top comment lines 22-31). Verification via existing `crossOrgRlsIsolation` floor test (unmodified) + new `chatInputCrossOrgRls.test.ts` integration test exercising chat-input drop entry-point cross-org isolation.

- **INV-DOC-001:** Phase 5.1 substrate; NOT chunk 3 scope. Forward-compatibility: chunk 3 writes `document_type='unknown'` per Cut 1 Flow (a) substrate; Phase 7 classifier + Phase 5.1 INV-DOC-001 enforcement work against this substrate; chunk 3 substrate-shape is forward-compatible.

- **Phase 1.1 audit framework UF-001 (transaction atomicity):** inherits from `ingestionService.handleDragDropUpload` existing transactional shape (atomic 5-table substrate write via `create_ingest_batch_with_documents_with_audit` RPC per chunk 6.1 migration 152). Chunk 3 introduces zero new atomicity surface.

## 8. Substrate touchpoint risks + mitigations

Six risks at chunk 3 grain. None require scope adjustment at brief-drafting; all are operationalizable at Session 13 onset.

### Risk 1 — DocumentIntakeRail state machine port complexity

**Risk:** Session A §A.10 implies "mostly copy" for the port; brief operationalization at §3 + Task 1 confirms the 6-state machine maps cleanly. But if Session 13 implementation surfaces unforeseen complexity (e.g., DocumentIntakeRail's `useEffect`-driven mount-fetch couples to component identity in ways that break when relocated to a tab-rendered context; OR section-header rendering coupling that doesn't simplify cleanly), brief surfaces scope expansion. Risk: port-complexity-surprise → reactive Path C invocation at Tasks 1-8 vs Task 9 boundary.

**Mitigation:** Brief reads DocumentIntakeRail.tsx at Session 11 onset; enumerated state transitions (6 states; 311 LOC source) match the port surface. If Session 13 surfaces state-to-UI coupling not enumerated at brief grain (e.g., DocumentCard rendering surprises due to tab-context size constraints differing from intake-rail sidebar size), brief carries the carry-forward to Phase 6.5 retrospective drafting (codification candidate: state-machine-to-UI-coupling reveal at port grain).

### Risk 2 — Three-moment acknowledgment timing coordination

**Risk:** Toast/canvas-tab timing-overlap (§5 carry-forward #4) requires coordination between AgentChatPanel toast lifecycle + SplitScreenLayout handleDropEvent → tab open. Race condition possible: toast steady-state vs tab open completing in unpredictable order; user perceives mismatched UX.

**Mitigation:** Task 6 toast lifecycle uses `appearedAt` timestamp + elapsed-time check at `useEffect` dependency on `toastState`; Task 7 `handleDropEvent` fires synchronously on `routeNewTab` state update. Operational specification: success-toast fade adapts based on mid-window check (early-fade if past 1500ms; standard if before). Session 13 implementation may simplify to always-standard-3000ms if testing surfaces no observable benefit. E2E test `phase-6-5-chat-drop.spec.ts` covers golden-path timing.

### Risk 3 — Failure-path dismissal interaction conflicts

**Risk:** Failure toast persists until dismissed; user dismisses via close button OR click-outside. Conflict scenario: user fires new ingest during failure toast persistence → new ingest starts but failure toast still visible → user confusion about whether new ingest fired or failure is being retried.

**Mitigation:** §5 carry-forward #5 specifies new-ingest-fire-dismisses-prior-error-toast (explicit operational rule). Task 6 implements: `fireIngest` first invokes `setToastState({ kind: 'ingesting', ... })` which transitions away from `error` state automatically. User UX: dismissal happens implicitly when retry fires; explicit dismissal (close button + click-outside) still available before retry.

### Risk 4 — DocumentIntakeRail-to-PendingDocumentsView test coverage migration

**Risk:** DocumentIntakeRail has test coverage (estimate 5-8 tests per Session 11 grep estimate; exact count at Session 13 grain); migrating tests to PendingDocumentsView during port is bounded but non-trivial. Risk: tests miss migration boundary; coverage drops at chunk 3 close OR tests cover the wrong component (DocumentIntakeRail-specific behaviors that don't apply to PendingDocumentsView like drop affordance).

**Mitigation:** Task 1 PendingDocumentsView port + Task 8 cross-task verification include explicit test-coverage migration check. Session 13 onset enumerates DocumentIntakeRail's test surfaces via grep; Task 1 ports state-machine-applicable tests; Task 8 verifies coverage. Tests covering DocumentIntakeRail's drop affordance migrate to AgentChatPanel.drop.test.tsx (Task 4 surface); tests covering state machine behavior (idle_with_recent_cards, showing_batch, error) migrate to PendingDocumentsView.test.tsx (Task 1 surface).

### Risk 5 — Commit 2 deletion verification-then-delete gate

**Risk:** DocumentIntakeRail.tsx deletion is irreversible at substrate-shape grain (git revert restores from history; no gradual rollback). Commit 1 ships with subtle bug in PendingDocumentsView that doesn't surface in test gates; Commit 2 deletes; bug surfaces post-chunk-3-close; forward-fix requires recreating DocumentIntakeRail file from git history.

**Mitigation:** Cycle closeout brief §5.3 Surface B documented verification-then-delete gate; Task 9 includes pre-deletion gate firing protocol (all Task 8 acceptance criteria green; PendingDocumentsView renders cleanly per founder spot-check; existing E2E specs preserved + new E2E specs green; grep confirms zero residual DocumentIntakeRail imports). Founder ratifies gate before Task 9 fires. Rollback: if Commit 2 surfaces issue, revert restores the file from git; the original three-week chunk 1 + chunk 2 + chunk 3 Commit 1 work stands.

### Risk 6 — Chunk 2 deferred-shots incidental verification robustness

**Risk:** Chunk 3's screenshot gate exercises chunk 2's multi-tab UI as side effect (§6.1 dual-purpose scope). If chunk 3 gate doesn't exercise sufficient multi-tab states (e.g., founder only fires 1-2 ingest batches during gate capture), chunk 2 deferred Shots 2-4 remain unverified at chunk 3 close.

**Mitigation:** Brief enumerates explicit chunk 3 gate-firing protocol covering multi-batch scenarios (3-tab; 5-tab; 6+-tab) in §8 Shot enumeration. Session 13 founder fires sequential drops in same dev session as chunk 3 gate capture; multi-tab states accumulate naturally. If despite protocol, chunk 3 gate doesn't reach 6+ tabs, chunk 2 deferred-empirical-verification carry persists into Phase 6.5 retrospective; retrospective evaluates codification options (chunk-2-amendment cycle vs accept-as-tested-by-impl-evidence-not-screenshot-evidence).

**Screenshot gate (~6-8 shots; dual-purpose scope per §6.1):**

1. **Default chat panel state.** Bridge open; AgentChatPanel expanded; staged tray empty; Send button disabled (no input + no attachments). Verify: chat input visible; tray region empty/hidden; Send disabled visual.

2. **Single-file drop staged.** Drag one PDF into chat input region → file appears in staged tray. Verify: tray shows 1 entry with PDF icon + filename + size; Send button enabled.

3. **Multi-file drop staged + Send fire.** Drop 3 files → tray shows 3 entries → click Send → toast appears "Ingesting 3 documents..." → toast transitions to success → canvas tab opens with "Pending Documents" title. Verify: tray cleared post-Send; toast appearance + transition; new tab in CanvasTabStrip; PendingDocumentsView rendered.

4. **Multi-batch (3 tabs)** — Shot 2 incidental verification per §6.1. Fire 3 sequential drop batches → 3 tabs in CanvasTabStrip. Verify: 3 separate tabs; active-tab visual distinction; close buttons appear on hover.

5. **Multi-batch (6+ tabs / overflow)** — Shot 3 + Shot 4 incidental verification per §6.1. Fire 6+ sequential drops → tab strip overflows → horizontal scroll + shadow indicators visible. Verify: overflow scroll renders; shadow indicators at boundaries.

6. **EC1.β prompt-on-replace (chunk 2 Shot 5 direct verification per §6.1).** From multi-tab state, click Zone 1 nav item (e.g., Open Bills) → window.confirm prompt fires → accept → active tab's directive replaces.

7. **Failure-path persistence.** Drop a file that triggers server error (e.g., temporarily disable backend OR drop oversized file) → Send → toast transitions to error → toast persists → user clicks close button → toast dismisses. Verify: error toast styling (red); close button rendered; persistence; dismissal mechanism.

8. **Pending Documents nav item with count badge.** From Zone 1 expanded mode, verify "Pending Documents" nav item rendered as 2nd item (between New Bill and Open Bills) with count badge showing N (where N is current incomplete-state document_cases count for active org). Click nav item → navigates to PendingDocumentsView via existing Zone 1 routing (handleMainframeNavigate → routeReplaceActive with EC1.β prompt; OR direct routing if no active tab is dirty — Session 13 picks specific based on user flow).

Screenshot capture happens at Session 13 founder-review grain post-implementation; orchestrator (Session 13 WSL-side) drafts the prescribed sequence at scope-lock; founder captures + orchestrator spot-checks per CLAUDE.md UI-session-screenshot-gate Steps 1-4.

## 9. Implementation order + parallelization

### Strict-sequence dependencies

```
Task 1 (PendingDocumentsView)
  ↓
Task 2 (canvasDirective + 3 consumer extensions; atomic RI-1 strict)
  ↓
  ├─ Task 3 (Zone 1 Billing nav 9th item) ──┐
  ├─ Task 4 (AgentChatPanel drop + tray + beforeunload) ──┤
  └─ Task 7 (SplitScreenLayout handleDropEvent + AgentChatPanel onDropEvent Prop) ──┘
            ↓
       Task 5 (Unified Send refactor; depends on Task 4 tray + Task 7 Prop)
            ↓
       Task 6 (Three-moment acknowledgment composite; depends on Tasks 4 + 5 + 7)
            ↓
       Task 8 (Cross-task verification + tests + screenshot gate; Commit 1 ceremony)
            ↓
       Task 9 (Commit 2 DocumentIntakeRail deletion; verification-then-delete gate)
```

### Subagent dispatch shape (if Session 13 chooses parallel dispatch)

- **Subagent A** — Task 1: PendingDocumentsView port (~200-280 LOC + ~5-8 tests).
- **Subagent B** — Task 2: canvasDirective + ContextualCanvas + tabTitle + describeDirective atomic four-file extension (~15-20 LOC total + compile-check verification).
- *After Subagents A + B complete:*
- **Subagent C** — Task 3: Zone 1 Billing nav item with count badge (~30-50 LOC; standalone).
- **Subagent D** — Task 4: AgentChatPanel drop affordance + staged tray + beforeunload (~120-180 LOC + ~6-10 tests).
- **Subagent E** — Task 7: SplitScreenLayout handleDropEvent + AgentChatPanel onDropEvent Prop signature (~30-50 LOC + integration with Subagent D's Prop interface).
- *After Subagents C + D + E complete:*
- **Subagent F** — Task 5: Unified Send refactor (~50-80 LOC; tight coupling with Task 4's tray state + Task 7's Prop).
- **Subagent G** — Task 6: Three-moment acknowledgment composite (toast lifecycle + timing-overlap; ~80-120 LOC + tests).
- *After Subagents F + G complete:*
- **Subagent H** — Task 8: Cross-task verification + tests + screenshot gate prep (~200-400 LOC for new test files; full validation gate firing).
- **Subagent I** (Commit 2) — Task 9: DocumentIntakeRail deletion + verification-then-delete gate firing.

### Sequential direct alternative

Session 13 may elect sequential single-subagent (or direct WSL-side implementation) instead of parallel subagent dispatch. Chunk 3's substantive complexity is concentrated in Tasks 4 + 5 + 6 (AgentChatPanel + Send + toast lifecycle) which couple tightly; splitting these across subagents may not yield meaningful parallelization win.

**Recommendation at brief-drafting grain:** Sequential direct OR partial-parallel (Tasks 1 + 2 sequential foundation; Tasks 3 + 4 + 7 parallel; Tasks 5 + 6 sequential after; Task 8 + 9 sequential terminal). Session 13 onset adjudicates based on operator preference + scope realized state.

## 10. Cross-references

### Cycle predecessors

- **Cycle closeout brief**: `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`). §5.3 chunk 3 acceptance criteria 21-item checklist + §7.3 chunk 3 partial-information carry-forwards 11 items + §6 inter-chunk dependency map (chunk 3 terminal).
- **v3 proposal**: `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`). §5.1 chunk 3 decomposition + §3 Cut 1 (Flow (a) substrate exclusively at v1) + §3 Cut 9 Pattern γ Rule 1 (drop event → new tab).
- **Amended `ui_architecture.md`**: `docs/03_architecture/ui_architecture.md` (commit `9e606f6`). § The Three-Path Entry Model § Path 2 Chat amendment notation (Phase 6.5 chunk 3 drag-drop + paste + "+" button + staged tray + unified Send + PendingDocumentsView canvas tab open per Cut 9 Pattern γ).
- **Chunk 1 brief + ship**: `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` (commit `1c65cd1`) + ship at `5a9492b`. Zone 1 Region 7.2 Billing nav substrate; useShellState + useWorkspace hooks; chunk-1 screenshot gate full pass precedent.
- **Chunk 2 brief + ship**: `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-2.md` (commit `05b478b`) + implementations at `94b0411` (10a substrate) + `c5d7e89` (10b feature). Tab data model state lift; canvasTabRouting six pure functions; CanvasTabStrip UI; tabTitle.ts mapping; per-source callback structure; chunk-2 deferred screenshot gate (Shots 2-5).
- **Session A preserved-evidence**: `docs/09_briefs/phase-6/2026-05-16-session-a-preserved-evidence.md` (commit `7834a26`). §A.6 AGENT panel substrate ownership inventory; §A.7 chat substrate shape (`agent_sessions.turns` JSONB; no `chat_messages` table); §A.10 DocumentIntakeRail state machine reference.

### Memory pointers (post-chunk-2 carry-forwards consumed)

- `project_phase_6_5_chunk_1_billing_nav_ordering.md` — Billing nav 8-item canonical order; chunk 3 inserts `pending_documents` as 2nd item (between New Bill primary action and Open Bills per workflow-frequency reading).
- `project_phase_6_5_full_page_routes_layout.md` — three-zone shell vs full-page route boundary; PendingDocumentsView renders in Zone 3 tab context (Pattern γ Rule 1 emits via `routeNewTab`); not a full-page route.

### ADRs

- **ADR-0010** — substrate-now-enforcement-later. Cumulative UI-layer instances catalog at Phase 6.5 retrospective drafting:
  1. Sub-Q7.4.α' Region 7.4 hidden with structural reservation (chunk 1 shipped).
  2. Sub-Q8.c.α₁→α₂ localStorage → DB-column persistence post-v1 (chunk 1 shipped α₁).
  3. Sub-Q9.d.α→δ session-only → IndexedDB attachment-persistence post-v1 (chunk 3 ships α; beforeunload prompt as defensive bridge).
  4. EC1.β v1-default-prompt-mechanism (chunk 2 commit body ratified instance).
  5. *Phase 6.5 retrospective adjudication candidate*: chunk 2 brief §2 "onDropEvent Prop additive at chunk 2 with chunk 3 consumer" instance — disk-verified did NOT ship at chunk 2; chunk 3 ships atomically per RI-1 strict at Prop-API grain. Retrospective evaluates whether to keep in catalog (N=5) OR reclassify as RI-1 strict (N=4).
- **ADR-0020** — source-tree authority-layer enumeration. Chunk 3 creates `apps/web/src/components/canvas/PendingDocumentsView.tsx` (existing `components/canvas/` subdir; co-located with deletion target DocumentIntakeRail.tsx). No new authority-layer sibling creation.
- **ADR-0022 §2** — supersession workflow. Chunk 3 closes the DocumentIntakeRail two-step removal via Commit 2 deletion per Sub-Q4 lock; ui_architecture.md amendment per Session 4 already shipped (chunk 3 inherits).

### Code surfaces (verified at HEAD `c5d7e89`)

- `apps/web/src/components/bridge/SplitScreenLayout.tsx` (~358 lines post-chunk-2; substantive extension at handleDropEvent + onDropEvent Prop pass).
- `apps/web/src/components/bridge/AgentChatPanel.tsx` (~880 lines post-chunk-1; substantive extension at drop handler + staged tray + Send refactor + toast lifecycle + onDropEvent Prop signature).
- `apps/web/src/components/bridge/ContextualCanvas.tsx` (~239 lines; small extension at `renderDirective` switch for `pending_documents` case).
- `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx` (~310 lines; small extension at BILLING_NAV_ITEMS + navItemToDirective for `pending_documents` 9th item with count badge).
- `apps/web/src/components/bridge/canvasTabRouting.ts` (228 lines; unchanged at chunk 3; consumer of `routeNewTab` Rule 1).
- `apps/web/src/components/bridge/CanvasTabStrip.tsx` (chunk 2 ship; unchanged at chunk 3; consumer of new tab via routeNewTab).
- `apps/web/src/components/canvas/DocumentIntakeRail.tsx` (311 lines; Commit 2 deletion target).
- `apps/web/src/shared/types/canvasDirective.ts` (38 active members at HEAD; 64 lines; small extension at member addition).
- `apps/web/src/shared/types/tabTitle.ts` (38 cases at HEAD; small extension at case addition).
- `apps/web/src/shared/types/canvasContext.ts` (singleton-shape preserved at TYPE level per Sub-Q18.α; zero touch at chunk 3).
- `apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts` (~120 lines; `describeDirective` exhaustive switch; small extension at case addition).
- `apps/web/src/services/document-platform/ingestionService.ts` (785 lines per Session A §A.6; unchanged at chunk 3; consumer of existing `handleDragDropUpload`). **Path correction note**: cycle closeout brief §10.4 + Session A §A.6 reference `apps/web/src/services/document-platform/ingestionService.ts`; the file's actual path is `apps/web/src/services/document-platform/ingestionService.ts` (path drift caught at brief-draft against the originating proposal's `services/ingestion/` reference; brief uses the canonical path).
- `apps/web/src/app/api/orgs/[orgId]/documents/ingest/drag-drop/route.ts` (chunk 6.2b ship; 150 lines; unchanged at chunk 3; consumer reuses).
- `apps/web/src/hooks/useShellState.ts` + `useWorkspace.ts` (chunk 1 ship; unchanged at chunk 3).

### Governance precedents

- **Phase 2.5** commit ceremony shape: per-amendment commit-grain governance trail. Chunk 3's two-commit ceremony inherits the per-substantive-step commit-grain discipline.
- **F-J-θ first-instance cross-phase substrate-modification**: `docs/09_briefs/phase-4/chunks/2026-05-14-phase-4-chunk-2.md`. Chunk 3 is NOT a substrate-modification chunk at the cross-phase grain (no DB schema / ENUM / RPC changes); F-J-θ governs chunks 1+2+3 only at the cycle-spec governance-trail grain (chunk-bodied commits + brief artifacts + retrospective writeup).
- **Phase 4 retrospective RI-1, RI-6, RI-7, RI-10**: `docs/07_governance/retrospectives/phase-4-retrospective.md`. RI-1 strict at chunk 3 Prop-API grain (onDropEvent atomic ship). RI-6 five-grain verification at chunk 3 Surface D + Task 8. RI-7 invocation NEGATIVE at brief-drafting grain (single-session implementation feasible; reactive Path C available at Tasks 1-8 vs Task 9 fault line). RI-10 framing count = 0 at brief-drafting (cycle locked all sub-Qs; no framing-revisits surface).
- **Chunk-6.2a + chunk-6.2b** (Path C prospective substrate-vs-feature split): `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-2a.md` + `chunk-2b.md`. Chunk 3 doesn't invoke Path C at brief grain; precedent stays available for reactive invocation at Session 13.
- **Chunk-6.3a precedent**: native HTML5 drag-drop pattern (chunk-6.2b ship); chunk 3 inherits the drag-drop UI pattern at a new grain (chat input vs canvas-rail).
- **Phase 6 retrospective**: `docs/07_governance/retrospectives/phase-6-retrospective.md`. Phase 6 closeout reference for ingestion substrate inheritance + chunk-6.2b LOC-undercount banking (Phase 6.5 retro candidate).

### Downstream

- **Phase 6.5 retrospective** (Session 13 close ceremony OR Session 13+1): `docs/07_governance/retrospectives/phase-6.5-retrospective.md` per Sub-Q6.d. Inputs from chunk 3:
  - ADR-0010 UI-layer instances catalog adjudication (N=4 vs N=5 with onDropEvent reclassification).
  - Path C invocation grain catalog (brief-draft prospective N=1 + Phase-A-close-prospective N=1 + mid-impl-reactive N=1).
  - Agent orgId session-context bug as Phase-6.5-revealed pre-existing finding; post-Phase-6.5 remediation session candidate.
  - Screenshot gate verification surface independence codification (gate should prescribe surfaces reachable via the chunk's own substrate, not via cross-substrate dependencies).
  - Verification-asymmetry across chunks 1/2/3 (chunk 1 full pass; chunk 2 partial; chunk 3 expected full; chunk 3 incidentally verifies chunk 2 deferred shots via §6.1 dual-purpose scope).
  - vitest DOM-environment gap N=4 (chunk-6.2b + Phase 6 retro candidate + chunk 1 + chunk 2 + chunk 3 inheritance).
  - LOC-forecast-methodology calibration: chunk 3 LOC at impl close may further calibrate RI-7's empirical bounds (chunk-6.2b's 97%-over-upper-bound was banked at N=3 observation-grain).
  - DocumentIntakeRail two-step verification-then-delete gate as Sub-Q4 precedent at Phase 6.5 retro codification.
  - Multi-tab drop-event-driven verification pattern at chunk 3 (Pattern γ Rule 1 + EC3.β empirically exercised across multiple drop batches).
- **Phase 5.1 amendments** (Sessions 14-15 per v3 §7 Step 7): downstream substrate consumer; v1 substrate-shape forward-compatibility verified at chunk 3 (document_type='unknown' written; Phase 7 classifier reclassifies post-extraction).
- **Phase 7 (Extraction)** (Sessions 17+): consumer of chunk 3's substrate; reads `document_jobs` rows from ingest_batches created via chat-input drop; classifies + routes via existing documentRouterService (Phase 4 ship).

# Phase 6.5 Chunk 1 Brief — Bridge shell consolidation + Zone 1 + workspace switches

- **Date:** 2026-05-16
- **Phase:** Phase 6.5 (amendment cycle per Phase 2.5 precedent)
- **Chunk:** 1 of 3 (chunk 1 shell consolidation; chunk 2 multi-tab canvas; chunk 3 chat-drop + PendingDocumentsView + DocumentIntakeRail deletion)
- **Status:** brief-drafting (Session 5) → scope-lock or implementation prep (Session 6) → implementation + chunk-close (Session 7)
- **Session shape:** three-session Shape A per v3 §7 Step 4-6. Session 6 may be light (absorbing into implementation prep) per operational flex.
- **Predecessors:** cycle closeout brief at `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`); v3 proposal at `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`); amended `ui_architecture.md` at `docs/03_architecture/ui_architecture.md` (commit `9e606f6`).
- **Baseline at Session 5 onset:** HEAD = `a46242e` on staging; `pnpm test` 1114/1114; `pnpm agent:validate` 26/26; `pnpm typecheck` green (per cycle closeout brief §8.2; no source changes since).

## 1. Preamble

This brief operationalizes Phase 6.5 chunk 1 at implementation-task grain. The cycle closeout brief at `79a6ceb` locked all substantive decisions across 20 sub-questions; this brief converts those locks into a numbered task list with per-task acceptance criteria, dependency annotations, and concrete values for the nine partial-information items the cycle deferred to brief-drafting (§7.1 of the cycle brief).

**Inheritance posture.** The cycle closeout brief is authoritative for *what* chunk 1 ships (Cuts 4-8 three-zone consolidation; Sub-Q7 leans Zone 1 four-region layout; Sub-Q8 leans collapsibility; workspace-as-module per Sub-Q7.1.a 7.1.α). This brief is authoritative for *how* chunk 1 builds it (component decomposition; hook signatures; visual treatments; keyboard bindings; localStorage key shapes). The cycle brief wins on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

**Cross-references.**

- Cycle closeout brief: `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`) — §5.1 chunk 1 acceptance criteria; §7.1 chunk 1 partial-information carry-forwards; §6 inter-chunk dependency map.
- v3 proposal: `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`) — §5.1 chunk decomposition; §3 Cuts 4-8 substrate cuts.
- Amended `ui_architecture.md` (commit `9e606f6`) — § The Split-Screen Layout (post-Phase-6.5 three-zone description); § The Three-Path Entry Model § Path 1 (Zone 1 post-Phase-6.5 description); § Shell architecture history (pre-Phase-6.5 preserved per ADR-0022 §2).
- ADR-0010 (substrate-now-enforcement-later) — Region 7.4 structural reservation discipline.
- ADR-0022 §2 (supersession workflow) — DocumentIntakeRail two-step removal precedent.
- Phase 6 chunk-6.3a brief at `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-3a.md` — structural shape inheritance (sub-Q resolution → verify-from-disk → reasoning chain; chunk-1's shape is task-decomposition-oriented because all sub-Qs are already locked at cycle close).
- F-J-θ precedent at `docs/09_briefs/phase-4/chunks/2026-05-14-phase-4-chunk-2.md` — cross-phase substrate-modification grain (Session A §A.5 inheritance; chunk 1 is NOT a substrate-modification chunk — pure React state + component work — but the grain precedent governs how cross-phase chunks shape their commit ceremony).

**This brief seeds `docs/09_briefs/phase-6.5/chunks/`.** Naming per Sub-Q6.b.α convention: `YYYY-MM-DD-phase-6-5-chunk-N.md` (phase-6.5 → phase-6-5 hyphenated for filename per Phase 2.5 commit-a/b precedent).

## 2. Scope

### What chunk 1 ships

- **Three-zone shell consolidation per Cuts 4-8.** Four-zone shell (Mainframe rail + Agent chat + Contextual canvas + DocumentIntakeRail) → three-zone shell (Zone 1 consolidated left panel + Zone 2 AgentChatPanel + Zone 3 ContextualCanvas).
- **Zone 1 four-region layout per Sub-Q7 leans.**
  - Region 7.1 — vertical workspace tabs (Billing | Reports) per Sub-Q7.1.b.β.
  - Region 7.2 — workspace-scoped navigation items per Sub-Q7.2.a (Billing 8 items at chunk-1 close; Reports 3 items).
  - Region 7.3 — persistent foundational footer (CoA + Journal Entries + Recurring Journals + AI Action Review) per Sub-Q7.3.β.
  - Region 7.4 — hidden with structural reservation per Sub-Q7.4.α′ (zero-render at v1; activates post-v1 multi-session chat substrate per ADR-0010 substrate-now-enforcement-later).
- **Workspace switches per Sub-Q7.1.a 7.1.α + Sub-Q7.1.b.β** (Billing ↔ Reports; vertical sidebar list affordance).
- **Zone 1 + Zone 2 collapsibility per Sub-Q8 leans** (Zone 1 → 64px rail-mode matching `w-16` MainframeRail width; Zone 2 → 44px rail-mode with new-output badge; keyboard + button triggers per Sub-Q8.a.α; localStorage persistence per Sub-Q8.c.α₁).
- **MainframeRail detachment + deletion.** `MainframeRail.tsx` removed from `SplitScreenLayout`; the component file is also deleted at chunk 1 (no two-step delay — Mainframe rail's semantic role transfers cleanly to Zone 1 Region 7.2 + 7.3 at the same commit; no test-then-delete gate is needed because Zone 1 directly subsumes its navigation surface).
- **DocumentIntakeRail mount detachment.** `<DocumentIntakeRail />` mount removed from `SplitScreenLayout`. **The component file `apps/web/src/components/canvas/DocumentIntakeRail.tsx` STAYS on disk** for chunk 3 deletion (Sub-Q4 two-step removal: chunk 1 detaches mount; chunk 3 deletes file after PendingDocumentsView state-machine port confirms green).

### What chunk 1 does NOT ship

Deferred to chunk 2 (multi-tab canvas):

- ContextualCanvas changes — Cut 6 don't-touch-canvas at chunk 1; multi-tab refactor lifts navigation history at chunk 2 Sub-Q11.b.α state lift.
- Per-source callbacks (`onAgentDirective` + `onMainframeNavigate` + `onCanvasDrillDown` + `onDropEvent`) — chunk 2 Sub-Q11.b state lift.
- canvasContext binding amendment — chunk 2 Sub-Q18.α active-tab-binding.
- Tab strip UI affordance — chunk 2 Sub-Q11.c.α.

Deferred to chunk 3 (chat-drop + PendingDocumentsView):

- AgentChatPanel drop handler (drag-drop + paste + "+" button).
- Staged attachments tray above chat input.
- PendingDocumentsView canvas view (ports DocumentIntakeRail `idle_with_recent_cards` state machine).
- DocumentIntakeRail.tsx file deletion (chunk 1 detaches mount only).
- `canvasDirective.ts` extension with `'pending_documents'` directive.
- "Pending Documents" navigation item in Zone 1 Billing workspace (8 → 9 items).

## 3. Substrate touchpoints

Verify-from-disk at session-onset (Session 7 implementation) before consuming any line/path reference below. All counts/paths confirmed at Session 5 brief-drafting against HEAD `a46242e`.

### Files modified

- **`apps/web/src/components/bridge/SplitScreenLayout.tsx`** (145 lines pre-chunk-1). Major rework: remove `MainframeRail` mount (line 116-119); remove `<DocumentIntakeRail />` mount (line 141); insert `<Zone1ConsolidatedPanel />` mount in its place; integrate Zone 1 + Zone 2 collapsed-state rendering at SplitScreenLayout grain (collapsed bridge zones render compact rails inline). Preserve: `directive` + `selectedEntity` + `currentUserRole` state; `handleCanvasNavigate` callback; `canvasContext` assembly; OrgSwitcher + AvatarDropdown top nav strip. `chatCollapsed` state migrates to `useShellState` hook (replaced by `zone2Collapsed`).
- **`apps/web/src/components/bridge/AgentChatPanel.tsx`** (788 lines pre-chunk-1). Extend to render collapsed-state at 44px rail-mode with new-output badge. Touch grain: small (~30-50 LOC addition to component file). Preserve: `onCollapse` Prop signature (line 39); two-mode wrapper (Onboarding vs ProductionChat); all existing send / persona / canvasContext / ProposedEntryCard wiring.

### Files created

- **`apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx`** (new; ~250-350 LOC estimated). Renders Zone 1's four regions inline (no per-region subcomponent files unless one region exceeds ~80 LOC during impl). Consumes `useShellState` for `zone1Collapsed` + `useWorkspace` for `activeWorkspace`. Renders collapsed-state rail-mode (64px) inline when `zone1Collapsed === true`. Receives `orgId` + `onNavigate: CanvasNavigateFn` Props (parent SplitScreenLayout assembles).
- **`apps/web/src/hooks/useShellState.ts`** (new; ~40-60 LOC estimated). First file under new `apps/web/src/hooks/` directory; permitted per `apps/web/src/README.md` "Authority-layer enumeration" ("cross-cutting React hooks; create when the first cross-cutting hook is authored"). Hook signature: `useShellState(): { zone1Collapsed: boolean; setZone1Collapsed: (v: boolean) => void; zone2Collapsed: boolean; setZone2Collapsed: (v: boolean) => void; }`. localStorage round-trip with SSR-safe initial-state hydration pattern (defaults at server; localStorage read at `useEffect` mount).
- **`apps/web/src/hooks/useWorkspace.ts`** (new; ~30-50 LOC estimated). Hook signature: `useWorkspace(): { activeWorkspace: 'billing' | 'reports'; setActiveWorkspace: (w: 'billing' | 'reports') => void; }`. localStorage round-trip with SSR-safe pattern.
- **Test files** (new; counts under §5 below):
  - `apps/web/src/hooks/__tests__/useShellState.test.tsx`
  - `apps/web/src/hooks/__tests__/useWorkspace.test.tsx`
  - `apps/web/src/components/bridge/__tests__/Zone1ConsolidatedPanel.test.tsx`
  - `apps/web/src/components/bridge/__tests__/SplitScreenLayout.test.tsx` (extend existing test surface if present; add three-zone-shell + collapse-integration coverage)
  - `apps/web/tests/e2e/phase-6-5-shell-consolidation.spec.ts` (new E2E spec; golden-path navigation + workspace-switch + collapse/expand + localStorage persistence)

### Files deleted

- **`apps/web/src/components/bridge/MainframeRail.tsx`** (98 lines). Deleted at chunk 1. Mainframe rail's 14-icon enumeration distributes to:
  - 8 → Region 7.2 Billing workspace nav items (`open_bills`, `ap_aging`, `vendor_balance`, `payment_approval_queue`, `pending_approvals`, `active_payments`, `paid_bills_history`, `bill_form` as primary action button).
  - 2 → Region 7.2 Reports workspace nav items (`pl`, `trial_balance`). Balance Sheet (3rd Reports item per Sub-Q7.2.a) wires *new* nav surface for existing `report_balance_sheet` directive (additive; no substrate change).
  - 4 → Region 7.3 foundational footer (`coa`, `journals`, `recurring`, `actions` — Chart of Accounts + Journal Entries + Recurring Journals + AI Action Review).

### Files unchanged

- **`apps/web/src/components/bridge/ContextualCanvas.tsx`** (239 lines) — Cut 6 don't-touch-canvas at chunk 1. Chunk 2 lifts navigation history.
- **`apps/web/src/components/canvas/DocumentIntakeRail.tsx`** (311 lines) — component file stays; mount detached at SplitScreenLayout. Chunk 3 deletes file.
- **`apps/web/src/shared/types/canvasDirective.ts`** (64 lines; 38 directive members) — chunk 3 extends with `'pending_documents'`. `report_balance_sheet` directive already present (line 16); Reports workspace wires existing directive for the first time.
- **Service layer** — no service-layer touches. Cycle Round 6 Surface D confirmed Two Laws all N/A (chunk 1 UI-only).
- **`canvasContext.ts` / `canvasContextSuffix.ts`** — Sub-Q18.α active-tab-binding is chunk 2 scope; chunk 1 preserves singleton-shape unchanged.

## 4. Tasks

Strict-sequence: Task 1 → Task 2 → Task 3 → (Task 4 + Task 5 + Task 6 parallel-eligible) → Task 7. See §6 for parallelization options.

### Task 1 — Substrate state hooks

Create `useShellState` + `useWorkspace` hooks under new `apps/web/src/hooks/` directory. Hooks own localStorage round-tripping with SSR-safe hydration (defaults at server; localStorage read at `useEffect` mount; subsequent state mutations write through to localStorage synchronously).

**Files:**

- Create: `apps/web/src/hooks/useShellState.ts`
- Create: `apps/web/src/hooks/useWorkspace.ts`
- Create: `apps/web/src/hooks/__tests__/useShellState.test.tsx`
- Create: `apps/web/src/hooks/__tests__/useWorkspace.test.tsx`

**localStorage key shape (carry-forward §7.1 operationalization):**

- `chounting:shell:zone1Collapsed` — `'true'` | `'false'` (default `'false'` = expanded)
- `chounting:shell:zone2Collapsed` — `'true'` | `'false'` (default `'false'` = expanded)
- `chounting:shell:activeWorkspace` — `'billing'` | `'reports'` (default `'billing'`)

Namespace `chounting:shell:*` confirmed uncontested by grep on `apps/web/src/` at Session 5 brief-drafting (no existing `localStorage.*` calls in `apps/web/src/`). Session 7 onset re-runs the grep to confirm before implementation.

**Acceptance criteria:**

- Hooks export functions matching signatures above; both consume zero arguments.
- localStorage write fires on every `setX(...)` call; localStorage read fires once at hook mount (in `useEffect`); intervening renders read from React state (not localStorage).
- SSR-safe: hook does not access `window` / `localStorage` during initial server render; uses default values at server; hydrates to localStorage value at client `useEffect` mount.
- Unit tests cover: default values; localStorage write round-trip; localStorage read at mount; malformed localStorage value falls back to default (don't crash); two hook instances render the same state per-key (React state is stored at hook grain; consumer components react via state — no cross-tab sync at v1).

**Dependency:** none; Task 1 fires first.

**Parallelizable:** `useShellState` + `useWorkspace` are independent files with no shared types; can split across two subagents if Session 7 chooses parallel dispatch.

### Task 2 — Zone 1 consolidated panel component

Create `Zone1ConsolidatedPanel.tsx` rendering the four-region layout. Inline all four regions in one file (no per-region subcomponent files unless one region exceeds ~80 LOC during impl, in which case Session 7 escalates to file split at implementation grain).

**Files:**

- Create: `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx`
- Create: `apps/web/src/components/bridge/__tests__/Zone1ConsolidatedPanel.test.tsx`

**Component signature:**

```typescript
interface Props {
  orgId: string;
  onNavigate: CanvasNavigateFn;
}

export function Zone1ConsolidatedPanel({ orgId, onNavigate }: Props) { ... }
```

Internal state: consumes `useShellState` + `useWorkspace`. Renders either expanded mode (~280-320px wide) or collapsed rail-mode (64px wide, icons-only) based on `zone1Collapsed`.

**Region 7.1 — Workspace tabs (carry-forward §7.1 operationalization):**

- Vertical sidebar list per Sub-Q7.1.b.β.
- Two tabs at v1: **Billing** (module icon `\u{1F4BC}` 💼) and **Reports** (module icon `\u{1F4CA}` 📊).
- Each tab: module icon (24px size; left-aligned) + label text (14px font-semibold; framing 9 bolder-weight refinement for workspace items vs nav items).
- Active-state highlight: 3px left-border accent (`border-l-3 border-neutral-900`) + background tint (`bg-neutral-100`).
- Inactive tab: no border accent; background `bg-white`; hover state `hover:bg-neutral-50`.
- Tab height: 48px (vertical-rhythm-anchor for Region 7.1 grain; see framing 9 §7 below).

**Region 7.2 — Workspace-scoped navigation items (carry-forward §7.1 operationalization):**

- Renders different nav-item list based on `activeWorkspace`.
- **Billing workspace nav** (8 items at chunk 1 close; chunk 3 adds Pending Documents → 9 items):
  - `[+ New Bill]` primary action button at top of Region 7.2 per Sub-Q7.2.a (α). Visual: button-styled (filled background `bg-neutral-900 text-white`; rounded; full-width; "+" prefix). Fires `onNavigate({ type: 'bill_form', orgId })`.
  - Below the primary action button, regular nav-item list (workflow ordering):
    1. Open Bills → `report_open_bills`
    2. AP Aging → `report_ap_aging`
    3. Vendor Balance → `report_vendor_balance`
    4. Payment Approval Queue → `report_payment_approval_queue`
    5. Pending Approvals → `report_pending_approvals`
    6. Active Payments → `report_active_payments`
    7. Paid Bills History → `report_paid_bills_history`
  - Workflow ordering rationale: create-then-view (New Bill at top); view-current-then-aging (Open Bills → AP Aging); vendor view (Vendor Balance); approval workflow (Payment Approval Queue → Pending Approvals); execution state (Active Payments → Paid Bills History).
- **Reports workspace nav** (3 items; locked per Sub-Q7.2.a):
  1. P&L → `report_pl`
  2. Trial Balance → `report_trial_balance`
  3. Balance Sheet → `report_balance_sheet` (new nav surface for existing directive; chunk 1 wires for the first time)
  - Reports ordering rationale: financial-statement reading order (income statement → trial balance → position statement).
- **Account drill-down stays under CoA** per Sub-Q7.2.a (γ) — `report_account_ledger` + `report_accounts_by_type` are NOT first-class Region 7.2 entries; reachable only via CoA navigation (Region 7.3 footer entry).
- Nav-item visual treatment per framing 9: entity-shape icons (16px); label text (13px font-normal — smaller font weight than Region 7.1 workspace tabs per Sub-Q7.1.b chunk-1-brief refinement); 36px row height; hover state `hover:bg-neutral-50`.

**Region 7.3 — Persistent foundational footer (carry-forward §7.1 operationalization):**

- 4 items per Sub-Q7.3.β: Chart of Accounts + Journal Entries + Recurring Journals + AI Action Review.
- Always visible across all workspaces (cross-workspace; not affected by `activeWorkspace`).
- Visual treatment per framing 9: separator line above (`border-t border-neutral-200`); slightly smaller icon size than Region 7.2 (14px vs 16px); 32px row height; same hover state.
- Click behavior:
  - CoA → `onNavigate({ type: 'chart_of_accounts', orgId })`
  - Journal Entries → `onNavigate({ type: 'journal_entry_list', orgId })`
  - Recurring Journals → `onNavigate({ type: 'recurring_template_list', orgId })`
  - AI Action Review → preserves MainframeRail current behavior — `router.push(\`/${locale}/${orgId}/agent/actions\`)` (NOT a canvas directive; routes to full-page surface).

**Region 7.4 — Hidden with structural reservation per Sub-Q7.4.α′ (carry-forward §7.1 operationalization):**

- Renders empty `<div>` with `data-region="7.4"` attribute and inline `style={{ display: 'none' }}`.
- Zero-render at v1; structural reservation acknowledges the slot's existence in the Zone 1 grid for post-v1 multi-session-chat activation per ADR-0010 substrate-now-enforcement-later.
- No props consumed; no state; no event handlers.

**Collapsed mode (carry-forward §7.1 operationalization):**

- When `zone1Collapsed === true`, render compact 64px-wide rail showing icon-only versions of all Region 7.2 + 7.3 entries (no Region 7.1 workspace tabs; no labels; tooltips on hover).
- 64px width matches existing MainframeRail `w-16` per Sub-Q8.b.α Zone 1 lock (architectural continuity).
- Region 7.1 workspace switching is hidden in collapsed mode (user must expand to switch workspaces); Region 7.2 + 7.3 nav items remain clickable.
- Region 7.4 stays hidden (display: none).
- Expand affordance: click-target on the rail (or keyboard shortcut per Task 6).

**Acceptance criteria:**

- Component renders all four regions structurally when `zone1Collapsed === false`.
- Region 7.1 renders 2 workspace tabs with active-state highlight following `activeWorkspace`.
- Region 7.2 renders correct nav-item list per `activeWorkspace` (Billing = 8 items including primary action button; Reports = 3 items).
- Region 7.3 renders 4 footer items unchanged across workspace switches.
- Region 7.4 zero-render at v1 with `data-region="7.4"` attribute present in DOM (structural reservation evidence).
- Each nav-item click fires `onNavigate(...)` with the correct directive per the mapping above.
- AI Action Review click fires `router.push(...)` (not `onNavigate`) preserving MainframeRail behavior.
- Collapsed mode renders 64px-wide rail with icons only; click on rail expands.
- Unit tests cover: Region 7.1 active-state highlight; Region 7.2 workspace-scoped switching; Region 7.3 cross-workspace stability; Region 7.4 zero-render with attribute present; collapsed-mode structural shape; nav-item click → `onNavigate` dispatch.

**Dependency:** Task 1 (consumes `useShellState` + `useWorkspace`).

**Parallelizable:** within Task 2, Region 7.1 + 7.2 + 7.3 + 7.4 can split across subagents if Session 7 chooses parallel dispatch. Recommendation: single-subagent unless implementation overshoots ~350 LOC.

### Task 3 — SplitScreenLayout three-zone consolidation

Rework `SplitScreenLayout.tsx` to render three zones. Remove MainframeRail mount + DocumentIntakeRail mount; insert Zone1ConsolidatedPanel mount; preserve Zone 2 (AgentChatPanel) + Zone 3 (ContextualCanvas) + top nav strip (OrgSwitcher + AvatarDropdown).

**Files:**

- Modify: `apps/web/src/components/bridge/SplitScreenLayout.tsx`
- Delete: `apps/web/src/components/bridge/MainframeRail.tsx`
- Extend or create: `apps/web/src/components/bridge/__tests__/SplitScreenLayout.test.tsx`

**Component changes:**

- Remove imports: `MainframeRail`, `DocumentIntakeRail`.
- Add imports: `Zone1ConsolidatedPanel`, `useShellState`.
- Replace local `chatCollapsed` state (line 61) with `useShellState` hook consumption (`zone1Collapsed`, `zone2Collapsed`).
- Replace `<MainframeRail orgId={orgId} onNavigate={handleCanvasNavigate} />` (lines 116-119) with `<Zone1ConsolidatedPanel orgId={orgId} onNavigate={handleCanvasNavigate} />`.
- Remove `<DocumentIntakeRail orgId={orgId} />` mount (line 141). Comment in file-top updates to reflect three-zone post-Phase-6.5 shape (CLAUDE.md file-top-comment-staleness-review discipline).
- Pass `onCollapse` Prop to AgentChatPanel; on collapse, `setZone2Collapsed(true)`. When `zone2Collapsed === true`, render compact 44px rail-mode (Task 4).
- Add expand affordance for collapsed Zone 2 (click on collapsed rail → `setZone2Collapsed(false)`).

**File-top comment update (CLAUDE.md file-top-comment-staleness-review discipline):**

Current comment (lines 1-31) describes four zones. Rewrite to describe three zones post-Phase-6.5:

```
// src/components/bridge/SplitScreenLayout.tsx
// The Bridge shell. Three zones (post-Phase-6.5 chunk 1):
//   1. Zone 1 — Consolidated left panel (Zone1ConsolidatedPanel;
//      ~280-320px expanded, 64px collapsed rail-mode). Four
//      regions: workspace tabs (Billing | Reports) + workspace-
//      scoped nav + persistent foundational footer + hidden v1
//      structural reservation per Sub-Q7.4.α′ / ADR-0010.
//   2. Zone 2 — Agent chat panel (~380px expanded, 44px collapsed
//      rail-mode with new-output badge).
//   3. Zone 3 — Contextual canvas (fills remaining width).
//
// Collapse state for both zones persists via useShellState
// localStorage keys (chounting:shell:zone1Collapsed +
// chounting:shell:zone2Collapsed). Workspace selection persists
// via useWorkspace (chounting:shell:activeWorkspace).
//
// Pre-Phase-6.5 four-zone shape (Mainframe rail + chat + canvas +
// DocumentIntakeRail) preserved in ui_architecture.md § Shell
// architecture history (pre-Phase-6.5) per ADR-0022 §2.
```

Preserve existing comment paragraphs on `canvasContext` state lift (Session 7.1 Commit 5) and memberships query (Session 8 Commit 1) — those concerns are unchanged at chunk 1.

**Acceptance criteria:**

- Three zones render in the order Zone 1 → Zone 2 → Zone 3 (left to right).
- MainframeRail.tsx is absent from the repository at chunk 1 commit (file deleted).
- DocumentIntakeRail mount is absent from SplitScreenLayout (component file remains on disk per Sub-Q4).
- Top nav strip (OrgSwitcher + AvatarDropdown) renders unchanged.
- `directive`, `selectedEntity`, `currentUserRole` state preserved; `handleCanvasNavigate` + `handleSelectEntity` callbacks preserved; `canvasContext` assembly preserved.
- Zone 1 collapse fires when `zone1Collapsed === true` (Zone1ConsolidatedPanel renders compact rail).
- Zone 2 collapse fires when `zone2Collapsed === true` (AgentChatPanel renders compact rail; Task 4).
- File-top comment updated to three-zone description.
- Unit tests cover: three-zone render shape; Zone 1 collapse trigger; Zone 2 collapse trigger; canvasContext assembly preserved.

**Dependency:** Task 1 (consumes `useShellState`); Task 2 (mounts `Zone1ConsolidatedPanel`).

**Parallelizable:** single-file; not parallelizable. Multiple subagent dispatches would conflict at the file grain.

### Task 4 — Zone 2 AgentChatPanel collapsed-state + new-output badge

Extend `AgentChatPanel.tsx` with collapsed-state rendering. When parent `SplitScreenLayout` indicates collapsed via Prop (passed from `useShellState`), AgentChatPanel renders a 44px-wide rail-mode showing the new-output badge.

**Files:**

- Modify: `apps/web/src/components/bridge/AgentChatPanel.tsx`
- Test: extend existing AgentChatPanel test surface OR add `apps/web/src/components/bridge/__tests__/AgentChatPanel.collapsed.test.tsx` (small file for collapsed-state coverage; preserves existing test isolation).

**Prop signature extension:**

Add a new optional Prop `collapsed?: boolean` to AgentChatPanel Props interface (alongside existing `onCollapse?: () => void`). When `collapsed === true`, render compact rail-mode instead of the full panel.

```typescript
interface Props {
  // ... existing Props ...
  /**
   * Phase 6.5 chunk 1: when true, render the collapsed 44px rail-mode
   * with new-output badge instead of the full chat panel. Parent
   * SplitScreenLayout flows this from useShellState.zone2Collapsed.
   */
  collapsed?: boolean;
}
```

**Collapsed-mode visual (carry-forward §7.1 operationalization):**

- 44px-wide rail (`w-11`).
- New-output badge: 16x16 circular badge with white background + neutral-800 border + count of new assistant messages received since last expand. Positioned at top-right of the collapsed rail (~8px from top, ~8px from right).
- Badge color: neutral default; semantic-red variant if any message arrived with error state (preserves agent-error visibility through collapse).
- Badge count text: 12px font-bold; centered in badge; supports 1-99 (caps at "99+" for ≥100).
- Animation: subtle pulse on new message arrival (CSS `transform: scale(1) → scale(1.1) → scale(1)` over 300ms; CSS animation `bridge-badge-pulse`).
- Counter resets to 0 on expand (when `collapsed` transitions from `true` → `false`).
- Click on the rail expands (calls a new `onExpand?: () => void` Prop counterpart to `onCollapse`).

**Acceptance criteria:**

- When `collapsed === true`, AgentChatPanel renders 44px-wide rail-mode (not the full panel).
- Badge displays correct count of unread messages since last expand.
- Badge animates pulse on new message arrival.
- Counter resets to 0 when `collapsed` transitions `true` → `false`.
- Click on rail fires `onExpand` callback (or equivalent — Session 7 may choose a different Prop name; brief is non-prescriptive on the exact callback shape).
- When `collapsed === false`, AgentChatPanel renders the existing full panel (no behavioral change to existing send / persona / canvasContext / ProposedEntryCard wiring).
- `onCollapse` Prop signature unchanged (preserves existing parent wiring).
- Unit tests cover: collapsed-mode render at 44px; badge count rendering; badge pulse animation trigger on new message; counter reset on expand; click-to-expand callback.

**Dependency:** Task 1 (`useShellState` provides `zone2Collapsed`); Task 3 (SplitScreenLayout flows `collapsed` Prop down).

**Parallelizable:** independent of Tasks 5 + 6; can fire in parallel after Task 3 lands.

### Task 5 — Workspace switch logic

Wire workspace tabs (Region 7.1) to switch active workspace; ensure Region 7.2 re-renders correct nav-item list per active workspace.

**Files:**

- Modify: `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx` (wires Region 7.1 click handlers to `setActiveWorkspace` from `useWorkspace`).
- Test: extend `apps/web/src/components/bridge/__tests__/Zone1ConsolidatedPanel.test.tsx` with workspace-switch coverage.

**Behavior:**

- Click on Billing workspace tab → `setActiveWorkspace('billing')` → Region 7.2 re-renders Billing 8-item nav list; Region 7.1 active-state highlight moves to Billing tab.
- Click on Reports workspace tab → `setActiveWorkspace('reports')` → Region 7.2 re-renders Reports 3-item nav list; Region 7.1 active-state highlight moves to Reports tab.
- localStorage persists active workspace across reload (`chounting:shell:activeWorkspace` key).
- Default active workspace at first-time-user (empty localStorage): `'billing'` per cycle Round 6 implicit default (Billing is the v1 primary product surface).
- Region 7.3 footer + Region 7.4 reservation unaffected by workspace switch.

**Acceptance criteria:**

- Workspace tab click switches `activeWorkspace` state via `setActiveWorkspace`.
- Region 7.2 nav-item list updates to match `activeWorkspace`.
- Region 7.1 active-state highlight moves to clicked tab.
- localStorage value at `chounting:shell:activeWorkspace` updates on switch.
- Reload preserves last-active workspace (localStorage read at hook mount).
- First-time user sees Billing workspace active (default).
- Unit tests cover: Billing → Reports switch; Reports → Billing switch; localStorage persistence; default first-time workspace.

**Dependency:** Task 1 (`useWorkspace` hook); Task 2 (Zone1ConsolidatedPanel renders Region 7.1 tabs).

**Parallelizable:** not independently; integrates into Task 2's Region 7.1 + 7.2 components. Sessions 7 may fold Task 5 into Task 2's subagent dispatch.

### Task 6 — Keyboard shortcut wiring

Wire keyboard shortcuts for Zone 1 + Zone 2 collapse toggles per Sub-Q8.a.α (both keyboard + button).

**Files:**

- Modify: `apps/web/src/components/bridge/SplitScreenLayout.tsx` (mount keyboard handler at shell grain).
- Test: extend `apps/web/src/components/bridge/__tests__/SplitScreenLayout.test.tsx` with keyboard-shortcut coverage.

**Keyboard bindings (carry-forward §7.1 operationalization):**

- **Zone 1 toggle:** `Cmd+\` (macOS) / `Ctrl+\` (Windows + Linux). Toggles `zone1Collapsed`.
- **Zone 2 toggle:** `Cmd+Shift+\` (macOS) / `Ctrl+Shift+\` (Windows + Linux). Toggles `zone2Collapsed`.

**Conflict-check evidence (Session 5 brief-drafting; Session 7 onset re-runs):**

- Grep on `apps/web/src/` for `metaKey | ctrlKey | onKeyDown | addEventListener\('keydown'` returned empty at Session 5 (no existing keyboard handlers in source tree).
- Browser default: Cmd+\\ / Ctrl+\\ is unassigned in major browsers (Chrome / Safari / Firefox / Edge); Cmd+Shift+\\ likewise unassigned.
- Command palette (`Cmd+K` per `ui_architecture.md` Path 3) is documented as Phase 1.2+ but NOT implemented at code grain per the empty-grep — when it lands, it uses `K` not `\\`.
- Fallback bindings if Session 7 onset re-grep surfaces a conflict: `Cmd+B` / `Ctrl+B` for Zone 1; `Cmd+Shift+B` / `Ctrl+Shift+B` for Zone 2 (`B` is similarly unassigned at browser-default grain).

**Implementation pattern:**

- Mount a global `keydown` event listener at SplitScreenLayout grain (within a `useEffect` with empty deps; clean up on unmount).
- Listener checks `event.metaKey` (macOS) OR `event.ctrlKey` (non-macOS) AND `event.key === '\\'` AND `!event.shiftKey` for Zone 1; same condition with `event.shiftKey === true` for Zone 2.
- Call `event.preventDefault()` to swallow browser default (defensive — browser default is unassigned but Cmd+Shift+\\ may map in some keyboard layouts).
- Dispatch `setZone1Collapsed(!zone1Collapsed)` or `setZone2Collapsed(!zone2Collapsed)`.

**Acceptance criteria:**

- Cmd+\\ (or Ctrl+\\) toggles `zone1Collapsed` state.
- Cmd+Shift+\\ (or Ctrl+Shift+\\) toggles `zone2Collapsed` state.
- Both shortcuts work cross-platform (metaKey on macOS; ctrlKey on Windows + Linux).
- No conflicts with existing app shortcuts (Session 7 onset re-grep confirms).
- Keyboard event handler unmounts cleanly on SplitScreenLayout unmount.
- Unit tests cover: Cmd+\\ toggle dispatch; Cmd+Shift+\\ toggle dispatch; Shift+\\ alone (no meta) NO-OP; clean unmount.

**Dependency:** Task 1 (`useShellState`); Task 3 (SplitScreenLayout consumes hook).

**Parallelizable:** integrates into Task 3's SplitScreenLayout rework; Session 7 may fold Task 6 into Task 3's subagent dispatch.

### Task 7 — Cross-task verification + chunk-close ceremony

Final integration check + validation gates + screenshot gate + commit ceremony.

**Files:**

- Create: `apps/web/tests/e2e/phase-6-5-shell-consolidation.spec.ts` (new E2E spec; see §5 for scope).
- Update file-top comments on all chunk-1-touched files (CLAUDE.md file-top-comment-staleness-review).

**Pre-commit validation gates:**

- `pnpm test` — expected 1114 (baseline) + new unit tests = ~1129-1135 (revised count depends on test grain; Surface C target ~10-15 new unit tests).
- `pnpm agent:validate` — expected 26/26 baseline preserved (no service-layer or schema touches; floor tests unaffected).
- `pnpm typecheck` — expected green (`tsc --noEmit` no errors).
- `pnpm test:e2e` — expected existing 12 specs green + 1 new chunk-1 spec green (or N new specs per §5).

**Screenshot gate per CLAUDE.md UI-session-screenshot-gate (chunk 1 ships UI; gate fires):**

Five-shot prescribed sequence against fresh `pnpm db:reset:clean && pnpm db:seed:all` state:

1. **Three-zone shell — default state.** Billing workspace active; Zone 1 expanded; Zone 2 expanded; Zone 3 showing `{ type: 'none' }` neutral state. Verify: Region 7.1 shows 2 workspace tabs (Billing active); Region 7.2 shows 8 Billing nav items (with New Bill primary action button at top); Region 7.3 shows 4 footer items; Region 7.4 zero-render (DOM attribute present but `display: none`).
2. **Zone 1 collapsed — 64px rail-mode.** Click Zone 1 collapse trigger (or fire Cmd+\\); rail shows icons-only at 64px width. Verify: width = 64px (`w-16`); Region 7.1 hidden; Region 7.2 + 7.3 icons visible; Region 7.4 still hidden.
3. **Zone 2 collapsed — 44px rail-mode with new-output badge.** Send a message in chat; verify assistant reply arrives; trigger Zone 2 collapse (Cmd+Shift+\\); badge shows "1" with pulse animation. Verify: width = 44px (`w-11`); badge positioned top-right; count "1".
4. **Workspace switched — Reports active.** Click Reports workspace tab; verify Region 7.2 re-renders to Reports 3-item list (P&L + Trial Balance + Balance Sheet); Region 7.1 active-state highlight on Reports tab; Region 7.3 footer unchanged.
5. **Region 7.3 footer rendered + AI Action Review click.** Click AI Action Review footer item; verify navigation to `/[locale]/[orgId]/agent/actions` (full-page route, NOT a canvas directive); verify behavior preserved from MainframeRail.

Screenshot capture happens at Session 7 founder-review grain post-implementation; orchestrator (Session 7 WSL-side) drafts the prescribed sequence at scope-lock; founder captures + orchestrator spot-checks per CLAUDE.md UI-session-screenshot-gate Steps 1-4.

**Commit shape:**

Single commit on staging per Round 6 Surface B HIGH reversibility:

```
feat(bridge): Phase 6.5 chunk 1 — Bridge shell consolidation + Zone 1 + workspace switches

[3-5 paragraphs covering: chunk 1 scope; per-region implementations; collapse behavior + keyboard shortcuts; workspace switch + localStorage persistence; rollback posture]

Cross-references:
- Cycle closeout brief: docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md (79a6ceb)
- v3 proposal: docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md (b7cb081)
- Amended ui_architecture.md (9e606f6)
- Chunk 1 brief: docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md (this brief's commit SHA — captured at Session 7)
```

Single-commit shape valid per Surface B HIGH reversibility (no multi-step rollback complexity; revert produces clean pre-chunk-1 four-zone shell).

**Acceptance criteria:**

- All four validation gates green.
- Screenshot gate passes (founder ratifies all 5 shots).
- Commit lands on staging at Session 7 close.
- Push readiness three-condition gate met: test-suite health (Condition 1); doc-sync (Condition 2: brief + cycle brief + ui_architecture.md consistent — already locked at Sessions 1-4); governance closeout (Condition 3: chunk-1-close friction-journal entry if any deviations surfaced; no retrospective at chunk close — Phase 6.5 retrospective is per Sub-Q6.d).

**Dependency:** Tasks 1-6 complete.

**Parallelizable:** terminal task; not parallelizable.

## 5. Cross-task verification + integration testing

### Unit test scope

Target: ~10-15 new unit tests per cycle Round 6 Surface C. Distribution:

- **`useShellState.test.tsx`** (~3-4 tests): default values; localStorage write round-trip; localStorage read at mount; malformed-value fallback.
- **`useWorkspace.test.tsx`** (~3 tests): default value; round-trip; reload persistence.
- **`Zone1ConsolidatedPanel.test.tsx`** (~5-6 tests): four-region structural shape; Region 7.1 active-state highlight; Region 7.2 workspace-scoped switching; Region 7.3 cross-workspace stability; Region 7.4 zero-render with attribute; collapsed-mode rail shape.
- **`SplitScreenLayout.test.tsx`** (~2-3 tests; extension): three-zone render; collapse triggers (Zone 1 + Zone 2); canvasContext assembly preserved.
- **`AgentChatPanel.collapsed.test.tsx`** OR inline extension (~2-3 tests): collapsed-mode render at 44px; badge count rendering; counter reset on expand.

**Note on vitest config gap:** Per `[[project_phase_6_chunk_2b_implementation_notes]]` MEMORY entry, current vitest config lacks React DOM environment (jsdom/@testing-library/react); chunk 6.2b's UI unit tests were SKIPPED for this reason and the gap is banked as a Phase 6 retrospective candidate. **Chunk 1 must verify whether the vitest config gap has been closed since chunk 6.2b** at Session 7 onset (re-verify-from-disk on `vitest.config.ts` + relevant test-infrastructure files). If the gap persists, chunk 1's UI unit tests fall under the same SKIP regime; the integration + E2E layer carries verification weight instead. The brief is non-prescriptive on this — Session 7 adjudicates at impl-onset per disk state.

### E2E test scope

Target: ~3-5 new E2E spec scenarios per cycle Round 6 Surface C. Single new spec file `apps/web/tests/e2e/phase-6-5-shell-consolidation.spec.ts` containing:

- **Scenario 1 — Golden-path navigation.** Sign-in → land on `/[locale]/[orgId]/` → verify Zone 1 expanded with Billing workspace active → click Open Bills → verify `report_open_bills` canvas renders → click Reports workspace tab → verify Region 7.2 switches to Reports → click P&L → verify `report_pl` canvas renders.
- **Scenario 2 — Zone 1 collapse + expand via button.** Click Zone 1 collapse button → verify 64px rail-mode rendered → click rail → verify expanded mode restored.
- **Scenario 3 — Zone 2 collapse + expand via keyboard.** Fire Cmd+Shift+\\ → verify Zone 2 rail-mode rendered at 44px → fire Cmd+Shift+\\ again → verify expanded restored.
- **Scenario 4 — localStorage persistence across reload.** Set Zone 1 collapsed + active workspace = Reports → reload page → verify Zone 1 still collapsed + Reports still active.
- **Scenario 5 — Region 7.3 footer cross-workspace stability.** From Billing workspace, verify 4 footer items visible → switch to Reports → verify same 4 footer items still visible.

### Existing E2E specs preserved

All 12 existing E2E specs (per cycle Round 1 verify-from-disk count) must remain green at chunk 1 close. Reachability check: every canvas-view test fixture that previously navigated via MainframeRail now navigates via Zone 1 Region 7.2 (Billing) or 7.3 (foundational footer). Test-fixture URL paths unchanged; the navigation surface name changes but the resulting canvas directive is identical.

**Sentinel risk:** if any existing E2E spec hardcoded `MainframeRail` selectors (e.g., `data-testid="mainframe-rail"` or class queries on `nav.w-16`), it breaks at chunk 1. Session 7 onset must grep existing tests for `MainframeRail | mainframe-rail | mainframe` and update selectors to `Zone1ConsolidatedPanel | zone1-consolidated-panel | zone-1` equivalents. The grep + fix-up sits within Task 7 cross-task verification, not as a separate task.

### Surface D — Two Laws verification

All Two Laws (INV-SERVICE-001, INV-SERVICE-002, INV-AUTH-001) confirmed N/A per cycle Round 6 Surface D. Rationale: chunk 1 ships React component + state + localStorage changes only; no service-layer touches; no DB writes; no RLS-sensitive surfaces. Phase 1.1 audit framework UF-001 through UF-021 also N/A for the same reason.

Session 7 implementation must NOT introduce a service-layer or DB-write code path without escalating scope back to brief-amendment.

## 6. Implementation order + parallelization

### Strict-sequence dependencies

```
Task 1 (hooks)
  ↓
Task 2 (Zone1ConsolidatedPanel)
  ↓
Task 3 (SplitScreenLayout consolidation; deletes MainframeRail)
  ↓
  ├─ Task 4 (AgentChatPanel collapsed-state) ─┐
  ├─ Task 5 (workspace switch logic)          ├─→ Task 7 (verification + commit)
  └─ Task 6 (keyboard shortcut wiring)        ─┘
```

Task 4 + Task 5 + Task 6 can run in parallel post-Task-3.

### Subagent dispatch shape (if Session 7 chooses parallel dispatch)

- **Subagent A** — Task 1: useShellState + useWorkspace (one file each; ~70-110 LOC total).
- **Subagent B** — Task 2: Zone1ConsolidatedPanel (~250-350 LOC; four regions inline).
- **Subagent C** — Task 3: SplitScreenLayout rework + MainframeRail.tsx deletion (~80-120 LOC touched; deletes 98 LOC).
- **Subagent D** — Task 4: AgentChatPanel collapsed-state + badge (~30-50 LOC added).
- **Subagent E** — Task 5: workspace switch integration into Task 2's Region 7.1 + 7.2 (folds into Subagent B if Session 7 chooses; non-trivial coupling).
- **Subagent F** — Task 6: keyboard shortcut wiring into Task 3's SplitScreenLayout (folds into Subagent C if Session 7 chooses; tight coupling to SplitScreenLayout grain).
- **Final orchestrator pass** — Task 7: cross-task verification + commit ceremony + screenshot gate.

### Alternative: sequential single-subagent dispatch

Session 7 may elect sequential single-subagent (or direct WSL-side implementation) instead of parallel subagent dispatch. Chunk 1's total LOC forecast (cycle §5.1 Surface A: ~800-1100 LOC; current task breakdown estimate sums to ~500-700 LOC modified/created) is within single-session budget per RI-7 thresholds and below chunk-3-Phase-4's empirical upper bound (8 files + 1 migration + 1 types.ts regen + 5 framings + brief amendment cycle). Single-subagent sequential delivery is operationally valid.

**Recommendation at brief-drafting grain:** sequential single-subagent OR direct WSL-side implementation. Chunk 1's substantive complexity sits in Zone1ConsolidatedPanel composition (Region 7.1 + 7.2 + 7.3 internal layout coordination); splitting Region 7.1 + 7.2 + 7.3 across subagents adds coordination overhead disproportionate to the parallelization win. Session 7 onset adjudicates.

## 7. Substrate touchpoint risks + mitigations

Three risks at chunk 1 grain. None require scope adjustment at brief-drafting; all are operationalizable at Session 7 onset.

### Risk 1 — Framing 9 visual rhythm coherence

**Risk:** Workspace tab visual treatment (Region 7.1) vs nav-item visual treatment (Region 7.2) vs footer visual treatment (Region 7.3) may surface visual discord at integration grain. Cycle framing 9 (Round 2 cross-cutting) names this as carry-forward; this brief's task-grain operationalization picks specific font sizes / icon sizes / row heights, but the visual rhythm only proves coherent when rendered together.

**Mitigation:** Task 2 implementation eyeballs all three regions side-by-side at impl grain. Specific spec applied across regions:

| Region | Font size | Font weight | Icon size | Row height |
|---|---|---|---|---|
| Region 7.1 — workspace tabs | 14px | font-semibold | 24px (module icons) | 48px |
| Region 7.2 — nav items | 13px | font-normal | 16px (entity-shape icons) | 36px |
| Region 7.3 — footer items | 13px | font-normal | 14px (entity-shape icons) | 32px |

Visual rhythm rule: progressive de-emphasis from workspace tabs (heaviest) → nav items (medium) → footer (lightest). Active-state highlight (3px left-border + bg-neutral-100) lives ONLY at Region 7.1 grain (workspace switching is structurally distinct from nav clicking — nav clicks fire one-shot navigation, not state persistence).

If Session 7 founder review surfaces visual discord post-screenshot-gate, treat as in-scope refinement at chunk 1 (not a deferred carry-forward); the rhythm spec is hypothesis at brief-drafting; founder eye is canonical at screenshot-gate.

### Risk 2 — Keyboard shortcut conflict (surfaces at Session 7 onset)

**Risk:** Cmd+\\ / Cmd+Shift+\\ may conflict with shortcuts that surface between Session 5 brief-drafting and Session 7 implementation. Session 5 grep on `apps/web/src/` returned empty for `metaKey | ctrlKey | onKeyDown | addEventListener\('keydown'`, but Session 6 (if it fires) or third-party-library updates could introduce handlers.

**Mitigation:** Session 7 onset re-runs the grep. If a conflict surfaces, fall back to Cmd+B / Cmd+Shift+B per §7.1 fallback spec. The brief is non-prescriptive on the exact final bindings — Session 7 picks the winning pair at impl-onset based on disk state; the only invariant is "both zones get keyboard + button triggers per Sub-Q8.a.α."

If both Cmd+\\ and Cmd+B surface conflicts, escalate scope back to brief-amendment (RI-10 framing if N≥3 framing-revisits surface; otherwise inline friction-journal entry). At Session 5 brief-drafting, neither conflict is observed; the fallback is defensive.

### Risk 3 — localStorage namespace collision

**Risk:** `chounting:shell:*` namespace may collide with existing localStorage usage that surfaces between Session 5 and Session 7.

**Mitigation:** Session 7 onset re-greps `apps/web/src/` for `localStorage\.`. Session 5 grep returned empty — no existing localStorage usage in source tree. Any post-Session-5 localStorage additions in unrelated codebase areas are caught by the re-grep.

If collision surfaces, namespace shifts to `chounting:shell:v1:*` (add version prefix). The brief is non-prescriptive on the exact final namespace — Session 7 picks at impl-onset; the only invariant is "single namespace prefix scoped to shell state."

### Risk 4 (defensive) — vitest config DOM-environment gap from chunk 6.2b

**Risk** (per MEMORY entry `project_phase_6_chunk_2b_implementation_notes`): chunk 6.2b's UI unit tests were SKIPPED because vitest config lacks React DOM environment. If the gap persists at Session 7, chunk 1's UI unit tests (Task 2's Zone1ConsolidatedPanel; Task 3's SplitScreenLayout; Task 4's AgentChatPanel.collapsed) fall under the same SKIP regime.

**Mitigation:** Session 7 onset verifies vitest config + relevant test-infrastructure files. If gap persists:

- UI unit tests deferred (acknowledged at chunk-1-close friction-journal entry; carries to Phase 6.5 retrospective consolidation alongside chunk 6.2b's banked Phase 6 retrospective candidate).
- Integration + E2E layer carries verification weight (5 E2E scenarios at §5 cover the substantive observable behavior at fixture grain).
- Hook unit tests (Task 1) likely unaffected — `useShellState` + `useWorkspace` test against `localStorage` mock without React DOM (they expose functions, not rendered components).

If gap is closed since chunk 6.2b, all UI unit tests fire normally; no scope adjustment.

## 8. Cross-references

### Cycle predecessors

- **Cycle closeout brief**: `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`). §5.1 chunk 1 acceptance criteria + §7.1 chunk 1 partial-information carry-forwards + §6 inter-chunk dependency map.
- **v3 proposal**: `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`). §5.1 chunk decomposition + §3 Cuts 4-8 substrate cuts.
- **Amended `ui_architecture.md`**: `docs/03_architecture/ui_architecture.md` (commit `9e606f6`). § The Split-Screen Layout (post-Phase-6.5 three-zone description); § The Three-Path Entry Model § Path 1 (Zone 1 post-Phase-6.5 description); § Shell architecture history (pre-Phase-6.5).
- **Session A preserved-evidence**: `docs/09_briefs/phase-6/2026-05-16-session-a-preserved-evidence.md` (commit `7834a26`). §A.6 AGENT panel substrate ownership inventory; §A.10 DocumentIntakeRail current state (chunk 3 territory; cited for completeness).

### ADRs

- **ADR-0010** — substrate-now-enforcement-later (Region 7.4 structural reservation discipline).
- **ADR-0020** — Source-tree authority-layer enumeration (`hooks/` directory authorization).
- **ADR-0022** §2 — Supersession workflow (DocumentIntakeRail two-step removal precedent; ui_architecture.md amendment preserves pre-Phase-6.5 description as historical record).

### Code surfaces (verified at HEAD `a46242e`)

- `apps/web/src/components/bridge/SplitScreenLayout.tsx` (145 lines pre-chunk-1; rework target).
- `apps/web/src/components/bridge/MainframeRail.tsx` (98 lines; 14-icon enumeration at lines 23-38; `w-16` at line 80; deletion target).
- `apps/web/src/components/bridge/AgentChatPanel.tsx` (788 lines; `onCollapse?: () => void` Prop at line 39; extension target).
- `apps/web/src/components/bridge/ContextualCanvas.tsx` (239 lines; unchanged at chunk 1 per Cut 6).
- `apps/web/src/components/canvas/DocumentIntakeRail.tsx` (311 lines; mount detached at chunk 1; file stays for chunk 3 deletion).
- `apps/web/src/shared/types/canvasDirective.ts` (64 lines; 38 directive members; `report_balance_sheet` already present at line 16 — Reports workspace wires new nav surface for existing directive).
- `apps/web/src/hooks/` — new directory (created at Task 1).

### Governance precedents

- **Phase 2.5** commit ceremony shape: `docs/09_briefs/phase-2.5/2026-05-13-phase-2-5-commit-a.md` + `commit-b.md`. Source for phase-N.M-hyphenated filename convention (phase-2-5).
- **F-J-θ first-instance cross-phase substrate-modification**: `docs/09_briefs/phase-4/chunks/2026-05-14-phase-4-chunk-2.md`. Session A §A.5 inheritance for cross-phase grain precedent. Chunk 1 is NOT a substrate-modification chunk (pure React state + component); F-J-θ governs chunk 2 + chunk 3 grain at most, not chunk 1.
- **Phase 4 retrospective RI-1, RI-6, RI-7, RI-10**: `docs/07_governance/retrospectives/phase-4-retrospective.md`. RI-7 (Path C invocation) — chunk 1 stays within single-session budget per §6 analysis; Path C not invoked. RI-10 (brief amendment cycle at N≥3 framings) — chunk 1 brief-drafting accepts cycle locks without re-framing.
- **Chunk-6.3a brief precedent**: `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-3a.md`. Structural shape inheritance — chunk-6.3a's sub-Q-resolution-driven shape doesn't directly apply at chunk 1 (cycle close already resolved all sub-Qs); chunk 1's task-decomposition shape inherits the per-task acceptance-criteria + carry-forward operationalization convention from the cycle Round 6 framework.

### Downstream

- **Chunk 2 brief** (Session 8): `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-2.md` (anticipated path; may split to chunk 2a + 2b per Path C if substrate density exceeds single-session budget per cycle §6 Path C risk surface).
- **Chunk 3 brief** (Session 11): `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-3.md` (anticipated path).
- **Phase 6.5 retrospective** (Session 13 or 13+1): `docs/07_governance/retrospectives/phase-6.5-retrospective.md` per Sub-Q6.d (dedicated retrospective file; diverges from Phase 2.5 absorbed-into-phase-2 shape per v3 §10 directive).

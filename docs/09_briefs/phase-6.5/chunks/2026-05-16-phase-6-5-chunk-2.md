# Phase 6.5 Chunk 2 Brief — Multi-tab canvas (Cut 9 limited at v1)

- **Date:** 2026-05-16
- **Phase:** Phase 6.5 (amendment cycle per Phase 2.5 precedent)
- **Chunk:** 2 of 3 (chunk 1 shell consolidation shipped at `5a9492b`; chunk 3 chat-drop + PendingDocumentsView pending)
- **Status:** brief-drafting (Session 8) → scope-lock or implementation prep (Session 9, may collapse per Session 6 chunk 1 precedent) → implementation + chunk-close (Session 10)
- **Session shape:** three-session Shape A per v3 §7 Steps 5-6 with operational flex.
- **Predecessors:** cycle closeout brief at `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`); v3 proposal at `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`); amended `ui_architecture.md` at `docs/03_architecture/ui_architecture.md` (commit `9e606f6`); chunk 1 brief at `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` (commit `1c65cd1`); chunk 1 implementation at commit `5a9492b`.
- **Baseline at Session 8 onset:** HEAD = `5a9492b` on staging; `pnpm test` 1125/1125 (1114 baseline + 11 chunk 1 shellStateStorage); `pnpm agent:validate` 26/26; `pnpm typecheck` green; chunk 1 substrate stable.

## 1. Preamble

Phase 6.5 chunk 2 ships the multi-tab canvas substrate per Cut 9 Path 2 (limited at v1). The cycle closeout brief at `79a6ceb` locked the substantive decisions across Sub-Q11.a/b/c/d (Pattern γ source-driven routing + state lift into SplitScreenLayout + tab UI affordance + open/close/switch semantics) + Sub-Q18 (active-tab/chat-intent binding preserving Phase 1.2 canvasContext singleton-shape) + Sub-Q19 (build-from-scratch tab component reversing v3 §4.7's 19.β Radix UI recommendation per disk-verified absence of Radix/shadcn/TanStack). This brief operationalizes those locks at implementation-task grain.

**Inheritance posture.** Cycle closeout brief is authoritative for *what* chunk 2 ships (Cut 9 Pattern γ routing rules + Sub-Q11.b state lift shape + Sub-Q11.c tab strip browser-tab shape + Sub-Q11.d open/close/switch semantics + Sub-Q18.α active-tab binding + Sub-Q19.α build-from-scratch). This brief is authoritative for *how* chunk 2 builds it (file decompositions; Tab data model shape; CanvasTabStrip visual treatment; Pattern γ pure-function dispatch; EC1.β/EC2.β/EC3.β edge-case implementations; tab title derivation; test infrastructure additions). Cycle brief wins on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

**Cross-references.**

- Cycle closeout brief at `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`) — §5.2 chunk 2 acceptance criteria + §7.2 chunk 2 partial-information carry-forwards + §6 inter-chunk dependency map.
- v3 proposal at `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`) — §5.1 chunk 2 decomposition + §3 Cut 9.
- Amended `ui_architecture.md` (commit `9e606f6`) — § The `canvas_directive` Contract amendment (singleton-shape preserved at TYPE level; per-tab state lift at runtime); § Canvas ↔ Chat State Model Amendment 2026-05-16 (per-tab navigation history; bookmark pill Pattern γ routing rule).
- Chunk 1 brief (commit `1c65cd1`) + chunk 1 implementation (commit `5a9492b`) — substrate surfaces chunk 2 consumes: `SplitScreenLayout` post-chunk-1 three-zone state; `useShellState` + `useWorkspace` hooks; `Zone1ConsolidatedPanel` Pattern γ routing source; `AgentChatPanel` collapsed-state preserved; `apps/web/src/hooks/` + `apps/web/src/shared/storage/` sibling directories established.
- Memory pointers (post-chunk-1 carry-forwards):
  - `project_phase_6_5_chunk_1_billing_nav_ordering.md` — Billing nav-item canonical order; chunk 3 inserts Pending Documents at workflow slot.
  - `project_phase_6_5_full_page_routes_layout.md` — three-zone shell vs full-page route layout distinction; **load-bearing for chunk 2 scope** (tab strip is Zone 3 property; doesn't render on full-page routes; multi-tab routing does NOT extend to full-page routes like `/[locale]/[orgId]/agent/actions`).
- ADR-0010 — substrate-now-enforcement-later (fourth UI-layer-grain application at the Props-API grain for `onDropEvent` additive Prop; see §2 Props-API distinction).
- Phase 6 chunk-6.2a + 6.2b briefs at `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-2a.md` + `chunk-2b.md` — Path C split precedent (substrate-vs-feature fault line; brief-drafting-grain split shape inheritance if Option C invokes; see §1.1).
- Phase 4 chunk 3 brief at `docs/09_briefs/phase-4/chunks/2026-05-13-phase-4-chunk-3.md` + amended brief at `c76d264` — Path C split precedent (dispatcher-isolated; reactive-at-impl session-split shape if Option B invokes at Session 10).

### 1.1 Path C invocation evaluation

Per RI-7 (Phase 4 retrospective codification) + cycle closeout brief §6 Path C risk surface. Cycle pre-disposes Path-C-positive based on greenfield substrate + substantively-novel-logic scope across multiple surfaces. Brief drafter ratifies the third criterion (volume estimate + framing-revisits realized state) against disk evidence at Session 8 brief-draft grain.

**Criterion 1 — Volume estimate.**

Brief-draft re-evaluation against disk (chunk 1 post-implementation state):

- ContextualCanvas refactor: lines 67-93 (internal history state + useEffect sync + goBack/goForward) total ~28 LOC removed + ~35 LOC added for Props-driven render = ~+7 LOC net.
- SplitScreenLayout state lift: ~100-150 LOC added (tabs array data model + 4 per-source callback handlers + Pattern γ dispatch hookups; SplitScreenLayout currently 159 lines post-chunk-1).
- New `CanvasTabStrip.tsx`: ~250-350 LOC (build-from-scratch per Sub-Q19.α; no Radix/shadcn dependency).
- Pattern γ pure routing functions (new `canvasTabRouting.ts`): ~80-150 LOC (4 rules + 3 edge cases + matcher functions).
- canvasContext active-tab binding: ~15-25 LOC at SplitScreenLayout (derives canvasContext from `tabs[activeTabId]` instead of singleton state).
- Tab data model type (new `canvasTab.ts`): ~30-50 LOC.
- Tab title derivation (new `tabTitle.ts`): ~80-120 LOC (per-directive-type → human-shorthand label).
- Tests: ~500-700 LOC (12-18 unit + 5-8 E2E + new fixtures + helpers).
- File-top comment updates per CLAUDE.md staleness review: small (~20-40 LOC across touched files).

**Total estimate: ~1000-1500 LOC.** Sits at the HIGH end of cycle forecast (1000-1300 per v3 §5.1) but below chunk-6.2b empirical reference (2335 LOC) and below chunk-3-Phase-4 RI-7 empirical single-session ceiling (~2000 LOC; 8 files + 1 migration + 1 types.ts regen + 5 framings + brief amendment cycle). Single-session implementation budget feasible.

**Criterion 2 — Framing-revisits at brief-draft.**

Cycle closeout brief locked all chunk-2-relevant sub-Qs (Sub-Q11.a/b/c/d/e + Sub-Q18 + Sub-Q19). Verify-from-disk at Session 8 onset (this brief) confirms:
- SplitScreenLayout post-chunk-1 state (159 lines; `useShellState` + `useWorkspace` integration; `handleCanvasNavigate` callback pattern) ready for state lift.
- ContextualCanvas (239 lines) internal history state at lines 67-93 ready for lift.
- canvasContext.ts singleton-shape preserved per Sub-Q18.α target.
- canvasContext.schema.ts + canvasContextSuffix.ts zero-touch confirmed.
- agent_interface.md (246 lines) is pure behavioral/persona document; NO component-library direction surfaced (grep on `shadcn|radix|tanstack|component library` returned zero matches). **Confirms cycle Round 4 framing 5 candidate (c) finding**: v3 §4.7's "matches existing component library" citation was authoring-time silent confidence about substrate that didn't exist. Build-from-scratch operates with full design discretion at chunk 2 grain.

No framing-revisits surface at brief-draft. Framing-revisit risk at Session 10 implementation (criterion 2 reactive grain) is bounded by RI-6 five-grain verification preventing existing-canvas-view-consumer-contract surprises.

**Criterion 3 — Substantively-novel-logic scope.**

Three substrate areas are genuinely greenfield:

1. **Build-from-scratch tab component** per Sub-Q19.α (no existing pattern in codebase; disk-verified absence of Radix/shadcn/TanStack; `components/ui/` directory absent; no shadcn-pattern imports).
2. **Per-source callback refactor** at SplitScreenLayout (`onAgentDirective` + `onMainframeNavigate` + `onCanvasDrillDown` + `onDropEvent` replacing current single `handleCanvasNavigate` dispatch).
3. **Pattern γ source-driven routing** with 4 rules + 3 edge cases (EC1.β always-prompt-on-replace + EC2.β focus-existing-on-exact-match + EC3.β one-tab-per-batch).

All three are substantively-novel-logic per RI-7 criterion 3.

**Path C invocation disposition: Option B (single chunk-2 brief; single-session implementation budget by default; explicit Path C session-split availability at Session 10 onset).**

Rationale:

- Volume forecast (~1000-1500 LOC) sits at HIGH end of cycle band but below RI-7 empirical single-session ceiling. Single-session implementation is feasible.
- Option A (single brief; single-session-no-split-capability) under-prepares for the case where Session 10 implementation surfaces budget overrun at any task boundary — the operational flex valve becomes implicit rather than explicit.
- Option C (upfront split into chunk-2a + chunk-2b briefs) is operationally available — substrate-coherent split boundary exists (state lift + ContextualCanvas refactor + canvasContext binding | Pattern γ routing + CanvasTabStrip + tests). But Option C pays ~2x brief authoring overhead upfront for a scenario that may not materialize.
- Option B preserves Option C's split-capability without paying the brief overhead upfront. If Session 10 implementation surfaces budget overrun OR framing-revisit at Task 3 → Task 4 boundary, the implementation splits across Sessions 10a + 10b at the substrate-coherent fault line (Tasks 1+2+3+6 vs Tasks 4+5+7). Phase 4 chunk 3 precedent (reactive Path C invocation at impl) and Phase 6 chunk-6.2 precedent (prospective Path C split at brief-draft) both apply — Option B chooses the prospective-light shape (preserves split availability + reactive flexibility).

**Substrate-coherent session-split fault line (forward-pointer for Session 10 onset adjudication):**

If Session 10 implementation surfaces budget friction:

- **Session 10a scope** (substrate state lift; no user-visible behavior change): Task 1 (Tab data model) + Task 2 (ContextualCanvas refactor) + Task 3 (SplitScreenLayout state lift + per-source callbacks) + Task 6 (canvasContext active-tab binding). Effective behavior at chunk-10a close: SplitScreenLayout holds tabs array of length 1; per-source callbacks all route to "replace active tab" via default Pattern-γ-degenerate fallback (no Pattern γ logic yet); ContextualCanvas renders Props-driven (single tab acts like singleton); no tab strip UI; existing 12 E2E specs preserved.
- **Session 10b scope** (Pattern γ routing + tab UI + tests + RI-6 five-grain verification): Task 4 (Pattern γ dispatch + edge cases) + Task 5 (CanvasTabStrip) + Task 7 (full test suite + RI-6 five-grain).

**Session 9 disposition (operational flex per Session 6 chunk 1 precedent):**

If Session 8 brief surfaces low scope-lock complexity (all sub-Qs locked at cycle; no framing-revisits at brief-draft; no Path C invocation at brief-draft grain), Session 9 may collapse per chunk 1 precedent. Brief drafter does not pre-commit; Session 9 onset adjudicates.

## 2. Scope

### What chunk 2 ships

- **Tab data model lift** per Sub-Q11.b.α: `tabs: Array<Tab> + activeTabId: string` at SplitScreenLayout grain; each `Tab` carries `{ tabId, directive, selectedEntity, history, historyIndex }`. Tabs array seeded with one tab on mount; multi-tab state evolves via Pattern γ routing.
- **ContextualCanvas refactor**: internal history state removed (lines 67-93 lifted); component becomes pure render-from-Props for active tab's `directive` + Props-driven history navigation (`history`, `historyIndex`, `onGoBack`, `onGoForward` Props receive lifted state from parent).
- **Per-source callback Props refactor** at SplitScreenLayout: `handleCanvasNavigate` single callback decomposes into four per-source handlers internal to SplitScreenLayout, each implementing its own Pattern γ routing:
  - `handleMainframeNavigate` — passed to `Zone1ConsolidatedPanel` as `onNavigate` Prop; routes per Pattern γ (replace active tab; EC1.β always-prompt-on-replace).
  - `handleAgentDirective` — passed to `AgentChatPanel` as `onNavigate` Prop; routes per Pattern γ (new tab focused OR focus-existing per EC2.β).
  - `handleCanvasDrillDown` — passed to `ContextualCanvas` as `onDirectiveChange` Prop; routes per Pattern γ (stays in active tab; adds to that tab's history).
  - `handleDropEvent` — substrate-now-enforcement-later additive callback Prop at chunk 2; routes per Pattern γ (new tab; EC3.β one-tab-per-batch); **chunk 3 wires consumer from AgentChatPanel drop side**.
- **Pattern γ source-driven routing logic** per Sub-Q11.a — pure functions in new `canvasTabRouting.ts` module:
  - Rule 1 (Drop event → new tab focused per 11.d.open.α; chunk 3 consumer wires).
  - Rule 2 (Agent canvas_directive → new tab focused; OR focus-existing on exact `(directive, selectedEntity)` match per EC2.β).
  - Rule 3 (Zone 1 Region 7.2/7.3 navigation → replace active tab's directive with EC1.β always-prompt-on-replace).
  - Rule 4 (In-canvas drill-down → stays in active tab; appends to that tab's history; advances historyIndex).
  - EC1.β: always-prompt-on-replace (v1 default = native `window.confirm()` dialog; per-form dirty-state detection substrate-now-enforcement-later for post-v1 selective prompting).
  - EC2.β: focus-existing-on-exact-match (matcher function compares `(directive.type, directive.<id-field>, selectedEntity?)` tuple against open tabs).
  - EC3.β: one-tab-per-batch (drop event carries `drop_session_id` per chunk-6.2b precedent; tab opens once per batch regardless of file count).
- **CanvasTabStrip UI component** per Sub-Q11.c.α — build-from-scratch per Sub-Q19.α; browser-tab-shape at top of canvas area (above existing back/forward header):
  - Each tab: fixed width 140px (within Sub-Q11.c specification of ~120-180px); truncated title with overflow ellipsis; close button (×) on hover; active tab visually distinguished via background fill + bottom-border accent.
  - Open/close/switch semantics per Sub-Q11.d: open focused (11.d.open.α); close → adjacent-right or adjacent-left-fallback at rightmost (11.d.close.α); switch instant (11.d.switch.α; no animation overhead).
  - Tabs-zero state: when last tab closes, re-render `{type: 'none'}` directive with neutral empty state (refreshing the stale "Use the Mainframe rail" copy from pre-chunk-1 — see §3 chunk 1 side-finding).
  - Tab strip overflow at N≥6: horizontal scroll with subtle shadow indicators on left/right when content overflows; no max-tab-count enforcement per Cut 9 v1 limitation (Sub-Q14/15/16 deferred).
- **canvasContext active-tab binding** per Sub-Q18.α: source-of-truth changes from singleton state to active-tab state; type-level signature unchanged (`{ current_directive, selected_entity }` singleton shape preserved at TYPE level). **Zero touch to Phase 1.2 substrate**: `canvasContext.ts` type + `canvasContext.schema.ts` Zod + `canvasContextSuffix.ts` orchestrator-prompt formatter all unchanged.
- **Session-only persistence** (no localStorage at v1 per Cut 9 v1 limitation; tabs reset on reload).
- **Tab title derivation** in new `tabTitle.ts` module: per-directive-type human-shorthand mapping (e.g., `chart_of_accounts → "Chart of Accounts"`; `journal_entry { entryId } → "Journal #<entry_number>"` when resolvable; fallback to directive type label). Distinct from `canvasContextSuffix.ts:describeDirective` (different audience — UI tabs need punchy labels; agent prompt needs narrative phrasing).

### Props-API distinction (load-bearing for chunk 2 scope vs chunk 3 scope)

A precise discrimination from chunk 1 close that this brief codifies:

- **Discriminated-union member additions** (e.g., adding `pending_documents` to `canvasDirective.ts`'s discriminated union): RI-1 strict; requires consumer present at addition time. Every `switch(directive.type)` exhaustive check in the codebase must handle the new member at compile time. Chunk 2 does NOT add new directive members; `canvasDirective.ts` unchanged.
- **Optional callback Prop additions** (e.g., `onDropEvent?: (event: DropEvent) => void` on SplitScreenLayout): RI-1 doesn't apply because optional Props are additive — Props without callers compile cleanly. Chunk 2 ships the signature; chunk 3 wires the consumer from AgentChatPanel drop side as clean composition.

This is a genuine substrate-now-enforcement-later application of ADR-0010 at the Props-API grain. The signature lands at chunk 2 (so chunk 3 doesn't have to re-amend SplitScreenLayout Props when wiring the drop handler); the consumer fires at chunk 3.

**Fourth UI-layer instance of ADR-0010** across the Phase 6.5 cycle + chunks:

1. Sub-Q7.4.α' Region 7.4 hidden with structural reservation (cycle Round 2; chunk 1 shipped).
2. Sub-Q8.c.α₁→α₂ localStorage → DB-column persistence post-v1 (cycle Round 2; chunk 1 shipped α₁ at staging).
3. Sub-Q9.d.α→δ session-only → IndexedDB attachment-persistence post-v1 (cycle Round 3; chunk 3 ships α).
4. `onDropEvent` callback Prop additive at chunk 2 with chunk 3 consumer (this brief).

Codification N=4 at UI-layer grain strengthens the Phase 6.5 retrospective RI extension candidate.

### What chunk 2 does NOT ship (deferred to chunk 3)

- **`pending_documents` directive enum value** at `canvasDirective.ts` — chunk 3 adds when `PendingDocumentsView` ships as consumer.
- **`canvasContextSuffix.ts:describeDirective` case for `pending_documents`** — chunk 3 adds alongside enum addition (downstream forward-pointer; describeDirective switch is exhaustive over CanvasDirective union).
- **`tabTitle.ts:tabTitleForDirective` case for `pending_documents`** — chunk 3 adds when shipping PendingDocumentsView nav surface.
- **`onDropEvent` consumer wiring** at AgentChatPanel drop handler — chunk 3 wires.
- **`PendingDocumentsView.tsx` component** + state machine port from DocumentIntakeRail — chunk 3.
- **AgentChatPanel drop handler / staged attachments tray / acknowledgment toast** — chunk 3.
- **DocumentIntakeRail.tsx deletion** — chunk 3 second commit per Sub-Q4 two-step removal.
- **"Pending Documents" Region 7.2 Billing nav item** addition (8 → 9 items per cycle Sub-Q7.2.a) — chunk 3.

### What chunk 2 also does NOT touch

- **Zone 1 layout / Region 7.x internals** (chunk 1 substrate; chunk 2 consumes `Zone1ConsolidatedPanel` as Pattern γ routing source via `onNavigate` Prop but doesn't modify the panel).
- **AgentChatPanel internals** (`collapsed` + `onExpand` + `onAssistantTurnAdded` Props from chunk 1 preserved; chunk 2 doesn't extend; chunk 3 adds drop handler).
- **Full-page routes** per memory pointer `project_phase_6_5_full_page_routes_layout.md`: tab strip is Zone 3 property within the three-zone shell. Full-page routes (e.g., `/[locale]/[orgId]/agent/actions`) are out-of-band of the three-zone shell layout; Pattern γ routing rules apply only within the three-zone shell. Multi-tab does NOT extend to full-page routes at chunk 2.
- **Service layer** (no service-layer touches; Two Laws all N/A per cycle Round 6 Surface D — chunk 2 is React state + component work).
- **DB schema / migrations** (none).

## 3. Substrate touchpoints

Verify-from-disk at Session 10 implementation onset before consuming any line/path reference below. All counts/paths confirmed at Session 8 brief-drafting against HEAD `5a9492b`.

### Files modified

- **`apps/web/src/components/bridge/SplitScreenLayout.tsx`** (159 lines post-chunk-1). State lift from singleton `(directive, selectedEntity)` to `(tabs, activeTabId)`; per-source callback decomposition replacing single `handleCanvasNavigate`; canvasContext derivation from active-tab. Touch grain: substantive (~100-150 LOC modified). Preserve: `useShellState` + `useWorkspace` integration; keyboard handler for Cmd+\\ / Cmd+Shift+\\; `chatCollapsed` / Zone 2 collapsed-state Prop wiring; `currentUserRole` membership query + propagation; OrgSwitcher + AvatarDropdown top nav strip; canvasContext shape passed to AgentChatPanel.
- **`apps/web/src/components/bridge/ContextualCanvas.tsx`** (239 lines). Remove internal history state (lines 67-68 `useState<CanvasDirective[]>([directive])` + `useState(0)`); remove useEffect sync (lines 71-77); refactor `goBack`/`goForward` (lines 79-93) from local mutations to callback dispatches to parent. Update file-top comment per CLAUDE.md staleness review (current comment lines 2-4 say "Maintains its OWN navigation history" — post-chunk-2 it does NOT; lifts to parent). Update `'none'` case copy at lines 213-216 (currently references deleted MainframeRail — refresh to reference Zone 1 OR generic empty state per tabs-zero design). Touch grain: ~50-80 LOC modified.

### Files created

- **`apps/web/src/shared/types/canvasTab.ts`** (new; ~30-50 LOC). Defines `Tab` type:
  ```typescript
  export type Tab = {
    tabId: string;          // crypto.randomUUID at open time
    directive: CanvasDirective;
    selectedEntity?: SelectedEntity;
    history: CanvasDirective[];
    historyIndex: number;
  };
  ```
  Plus helper signatures (e.g., `createTab(directive: CanvasDirective): Tab`). Co-located in `shared/types/` adjacent to `canvasDirective.ts` + `canvasContext.ts`.
- **`apps/web/src/components/bridge/CanvasTabStrip.tsx`** (new; ~250-350 LOC). Browser-tab-shape UI component; consumes `tabs: ReadonlyArray<Tab> + activeTabId` + `onSwitch(tabId: string)` + `onClose(tabId: string)` Props. Renders horizontal tab strip with overflow scroll; per-tab title + close button + active-state. Build-from-scratch per Sub-Q19.α with full design discretion (agent_interface.md surfaced no component-library direction per cycle framing 5 finding).
- **`apps/web/src/components/bridge/canvasTabRouting.ts`** (new; ~80-150 LOC). Pure-function module exporting Pattern γ dispatch helpers:
  ```typescript
  export function routeNewTab(state: TabsState, directive: CanvasDirective, opts?: { focusExistingExactMatch?: boolean }): TabsState;
  export function routeReplaceActive(state: TabsState, directive: CanvasDirective): TabsState;
  export function routeStayInActive(state: TabsState, directive: CanvasDirective): TabsState;
  export function findExistingExactMatch(state: TabsState, directive: CanvasDirective, selectedEntity?: SelectedEntity): string | null;
  export function closeTab(state: TabsState, tabId: string): TabsState;
  export function switchTab(state: TabsState, tabId: string): TabsState;
  ```
  Pure functions enable unit testing without React DOM (chunk 6.2b vitest gap inheritance per chunk 1 A1-B disposition).
- **`apps/web/src/components/bridge/tabTitle.ts`** (new; ~80-120 LOC). Per-directive-type → human-shorthand label mapping. Exhaustive over `CanvasDirective` union (chunk 3 adds `'pending_documents'` case alongside enum addition).
- **`apps/web/tests/unit/canvasTabRouting.test.ts`** (new; ~6-10 tests). Pure-function unit tests for Pattern γ dispatch + edge case matchers.
- **`apps/web/tests/unit/tabTitle.test.ts`** (new; ~3-5 tests). Title derivation for representative directive types.
- **`apps/web/tests/e2e/phase-6-5-multi-tab.spec.ts`** (new; ~5-8 scenarios). E2E multi-tab golden paths + edge cases.
- **`apps/web/tests/e2e/fixtures/multiTab.ts`** (new; helper to seed N tabs with directive + history shapes per §7.2 carry-forward).
- **`apps/web/tests/helpers/canvasTab.ts`** (new; per-tab assertion helpers per §7.2 carry-forward).

### Files unchanged

- **`apps/web/src/shared/types/canvasDirective.ts`** (64 lines; 38 directive members) — chunk 3 extends with `'pending_documents'`.
- **`apps/web/src/shared/types/canvasContext.ts`** (22 lines) — Sub-Q18.α singleton-shape preserved at TYPE level.
- **`apps/web/src/shared/schemas/canvas/canvasContext.schema.ts`** (37 lines) — Zod schema unchanged.
- **`apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts`** (120 lines) — orchestrator prompt-suffix formatter unchanged. `describeDirective` switch is exhaustive over `CanvasDirective` union; chunk 3 adds `'pending_documents'` case alongside enum addition (downstream forward-pointer; not chunk 2 scope).
- **`apps/web/src/components/bridge/AgentChatPanel.tsx`** (~880 lines post-chunk-1; `collapsed` + `onExpand` + `onAssistantTurnAdded` Props preserved). Chunk 3 extends with drop handler.
- **`apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx`** (~330 lines per chunk 1) — chunk 2 consumes as Pattern γ routing source via `onNavigate` Prop only.
- **`apps/web/src/hooks/useShellState.ts`** + **`useWorkspace.ts`** + **`apps/web/src/shared/storage/shellStateStorage.ts`** (chunk 1 substrate).
- **`apps/web/src/components/canvas/DocumentIntakeRail.tsx`** (311 lines; chunk 3 deletes).
- **Service layer** — no touches.

### Chunk 1 side-finding to address at chunk 2

`apps/web/src/components/bridge/ContextualCanvas.tsx` lines 213-216:

```typescript
case 'none':
  return (
    <div className="text-neutral-400 text-sm">
      Use the Mainframe rail on the left to choose a view.
    </div>
  );
```

The `Use the Mainframe rail on the left` copy is stale post-chunk-1 (MainframeRail deleted; Zone 1 subsumes its role). Chunk 2's tabs-zero state refactor naturally folds the copy update — replace with neutral empty-state copy referencing Zone 1 OR generic ("Pick a view from the left panel" or similar). Brief is non-prescriptive on exact phrasing; Session 10 implementation picks. Banking this as a chunk-1 sweep item that didn't surface at chunk 1 close (worth flagging in Phase 6.5 retrospective drafting as a CLAUDE.md file-top-comment-staleness-review-adjacent pattern at neighbor-text grain).

## 4. Tasks

Strict-sequence per cycle §6: Task 1 → Task 2 → Task 3 → Task 4 → (Task 5 + Task 6 parallel-eligible) → Task 7.

Session 10 implementation onset adjudicates parallel dispatch vs sequential direct (see §6).

### Task 1 — Tab data model type

Create `apps/web/src/shared/types/canvasTab.ts` exporting `Tab` type + helper functions.

**Files:**
- Create: `apps/web/src/shared/types/canvasTab.ts`

**Tab shape per Sub-Q11.b.α:**

```typescript
import type { CanvasDirective } from './canvasDirective';
import type { SelectedEntity } from './canvasContext';

export type Tab = {
  tabId: string;
  directive: CanvasDirective;
  selectedEntity?: SelectedEntity;
  history: CanvasDirective[];
  historyIndex: number;
};

export type TabsState = {
  tabs: ReadonlyArray<Tab>;
  activeTabId: string;
};

export function createTab(directive: CanvasDirective): Tab {
  return {
    tabId: crypto.randomUUID(),
    directive,
    history: [directive],
    historyIndex: 0,
  };
}
```

**Acceptance criteria:**

- `Tab` type exported with signature above.
- `TabsState` type exported.
- `createTab` helper exported; generates fresh `tabId` via `crypto.randomUUID()`; seeds `history` with the initial directive at `historyIndex: 0`.
- Co-located with existing `canvasDirective.ts` + `canvasContext.ts` in `shared/types/`.
- No localStorage / persistence at type level (session-only per Cut 9).

**Dependency:** none; Task 1 fires first.

**Parallelizable:** small task; not worth parallelizing.

### Task 2 — ContextualCanvas refactor (internal history lift)

Refactor `ContextualCanvas.tsx` to pure render-from-Props for active tab. Remove internal history state; accept history + historyIndex + back/forward callbacks from parent.

**Files:**
- Modify: `apps/web/src/components/bridge/ContextualCanvas.tsx`

**Prop signature post-Task-2:**

```typescript
interface Props {
  directive: CanvasDirective;
  history: ReadonlyArray<CanvasDirective>;
  historyIndex: number;
  canGoBack: boolean;
  canGoForward: boolean;
  onGoBack: () => void;
  onGoForward: () => void;
  onDirectiveChange: (d: CanvasDirective) => void;
  onSelectEntity?: (entity: SelectedEntity) => void;
}
```

Brief is non-prescriptive on exact Prop names — Session 10 may pick `canGoBack` / `canGoForward` Boolean Props vs derived from history+historyIndex. The substantive change is that `history` + `historyIndex` are owned by parent (SplitScreenLayout), not by ContextualCanvas.

**Acceptance criteria:**

- ContextualCanvas no longer holds `history` + `historyIndex` state internally.
- ContextualCanvas no longer holds the `useEffect` sync (lines 71-77 pre-Task-2 deleted).
- Back/forward arrow buttons still render at the canvas header; click invokes `onGoBack` / `onGoForward` parent callbacks.
- "N / M" history position counter still displays (computed from `historyIndex + 1` / `history.length` Props).
- `renderDirective` switch unchanged in shape; receives `directive` from Props.
- `'none'` case copy updated to remove stale "Use the Mainframe rail on the left" reference.
- File-top comment updated per CLAUDE.md staleness review (current line 3 says "Maintains its OWN navigation history" — post-Task-2 it does NOT; lifts to parent).

**Dependency:** Task 1 type definition.

**Parallelizable:** not independently; sequential after Task 1.

### Task 3 — SplitScreenLayout state lift + per-source callback refactor

Substantive architectural shift point. Refactor SplitScreenLayout from singleton-state `(directive, selectedEntity)` to tabs-array `(tabs, activeTabId)`. Decompose `handleCanvasNavigate` single callback into four per-source handlers.

**Files:**
- Modify: `apps/web/src/components/bridge/SplitScreenLayout.tsx`

**State shape post-Task-3:**

```typescript
const [tabsState, setTabsState] = useState<TabsState>(() => {
  const initialTab = createTab(initialDirective ?? { type: 'none' });
  return { tabs: [initialTab], activeTabId: initialTab.tabId };
});

// Derived: active tab object.
const activeTab = tabsState.tabs.find(t => t.tabId === tabsState.activeTabId)
  ?? tabsState.tabs[0]; // defensive: should always match in practice
```

**Per-source callback decomposition:**

- `handleMainframeNavigate(directive)` — Pattern γ Rule 3: replace active tab's directive; EC1.β prompt-on-replace via `window.confirm(...)` at v1 (per §7 risk 2). On accept: replace; on reject: no-op.
- `handleAgentDirective(directive)` — Pattern γ Rule 2: route per `findExistingExactMatch(...)` (EC2.β):
  - Match found → switch active to existing matched tab.
  - No match → open new tab focused (Sub-Q11.d.open.α).
- `handleCanvasDrillDown(directive)` — Pattern γ Rule 4: append to active tab's history; advance historyIndex; update active tab's directive.
- `handleDropEvent(event)` — Pattern γ Rule 1 + EC3.β: open new tab focused; one tab per `drop_session_id` (consumer wires at chunk 3; signature lands at chunk 2).
- `handleGoBack` / `handleGoForward` — for ContextualCanvas back/forward arrows: decrement/increment active tab's historyIndex; update active tab's directive.
- `handleSelectEntity` — update active tab's selectedEntity via `reduceSelection` (preserves existing semantics).

**canvasContext derivation:**

```typescript
const canvasContext: CanvasContext = {
  current_directive: activeTab.directive,
  selected_entity: activeTab.selectedEntity,
};
```

Singleton-shape preserved per Sub-Q18.α; source-of-truth is active tab.

**Acceptance criteria:**

- State lift functional: tabs array initialized with one tab on mount; activeTabId tracks active tab.
- Four per-source handlers implemented (handleMainframeNavigate + handleAgentDirective + handleCanvasDrillDown + handleDropEvent).
- `onDropEvent` Prop signature additive at SplitScreenLayout's render output; default no-op or undefined handler if no consumer wired (substrate-now-enforcement-later); chunk 3 wires consumer.
- canvasContext derives from active tab (Sub-Q18.α; zero touch to canvasContext.ts / .schema.ts / canvasContextSuffix.ts).
- AgentChatPanel receives canvasContext + onNavigate (handleAgentDirective) + existing Props.
- Zone1ConsolidatedPanel receives onNavigate (handleMainframeNavigate).
- ContextualCanvas receives active tab's directive + history + historyIndex + go-back/forward callbacks + onDirectiveChange (handleCanvasDrillDown) + onSelectEntity.
- CanvasTabStrip mounted between header strip and back/forward header (or above; exact placement at Session 10).
- File-top comment updated per CLAUDE.md staleness review (current describes chunk 1 three-zone shape; add per-tab state lift + Pattern γ routing).
- Existing 12 E2E specs preserved without modification (RI-6 five-grain criterion 5 — see Task 7).

**Dependency:** Task 1 (Tab type) + Task 2 (ContextualCanvas Props-driven shape).

**Parallelizable:** not (single file; substantive coordination).

### Task 4 — Pattern γ routing dispatch + edge case logic

Implement pure-function routing helpers in `canvasTabRouting.ts`. Pattern γ Rules 1-4 + edge cases EC1.β / EC2.β / EC3.β.

**Files:**
- Create: `apps/web/src/components/bridge/canvasTabRouting.ts`

**Pure functions:**

```typescript
import type { CanvasDirective } from '@/shared/types/canvasDirective';
import type { SelectedEntity } from '@/shared/types/canvasContext';
import type { Tab, TabsState } from '@/shared/types/canvasTab';
import { createTab } from '@/shared/types/canvasTab';

// EC2.β: find existing tab with matching directive + selectedEntity tuple.
// Match criterion: directive.type === target.type AND all directive
// payload fields equal AND (selectedEntity ?? null) equal-by-shape.
export function findExistingExactMatch(
  state: TabsState,
  directive: CanvasDirective,
  selectedEntity?: SelectedEntity,
): string | null { ... }

// Rule 1 + Rule 2: open new tab focused. If focusExistingExactMatch
// + match found, switch active to existing instead.
export function routeNewTab(
  state: TabsState,
  directive: CanvasDirective,
  opts: { focusExistingExactMatch?: boolean; selectedEntity?: SelectedEntity },
): TabsState { ... }

// Rule 3: replace active tab's directive (caller invokes EC1.β prompt
// before calling this fn if dirty-state policy requires).
export function routeReplaceActive(
  state: TabsState,
  directive: CanvasDirective,
): TabsState { ... }

// Rule 4: append directive to active tab's history; advance index.
export function routeStayInActive(
  state: TabsState,
  directive: CanvasDirective,
): TabsState { ... }

// Sub-Q11.d.close.α: close tab; switch to adjacent-right (fallback
// adjacent-left at rightmost). Returns updated state. When
// state.tabs becomes empty, caller adds a `{type: 'none'}` tab
// (tabs-zero handled at SplitScreenLayout grain via post-call check).
export function closeTab(state: TabsState, tabId: string): TabsState { ... }

// Sub-Q11.d.switch.α: switch active to tabId (instant; no animation).
export function switchTab(state: TabsState, tabId: string): TabsState { ... }
```

**EC1.β prompt-shape (per §7 risk 2 operational choice):**

EC1.β is invoked at the call site of `handleMainframeNavigate` in SplitScreenLayout, NOT inside the pure-function module (because `window.confirm` is a side effect):

```typescript
function handleMainframeNavigate(directive: CanvasDirective) {
  const message = `Replace the current view? Any unsaved changes will be lost.`;
  if (!window.confirm(message)) return;
  setTabsState(prev => routeReplaceActive(prev, directive));
}
```

Per-form dirty-state detection is substrate-now-enforcement-later per ADR-0010; v1 always-prompts (matches Sub-Q11.a EC1.β lock + cycle Round 4 brainstorming-side soft lean).

**Acceptance criteria:**

- All six pure functions exported with signatures above.
- `findExistingExactMatch` correctly matches `(directive.type, directive.<id-fields>, selectedEntity ?? null)` tuple — Session 10 implementation enumerates which directive fields are id-bearing (e.g., `entryId` for `journal_entry`; `accountId` for `report_account_ledger`; etc.).
- `routeNewTab` returns immutable updated state (returns new TabsState; doesn't mutate input).
- `routeReplaceActive` updates active tab's directive AND resets that tab's history to `[newDirective]` with `historyIndex: 0` (replacement is a fresh navigation; back-history of the previous directive is intentionally discarded per Sub-Q11.a Pattern γ Rule 3 specification).
- `routeStayInActive` appends to active tab's history; advances historyIndex by 1; trims any forward history past current index (canonical browser-back-history behavior).
- `closeTab` returns adjacent-right tab id as new activeTabId; if closing rightmost, adjacent-left; if closing only tab, returns empty `tabs: []` (caller handles tabs-zero refresh).
- `switchTab` updates activeTabId; tabs array unchanged.
- All functions are pure (no React hooks; no localStorage / window access at module level).

**Dependency:** Task 3 state lift (uses TabsState shape).

**Parallelizable:** functions are independent; can split EC2.β matcher + close/switch helpers across subagents at implementation grain.

### Task 5 — CanvasTabStrip component (build-from-scratch)

Browser-tab-shape UI per Sub-Q11.c.α. Build-from-scratch per Sub-Q19.α with full design discretion (no agent_interface.md component-library direction surfaced per cycle framing 5 + Session 8 brief-draft verify-from-disk).

**Files:**
- Create: `apps/web/src/components/bridge/CanvasTabStrip.tsx`
- Create: `apps/web/src/components/bridge/tabTitle.ts`

**Component signature:**

```typescript
import type { Tab } from '@/shared/types/canvasTab';

interface Props {
  tabs: ReadonlyArray<Tab>;
  activeTabId: string;
  onSwitch: (tabId: string) => void;
  onClose: (tabId: string) => void;
}

export function CanvasTabStrip({ tabs, activeTabId, onSwitch, onClose }: Props) { ... }
```

**Visual treatment:**

- Strip placement: above existing back/forward header in ContextualCanvas's `<main>` (height ~36px; horizontal scrollable container).
- Per-tab: fixed width 140px; height 32px; truncated title with ellipsis overflow; close button (×) appearing on hover; active-state styling: background `bg-white` + bottom-border 2px accent `border-b-2 border-neutral-900`; inactive: `bg-neutral-100 text-neutral-600 hover:bg-neutral-50`.
- Overflow at N≥6 tabs: horizontal scroll within strip container (`overflow-x-auto`); subtle shadow indicators on left/right when content overflows (CSS gradient masks or scroll-position-conditional shadows).
- No max-tab-count enforcement per Cut 9 v1 limitation.
- Close button: `aria-label="Close tab"`; click invokes `onClose(tab.tabId)` and stops propagation (prevents triggering switch).
- Click anywhere on tab body (except close button) invokes `onSwitch(tab.tabId)`.
- Active-tab close-button click: caller's `onClose` handler invokes `closeTab(...)` pure function; new activeTabId set by reducer; switch-to-adjacent-right happens automatically.

**Tab title via `tabTitle.ts`:**

```typescript
export function tabTitleForDirective(directive: CanvasDirective): string {
  switch (directive.type) {
    case 'chart_of_accounts':       return 'Chart of Accounts';
    case 'journal_entry':           return 'Journal Entry';
    case 'journal_entry_list':      return 'Journal Entries';
    case 'journal_entry_form':      return 'New Journal Entry';
    case 'report_pl':               return 'P&L Report';
    case 'report_trial_balance':    return 'Trial Balance';
    case 'report_balance_sheet':    return 'Balance Sheet';
    case 'report_open_bills':       return 'Open Bills';
    case 'report_ap_aging':         return 'AP Aging';
    case 'report_vendor_balance':   return 'Vendor Balance';
    // ... full mapping for all 38 active members ...
    case 'none':                    return 'New tab';
    // chunk 3 adds 'pending_documents' case alongside enum addition.
    default:                        return assertExhaustive(directive);
  }
}
```

Brief is non-prescriptive on exact labels; Session 10 picks per-type with founder review at screenshot gate. Distinct from `canvasContextSuffix.ts:describeDirective` (different audience: agent prompt narrative vs UI tab punchy label).

**Acceptance criteria:**

- CanvasTabStrip renders horizontal tab strip with per-tab title + close button + active-state distinction.
- Tab width = 140px; height = 32px; truncation via CSS `overflow: hidden; text-overflow: ellipsis; white-space: nowrap;`.
- Overflow scroll handles N≥6 tabs without breaking layout.
- Close button appears on hover; click invokes `onClose` without firing `onSwitch`.
- Click on tab body invokes `onSwitch`.
- Active tab visually distinguished (background + bottom-border accent).
- Tabs-zero state: when `tabs.length === 0`, render an empty placeholder (SplitScreenLayout reconciles tabs-zero by creating a fresh `{type: 'none'}` tab on close-last-tab; the empty placeholder is a defensive render not normally reached).
- Build-from-scratch with no Radix/shadcn/TanStack imports; only Tailwind classes + native HTML elements.
- `tabTitle.ts` exhaustive switch over `CanvasDirective` union; TypeScript compiler enforces exhaustiveness (with `assertExhaustive` helper or equivalent).

**Dependency:** Task 1 (Tab type) + Task 3 (SplitScreenLayout passes tabs + handlers as Props).

**Parallelizable:** subcomponents (header row vs scroll container vs individual tab render) can split across subagents at implementation grain.

### Task 6 — canvasContext active-tab binding

Refactor `canvasContext` derivation at SplitScreenLayout to read from active tab's `(directive, selectedEntity)` instead of singleton state. Type-level signature unchanged.

**Files:**
- Modify: `apps/web/src/components/bridge/SplitScreenLayout.tsx` (small adjustment within Task 3's larger refactor; isolated here for clarity).

**Change:**

```typescript
// Pre-chunk-2:
const canvasContext: CanvasContext = {
  current_directive: directive,
  selected_entity: selectedEntity,
};

// Post-chunk-2:
const canvasContext: CanvasContext = {
  current_directive: activeTab.directive,
  selected_entity: activeTab.selectedEntity,
};
```

**Acceptance criteria:**

- canvasContext correctly derives from `activeTab` at every render (synchronous; React state read).
- canvasContext singleton-shape preserved at TYPE level (`{ current_directive, selected_entity }` per Sub-Q18.α).
- Zero touch to `canvasContext.ts` type / `canvasContext.schema.ts` Zod / `canvasContextSuffix.ts` orchestrator prompt formatter / `canvasContextSuffix.ts:describeDirective` switch.
- AgentChatPanel receives canvasContext Prop unchanged (signature compatible; only source-of-truth changes).
- Orchestrator prompt unchanged.
- When user switches active tab via CanvasTabStrip, canvasContext re-derives from new active tab synchronously; next agent invocation sends correct canvasContext.

**Dependency:** Task 3 state lift (provides `activeTab` reference).

**Parallelizable:** folds into Task 3; not separately fired in practice.

### Task 7 — Cross-task verification + tests + RI-6 five-grain verification

Final integration check + validation gates + screenshot gate prep + commit ceremony.

**Files:**
- Create: `apps/web/tests/unit/canvasTabRouting.test.ts` (~6-10 tests)
- Create: `apps/web/tests/unit/tabTitle.test.ts` (~3-5 tests)
- Create: `apps/web/tests/e2e/phase-6-5-multi-tab.spec.ts` (~5-8 scenarios)
- Create: `apps/web/tests/e2e/fixtures/multiTab.ts` (multi-tab seed helper)
- Create: `apps/web/tests/helpers/canvasTab.ts` (per-tab assertion helpers)
- Update file-top comments on all chunk-2-touched files (CLAUDE.md staleness review).

**Unit test scope (~12-18 tests total; node-env compatible per A1-B disposition):**

- **`canvasTabRouting.test.ts`** (~8-10 tests): `findExistingExactMatch` happy + miss paths; `routeNewTab` opens focused; `routeNewTab` with `focusExistingExactMatch` + match found switches existing; `routeReplaceActive` resets active tab history; `routeStayInActive` appends + trims forward history; `closeTab` returns adjacent-right activeTabId; `closeTab` at rightmost falls back to adjacent-left; `closeTab` last tab returns empty array; `switchTab` updates activeTabId only.
- **`tabTitle.test.ts`** (~3-5 tests): representative directive types map to expected labels; `'none'` case returns "New tab"; exhaustiveness over CanvasDirective union (TypeScript compile check; runtime spot check).

**E2E test scope (~5-8 scenarios) at `phase-6-5-multi-tab.spec.ts`:**

1. **Multi-tab open via agent directive** — Send chat message inducing canvas_directive; verify new tab opens focused; original tab still present in strip.
2. **Tab close + adjacent-right switch** — Open 3 tabs; close middle tab; verify activeTabId moves to adjacent-right.
3. **Tab close at rightmost + adjacent-left fallback** — Open 3 tabs; switch to rightmost; close rightmost; verify activeTabId moves to adjacent-left.
4. **Tabs-zero state** — Open 1 tab; close it; verify SplitScreenLayout creates fresh `{type: 'none'}` tab with neutral empty-state copy.
5. **Zone 1 navigation → replace active tab (no dirty state)** — Click Zone 1 nav item; verify EC1.β prompt fires; accept → active tab's directive replaces.
6. **Zone 1 navigation → EC1.β prompt rejected** — Click Zone 1 nav item; verify prompt fires; reject → active tab unchanged.
7. **In-canvas drill-down stays in active tab** — From Chart of Accounts, click an account row (drill-down to ledger); verify active tab's history advances; tab count unchanged.
8. **Agent directive matching existing tab → focus-existing per EC2.β** — Open Open Bills tab; send agent directive that resolves to Open Bills; verify existing tab re-focused (no new tab).
9. **Multi-tab navigation preserves per-tab history** — Open 2 tabs; each does some drill-down navigation; switch between; verify each tab's back/forward state independent.

**Test infrastructure additions per §7.2 carry-forward:**

- **`multiTab.ts` fixture** at `apps/web/tests/e2e/fixtures/`: helper to seed N tabs with directive + history shapes; Playwright pattern with `page.evaluate(...)` to set initial tabs state in dev console OR via UI sequence (open tabs sequentially via agent + Zone 1 clicks).
- **`canvasTab.ts` helpers** at `apps/web/tests/helpers/`: assertion utilities like `expectActiveTabDirectiveType(page, 'chart_of_accounts')` + `expectTabCount(page, N)` + `expectTabPosition(page, tabId, position)`.

**RI-6 five-grain verification (load-bearing per cycle Round 6 Surface D):**

Per chunk-3-Phase-4 RI-6 codification. Five grains:

1. **Substrate-shape grain.** Tab type matches Sub-Q11.b.α specification verbatim. `canvasContext` singleton-shape preserved at TYPE level per Sub-Q18.α. `canvasDirective.ts` unchanged. `canvasContextSuffix.ts:describeDirective` unchanged.

2. **Per-trigger semantic coverage grain.** Each Pattern γ rule (drop / agent / Zone 1 / in-canvas drill-down) has explicit per-source handler at SplitScreenLayout + explicit pure-function dispatch + explicit test coverage per E2E + unit tests.

3. **Per-trigger × per-decision-outcome conformance grain.** Matrix of `(source × outcome)` test coverage:

   | Source | Outcome | Test |
   |---|---|---|
   | Agent | Exact-match-found | E2E scenario 8 (focus existing) |
   | Agent | No match | E2E scenario 1 (new tab) |
   | Zone 1 | Clean tab | E2E scenario 5 (replace) |
   | Zone 1 | Dirty (always-prompt v1) + accept | E2E scenario 5 (replace) |
   | Zone 1 | Dirty + reject | E2E scenario 6 (no-op) |
   | Drill-down | Always | E2E scenario 7 (history append) |
   | Drop (chunk 3 wiring) | — | Substrate-only at chunk 2; chunk 3 covers |

4. **Idempotency-and-side-effect-contract conformance grain.** Tab operations idempotent: `switchTab(state, activeTabId)` returns unchanged state; `closeTab(state, nonExistentTabId)` returns unchanged state. Unit tests cover.

5. **Fifth grain — existing-canvas-view-consumer-contract conformance grain.** Every existing canvas view's tab-aware contract preserved per Cut 6 don't-touch-canvas. Verification protocol:
   - Enumerate every existing canvas view from `canvasDirective.ts` active members (38 total at chunk 2; subtract Phase 2+ stubs).
   - For each, render the view in tab context (open new tab with that directive); confirm the view component itself is unchanged (no view-component code modifications in chunk 2 file diff); confirm rendering behavior preserved.
   - Existing 12 E2E specs pass without modification (`apAgingView.spec.ts` + `openBillsView.spec.ts` + `vendorBalanceView.spec.ts` + etc.). Spot-check: each E2E spec uses `page.getByTitle(...)` selectors against Zone 1 nav items (preserved in chunk 2); the resulting canvas directive renders in active tab; assertion against canvas heading + structure still passes.
   - **Sentinel risk**: a canvas view has implicit dependency on being singleton-rendered (e.g., useEffect with no dependency array that fires once-per-mount; works at singleton; misbehaves when tab close-open re-mounts). Mitigation: Task 7 runs full E2E suite; any regression surfaces and forward-fixes.

**Pre-commit validation gates:**

- `pnpm typecheck` — expected green.
- `pnpm test` — expected 1125 baseline + ~9-15 new unit tests = ~1134-1140 (revised count at ceremony).
- `pnpm agent:validate` — expected 26/26 preserved.
- `pnpm test:no-hardcoded-urls` — expected green.
- `pnpm test:e2e` (full) — expected 12 existing specs + 1 chunk 1 spec + 1 new chunk 2 spec all passing.

**Screenshot gate per CLAUDE.md UI-session-screenshot-gate (~4-6 shots per cycle Round 6 Surface C):**

1. **Single-tab default state** — Fresh session; one tab visible in strip with title (e.g., "New tab" for `{type: 'none'}` or last-loaded directive's title); back/forward arrows + history counter visible.
2. **Multi-tab strip with 3 tabs** — Three tabs open via mixed sources (agent + Zone 1 + agent); active tab visually distinguished; close buttons appear on hover.
3. **Tab strip overflow with 6+ tabs** — Six tabs open; verify horizontal scroll renders; shadow indicators visible at scroll boundaries.
4. **EC1.β prompt-on-replace dialog** — Click Zone 1 nav item with non-trivial active tab; verify `window.confirm` dialog renders.
5. **Tabs-zero state** — Close all tabs; verify SplitScreenLayout creates `{type: 'none'}` neutral state with updated copy (no stale "Mainframe rail" reference).
6. **Active-tab binding for agent context** — Switch tabs; verify chat agent's next reply correctly references active tab's directive (via canvasContext propagation; DevTools network tab can verify canvas_context payload).

**Commit shape:**

Single commit on staging per Round 6 Surface B HIGH reversibility (or two commits if Path C session-split at Session 10 onset; see §6 forward-pointer):

```
feat(bridge): Phase 6.5 chunk 2 — Multi-tab canvas (Cut 9 limited at v1)

[5-7 paragraphs covering: chunk 2 scope; tab data model + state lift;
Pattern γ source-driven routing with EC1.β + EC2.β + EC3.β; CanvasTabStrip
build-from-scratch; canvasContext active-tab binding; RI-6 five-grain
verification outcome; rollback posture]

Cross-references:
- Session 8 chunk 2 brief: docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-2.md (this commit's brief SHA)
- Cycle closeout brief: 79a6ceb
- Chunk 1 brief + implementation: 1c65cd1 + 5a9492b
- v3 proposal: b7cb081
- Amended ui_architecture.md: 9e606f6
```

**Acceptance criteria:**

- All 5 validation gates green.
- Screenshot gate passes (founder ratifies all 4-6 shots).
- Commit lands on staging at Session 10 close (or two-commit shape per Path C session-split if invoked).
- Push readiness three-condition gate met.

**Dependency:** Tasks 1-6 complete.

**Parallelizable:** test subgroups (unit vs E2E vs fixture construction) can split across subagents at implementation grain.

## 5. Cross-task verification + integration testing

Per Task 7 above + Phase C ceremony.

**Floor preservation:** 1125/1125 vitest (chunk 1 baseline) + 26/26 agent:validate + 12 existing E2E specs + 1 chunk 1 E2E spec preserved.

**New tests:** ~12-18 unit + ~5-9 E2E scenarios + 2 fixture/helper files per Task 7 enumeration.

**RI-6 five-grain verification** load-bearing per cycle Round 6 Surface D — substrate-shape + per-trigger-semantic + per-trigger×per-decision-outcome + idempotency + existing-canvas-view-consumer-contract grains all explicit per Task 7.

**Visual regression screenshot gate:** ~4-6 shots per Task 7 enumeration.

## 6. Implementation order + parallelization

### Strict-sequence dependencies

```
Task 1 (Tab type)
  ↓
Task 2 (ContextualCanvas refactor)
  ↓
Task 3 (SplitScreenLayout state lift) [folds Task 6]
  ↓
Task 4 (Pattern γ pure routing)
  ↓
  ├─ Task 5 (CanvasTabStrip + tabTitle) ─┐
  └─ Task 7 (tests + RI-6 verification)  ─┴─→ Phase C ceremony
```

Task 5 and Task 7 can run in parallel post-Task-4 (Task 7's E2E scenarios depend on tab strip render, but most unit tests don't; Session 10 partitions parallelizable subtasks at impl onset).

### Subagent dispatch shape (if Session 10 chooses parallel dispatch)

- **Subagent A** — Task 1 + Task 2 (small co-located refactor; ~80-130 LOC).
- **Subagent B** — Task 3 + Task 6 (substantive state lift; ~120-175 LOC; tight coupling).
- **Subagent C** — Task 4 (Pattern γ pure routing; ~80-150 LOC; testable in isolation).
- **Subagent D** — Task 5 (CanvasTabStrip + tabTitle; ~330-470 LOC; visual + tab title mapping).
- **Subagent E** — Task 7 (tests + fixtures + helpers + RI-6 verification; ~500-700 LOC).

### Sequential direct alternative

Session 10 may elect sequential single-subagent (or direct WSL-side implementation). Chunk 2's state-lift coordination + RI-6 five-grain verification may dominate parallelization win — state lift coordination needs unified context; verification protocol benefits from single orchestrator. Sequential direct keeps commit-grain visibility cleaner.

**Recommendation at brief-draft grain:** sequential direct OR partial-parallel (Subagents D + E parallel post-Task-3; Tasks 1+2+3+4 sequential). Session 10 onset adjudicates.

### Path C session-split availability (forward-pointer for Session 10 onset)

Per §1.1 Option B disposition. If Session 10 implementation surfaces budget friction at Task 3 → Task 4 boundary OR framing-revisit at any task:

- **Session 10a scope** (substrate state lift; no user-visible behavior change): Tasks 1 + 2 + 3 + 6.
  - Effective behavior at Session 10a close: SplitScreenLayout holds tabs array of length 1; per-source handlers all route to "replace active tab" via degenerate Pattern-γ fallback (no full Rule 1/2/4 logic yet); ContextualCanvas Props-driven; canvasContext active-tab-bound; no tab strip UI; existing 12 E2E specs preserved.
  - Single commit on staging at Session 10a close: `feat(bridge): Phase 6.5 chunk 2a — tab data model + state lift substrate`.
- **Session 10b scope** (feature; Pattern γ routing + tab UI + tests): Tasks 4 + 5 + 7.
  - Single commit on staging at Session 10b close: `feat(bridge): Phase 6.5 chunk 2b — Pattern γ routing + CanvasTabStrip + tests`.

Session 10b inherits Session 10a's substrate; no brief amendment required at the split.

If Path C invokes, the chunk-2 brief stays single (this document); split is at implementation-session grain, not brief grain. Path C reactive shape per Phase 4 chunk 3 precedent.

## 7. Substrate touchpoint risks + mitigations

Five risks at chunk 2 grain. None require scope adjustment at brief-drafting; all are operationalizable at Session 10 onset.

### Risk 1 — State lift complexity

**Risk:** Tab data model lift from singleton to array-with-active-pointer touches multiple callsites of `handleCanvasNavigate`; per-source callback refactor inverts the dispatch shape. Risk: coordination error at any callsite breaks routing.

**Mitigation:** Task 3 explicit per-callsite enumeration (handleMainframeNavigate → Zone1ConsolidatedPanel; handleAgentDirective → AgentChatPanel; handleCanvasDrillDown → ContextualCanvas; handleDropEvent → additive Prop at SplitScreenLayout). Task 7 RI-6 five-grain verification with per-trigger × per-decision-outcome test matrix catches coordination errors. Session 10 implementation should grep `handleCanvasNavigate` after Task 3 refactor to verify zero remaining callsites of the deprecated handler.

### Risk 2 — EC1.β prompt-shape operational choice (native `confirm()`)

**Risk:** v1 default = native `window.confirm()` dialog (substrate-now-enforcement-later for React modal post-v1 if friction surfaces). Native confirm UX is jarring (browser chrome; can't style; can't customize button labels beyond OK/Cancel). May feel out-of-place in the Bridge's neutral-professional UI aesthetic.

**Mitigation:** Substrate-now-enforcement-later per ADR-0010 (fifth UI-layer instance candidate alongside the four enumerated at §2). Phase 6.5 retrospective re-evaluates if N=2+ friction signals surface from founder usage. Screenshot gate shot 4 captures the native confirm dialog so founder review evaluates fit; if dialog feels out-of-place at gate review, forward-fix-with-React-modal candidate flagged for chunk 3 brief drafting or post-Phase-6.5 amendment.

### Risk 3 — Tab strip overflow at N≥6 tabs

**Risk:** No max-tab-count enforcement per Cut 9 v1 limitation; tab strip needs overflow handling. Brief operationalizes (horizontal scroll with shadow indicators at boundaries; fixed 140px tab width per Sub-Q11.c). Risk: visual ergonomics at extreme tab counts unverified — does the strip stay usable at N=10? N=20?

**Mitigation:** Screenshot gate shot 3 explicitly captures 6+ tab overflow state. If usage evidence post-chunk-2 surfaces tab-count anti-patterns (e.g., founder regularly hits N=15+ and loses track), Phase 6.5 retrospective evaluates Sub-Q16 (Tab management UX — max-count enforcement; close-others; etc.) graduation candidate. v1 ships with no enforcement; Sub-Q14/15/16 deferrals from cycle still apply.

### Risk 4 — canvasContext derivation timing on tab switch

**Risk:** When user switches active tab via CanvasTabStrip click, canvasContext must update synchronously before AgentChatPanel's next agent invocation. Race condition between tab switch and agent prompt construction would send stale canvasContext to orchestrator.

**Mitigation:** Task 6 canvasContext derives from React state (synchronous read); useState updates are synchronous within a single render pass; React's render cycle guarantees that subsequent agent invocations from AgentChatPanel see the new canvasContext. Task 7 E2E scenario 6 (active-tab binding for agent context) covers tab-switch + agent-invocation sequence. No async coordination needed at chunk 2 grain.

### Risk 5 — RI-6 five-grain fifth-grain verification scope (existing-canvas-view-consumer-contract)

**Risk:** Per cycle Round 6 Surface D, every existing canvas view's tab-aware contract must be preserved per Cut 6 don't-touch-canvas. Risk: a canvas view has implicit dependency on being singleton-rendered (e.g., `useEffect` with empty deps that fires once-per-mount; works at singleton; misbehaves when tab close-open re-mounts and triggers second fire). Could surface as: stale data, double-fetch, double-subscribe, leaked event listeners.

**Mitigation:** Task 7 verification protocol enumerates every existing view from `canvasDirective.ts` active members; renders each in tab context; confirms behavior preserved. Existing 12 E2E specs pass without modification provides the broad-spectrum regression check (each spec exercises one canvas view through agent + Zone 1 nav + drill-down flows). Sentinel risk grep: search `apps/web/src/components/canvas/` for `useEffect(.+, \[\])` patterns; flag any that look mount-event-coupled vs render-bound. Brief is non-prescriptive on exhaustive grep at scope-lock; Session 10 implementation evaluates on a per-view basis if RI-6 five-grain check surfaces a regression.

## 8. Cross-references

### Cycle predecessors

- **Cycle closeout brief**: `docs/09_briefs/phase-6.5/2026-05-16-document-drop-and-shell-consolidation-scope-lock-cycle-close.md` (commit `79a6ceb`). §5.2 chunk 2 acceptance criteria + §7.2 chunk 2 partial-information carry-forwards + §6 inter-chunk dependency map (Path C risk surface).
- **v3 proposal**: `docs/09_briefs/phase-6/2026-05-16-cto-proposal-v3-document-drop-shell-consolidation.md` (commit `b7cb081`). §5.1 chunk 2 decomposition + §3 Cut 9.
- **Amended `ui_architecture.md`**: `docs/03_architecture/ui_architecture.md` (commit `9e606f6`). § The Split-Screen Layout (Zone 3 multi-tab structure post-Phase-6.5); § The `canvas_directive` Contract Amendment (singleton-shape preserved at TYPE level per Sub-Q18.α); § Canvas ↔ Chat State Model Amendment 2026-05-16 (per-tab navigation history + bookmark pill Pattern γ rule).
- **Chunk 1 brief**: `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` (commit `1c65cd1`).
- **Chunk 1 implementation**: commit `5a9492b`.

### Memory pointers (post-chunk-1 carry-forwards consumed)

- `project_phase_6_5_chunk_1_billing_nav_ordering.md` — Billing nav-item canonical order; chunk 2 doesn't modify Region 7.2 but routes via `handleMainframeNavigate` (per-source callback) when Region 7.2 nav items fire.
- `project_phase_6_5_full_page_routes_layout.md` — **load-bearing** for chunk 2 scope-lock: full-page routes (e.g., `/[locale]/[orgId]/agent/actions`) are out-of-band of three-zone shell; multi-tab Pattern γ routing does NOT extend to full-page routes; tab strip is Zone 3 property only.

### ADRs

- **ADR-0010** — substrate-now-enforcement-later. Chunk 2 adds the **fourth UI-layer-grain instance** at Props-API grain: `onDropEvent` additive callback Prop with chunk 3 consumer. Prior three instances enumerated at §2. Phase 6.5 retrospective drafting evaluates UI-layer N=4 codification candidate (potentially N=5 if EC1.β React-modal post-v1 graduates per §7 risk 2).
- **ADR-0020** — Source-tree authority-layer enumeration. Chunk 2 creates `apps/web/src/shared/types/canvasTab.ts` (existing `shared/types/` subdir) and `apps/web/src/components/bridge/canvasTabRouting.ts` + `tabTitle.ts` + `CanvasTabStrip.tsx` (existing `components/bridge/` subdir). No new authority-layer sibling creation; chunk 1 already established `apps/web/src/hooks/` + `apps/web/src/shared/storage/` siblings.
- **ADR-0022** §2 — Supersession workflow. Chunk 2 doesn't supersede anything; cycle's `ui_architecture.md` amendment per Session 4 covers multi-tab supersession.

### Code surfaces (verified at HEAD `5a9492b`)

- `apps/web/src/components/bridge/SplitScreenLayout.tsx` (159 lines post-chunk-1; substantive rework target).
- `apps/web/src/components/bridge/ContextualCanvas.tsx` (239 lines; internal history lift target; `'none'` case copy refresh at lines 213-216).
- `apps/web/src/shared/types/canvasDirective.ts` (64 lines; 38 directive members; unchanged at chunk 2).
- `apps/web/src/shared/types/canvasContext.ts` (22 lines; singleton-shape preserved per Sub-Q18.α).
- `apps/web/src/shared/schemas/canvas/canvasContext.schema.ts` (37 lines; Zod schema unchanged).
- `apps/web/src/agent/prompts/suffixes/canvasContextSuffix.ts` (120 lines; `describeDirective` exhaustive switch over `CanvasDirective` — chunk 3 adds `'pending_documents'` case downstream).
- `apps/web/src/components/bridge/AgentChatPanel.tsx` (~880 lines post-chunk-1; `collapsed` + `onExpand` + `onAssistantTurnAdded` Props preserved).
- `apps/web/src/components/bridge/Zone1ConsolidatedPanel.tsx` (~330 lines per chunk 1; consumed as Pattern γ routing source via `onNavigate` Prop).

### Governance precedents

- **Phase 4 chunk 3** at `docs/09_briefs/phase-4/chunks/2026-05-13-phase-4-chunk-3.md` + amended brief at `c76d264` — Path C reactive split at implementation (dispatcher-isolated fault line). Inheritance: chunk 2 brief operates Option B (single brief; reactive session-split availability at Session 10 onset).
- **Phase 6 chunk-6.2a + chunk-6.2b** at `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-2a.md` + `chunk-2b.md` — Path C prospective split at brief-drafting (substrate-vs-feature fault line). Inheritance shape available if Path C escalates from Option B to Option C at Session 10 onset (substrate-coherent split exists per §1.1 + §6 forward-pointer).
- **Phase 4 retrospective RI-1, RI-6, RI-7, RI-10**: `docs/07_governance/retrospectives/phase-4-retrospective.md`. RI-6 five-grain load-bearing at chunk 2 close per §5 + Task 7. RI-7 single-session budget evaluation at §1.1.
- **Chunk-6.3a brief precedent**: `docs/09_briefs/phase-6/chunks/2026-05-15-phase-6-chunk-3a.md` — structural shape inheritance for sub-Q-resolution + verify-from-disk + reasoning chain pattern (chunk 2's task-decomposition shape inherits the per-task acceptance-criteria + carry-forward operationalization convention).

### Downstream

- **Chunk 3 brief** (Session 11 or later): anticipated at `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-3.md`. Chunk 3 ships:
  - `pending_documents` directive enum value at `canvasDirective.ts` (substrate-with-consumer per RI-1; consumer = PendingDocumentsView).
  - `describeDirective` case for `'pending_documents'` at `canvasContextSuffix.ts`.
  - `tabTitleForDirective` case for `'pending_documents'` at `tabTitle.ts`.
  - `onDropEvent` consumer wiring at AgentChatPanel drop handler.
  - PendingDocumentsView component + state-machine port from DocumentIntakeRail.
  - AgentChatPanel drop handler + staged tray + acknowledgment toast.
  - DocumentIntakeRail.tsx deletion (chunk 3 second commit per Sub-Q4 two-step removal).
  - "Pending Documents" Region 7.2 Billing nav item (8 → 9 items).
- **Phase 6.5 retrospective** (Session 13 or later): `docs/07_governance/retrospectives/phase-6.5-retrospective.md`. Inputs from chunk 2:
  - ADR-0010 UI-layer N=4 (potentially N=5 if EC1.β graduates).
  - vitest DOM-environment gap N=3 (chunk 6.2b + Phase 6 retro candidate + chunk 1 → chunk 2 inherits at A1-B).
  - Build-from-scratch tab component first-instance precedent.
  - Path C invocation evaluation outcome (Option A/B/C ratification at brief-drafting + at Session 10 onset).
  - Tab title mapping discipline (UI-shorthand vs agent-prompt-narrative distinct mappings).
  - Sub-Q14/15/16 deferred-pending-usage-evidence re-evaluation if usage signals surface.

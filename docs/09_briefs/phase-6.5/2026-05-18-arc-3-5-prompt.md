I'm opening ARC 3.5 against origin/staging at 46d3d0e. ARC 3 closed
cleanly with two graduations (prediction-grounding, regex-permissive-
matching), one documented deferral (audit-fix-verify-surfaces-banking),
and six first-run observations queued as substrate for this arc.

This arc resolves the six queued observations from ARC 3's first-run
operational exercise of the codify-convention skill. The observations
split naturally into two phases, each its own audit-and-fix arc shape
(same as ARC 2 / ARC 2.5):

  Phase A (this prompt's first work): codify-convention skill refinements
    (observations 1, 2, 3, 4, 5 from ARC 3 close — relatively contained,
    field schema and routing rule changes)

  Phase B (deferred to ARC 3.6 by default): Stage A graduation-check
    refinement (observation 6 — substantial script audit-and-fix arc
    requiring its own grounding against actual convention file footers)

The default end-state for this session is Phase A complete, Phase B
queued as ARC 3.6. Phase B can be folded into this session ONLY if Phase
A closes early and you (the operator) explicitly choose to continue.
Don't auto-proceed.

---

STEP 0: PROMPT RECONCILIATION

This in-context prompt is canonical. If an on-disk prompt exists at
docs/09_briefs/phase-6.5/2026-05-XX-arc-3-5-prompt.md or similar, read
it and reconcile per the same pattern ARC 3 used (in-context wins;
update on-disk to match; commit as a substrate-reconciliation commit
before proceeding).

If no on-disk prompt exists, write this prompt to
docs/09_briefs/phase-6.5/2026-05-XX-arc-3-5-prompt.md with today's
date, and commit before STEP 1.

---

CONTEXT (read these from disk before any operational work)

1. .claude/skills/codify-convention/SKILL.md
   The skill whose first operational exercise surfaced the six
   observations. Phase A modifies this skill's specification.

2. .claude/rules/docs-codification.md
   Path-scoped codification mechanics, including Promoted from: footer.

3. docs/04_engineering/conventions/prediction-grounding.md
   Graduated convention from ARC 3. The "Evaluation basis" footer
   field was added inline here as precedent. Phase A formalizes it.

4. docs/04_engineering/conventions/regex-permissive-matching.md
   Graduated convention from ARC 3. Same "Evaluation basis" precedent.

5. docs/07_governance/friction-journal.md
   Read the ARC 3 close H2 directly. The five banked first-run
   observations live there as primary substrate.

6. docs/09_briefs/phase-6.5/2026-05-17-friction-pattern-detector-design.md
   Spec carries ARC 2 and ARC 2.5 amendments. §Bucket extraction is
   the relevant section. Phase A observation 5 updates this spec.

7. CLAUDE.md
   Operational discipline: verify-from-disk, codify-while-deciding-
   not-while-implementing, stop-on-mismatch.

---

THE SIX QUEUED OBSERVATIONS

Phase A (this session):

1. Formalize "Evaluation basis" field in codify-convention footer
   The field was added inline to both convention files in ARC 3 as
   precedent. The skill's documented footer schema doesn't yet
   include it. Formalize by adding the field to the skill's spec,
   documenting its required content (load-bearing / generalizable /
   stable assessment with one sentence per criterion), and confirming
   both existing conventions match the formalized format.

2. Refine load-bearing criterion (prescriptive vs descriptive sub-shapes)
   Family 1 was prescriptive (imperative verbs, decision points);
   family 2 deferral was descriptive (heads-up, no operator action).
   The skill's load-bearing criterion as currently stated doesn't
   separate them. Refine the criterion in the skill's evaluation
   framework section to explicitly distinguish prescriptive load-
   bearing (drives operator action) from descriptive load-bearing
   (helps understanding). Prescriptive graduates cleanly; descriptive
   should require either elevation to prescriptive (find the actionable
   sub-shape) or DEFER/DISMISS.

3. Document substrate-vs-mechanism precedence for family-tag banking
   When banking an observation under a family tag, the family is
   determined by the *mechanism of the observation*, not by the
   *substrate the observation operated on*. The ARC 2.5 banking of
   regex-permissive-cost-class instance 4 was tagged by substrate
   (regex) when its mechanism (audit-grounding-not-magic-number) was
   actually caveat-prediction-vs-empirical-resolution's family.
   Document this precedence as guidance in the friction-journal
   banking discipline, likely as a sub-section of the journal-entry-
   rules skill or as a new section in the codify-convention skill's
   "Reading the friction-journal for graduation" guidance.

4. Add "update upstream artifacts" step to codify-convention's output spec
   When a convention is graduated, upstream artifacts that contain the
   pattern's discovery provenance (e.g., spec caveats, banked journal
   entries with prose explanations) benefit from a cross-reference to
   the new canonical source. ARC 3 did this manually for both
   graduations; the skill's spec should require it. Add a step to the
   skill's output checklist: "Identify upstream artifacts containing
   pattern discovery provenance; add 'see conventions/<file>.md for
   the discipline' cross-references in those artifacts as part of the
   graduation commit."

5. Update friction-pattern-detector design spec to cross-reference
   new conventions
   The §Bucket extraction section's ARC 2 and ARC 2.5 amendments
   carry discovery provenance for the cost classes. After ARC 3's
   graduations, the canonical discipline lives in conventions/
   regex-permissive-matching.md. Add a cross-reference at the
   appropriate point in §Bucket extraction. This is a one-time fix
   for an existing spec; observation 4 ensures this happens
   automatically for future graduations.

Phase B (default deferred to ARC 3.6):

6. Refine Stage A graduation check to distinguish codification from
   cross-reference
   The Stage A grep in scripts/friction-journal-tally.sh's
   check_graduated_a function searches the family tag string anywhere
   in CLAUDE.md, conventions/, and .claude/skills/. The grep can't
   distinguish a Promoted-from footer (codification) from a cross-
   reference body (mention). ARC 3's family 2 deferral surfaced this
   as a Y(A) false-positive when prediction-grounding.md's Cross-
   references section mentioned audit-fix-verify-surfaces-banking as
   the paired family.

   This is a substantial script audit-and-fix arc, not a small
   refinement. It requires:
   - An audit of all existing convention files for actual footer
     format consistency (do all of them use "Promoted from:" verbatim,
     or are there variants?)
   - A design decision on the discriminator shape (footer-line-only?
     specific anchor pattern? structured metadata block?)
   - The fix itself with regression checks against ARC 3's three
     decided families
   - Update to the family 2 deferral entry once Stage A correctly
     reports graduated=N for it

   Phase B is deferred to ARC 3.6 by default. Do not start Phase B in
   this session unless Phase A closes early and the operator explicitly
   chooses to fold it in. The default end-state is Phase A complete,
   ARC 3.6 queued.

---

STEP 1: SUBSTRATE READING

Before any operational work, read the five Phase A observations as
they were banked in the friction-journal (ARC 3 close H2). The
summaries in this prompt are compressions; the banked entries carry
the actual reasoning that produced them. Phase A's changes adapt the
skill to those specific findings; reading the findings directly
prevents Phase A from drifting into adapting to *summaries of* the
findings.

Specifically, for each of observations 1-5:
  - Read the friction-journal entry verbatim
  - Note any nuance the prompt's compression dropped
  - Surface any nuance that would change the Phase A change's shape

If any banked observation surfaces nuance the prompt didn't anticipate,
STOP and surface before proceeding. Phase A's changes should match the
banked findings, not the summarized ones.

---

STEP 2: PHASE A AUDIT

For each of the five Phase A observations, the change shape is mostly
self-evident from the observation itself. But before applying, audit
two things:

(a) Audit the codify-convention skill's current spec for the relevant
    sections. The changes are amendments to existing sections, not
    new sections. Read what's there before deciding what to change.

(b) Audit both graduated convention files (prediction-grounding.md,
    regex-permissive-matching.md) for consistency with the formalized
    "Evaluation basis" field. If they're already consistent with the
    formalization, observation 1 is essentially a skill-spec update.
    If they're inconsistent, observation 1 also requires updating
    those files to match.

After the audit, surface findings for review:
  - What's in the skill's current spec for each affected section?
  - Are the two existing conventions consistent with the formalization,
    or do they need cleanup?
  - For observation 3 (substrate-vs-mechanism precedence): where does
    this guidance most naturally live — journal-entry-rules skill, or
    codify-convention skill, or a new sub-section in CLAUDE.md? The
    answer depends on what the existing surfaces look like.

Wait for review before proceeding to STEP 3.

---

STEP 3: PHASE A APPLICATION

After audit findings are surfaced and reviewed, apply the five Phase A
changes one at a time, surfacing each for approval. Same per-change
discipline as ARC 3's per-family rule. Don't batch.

For each change:
  - Apply the change to the relevant file(s)
  - Re-read the affected sections to verify the change landed cleanly
  - Verify the change doesn't conflict with other Phase A changes
    already applied (the changes touch related sections; later changes
    may need to adapt to earlier ones)
  - Commit with a message describing the change and referencing the
    originating observation

Commit message template:
  refactor(skill): formalize <element> in codify-convention
  
  Originating observation: friction-journal line <N>, banked at ARC 3
  close. <One-sentence change summary>.

(or `docs(brief):` if the change is to the friction-pattern-detector
spec rather than the skill.)

---

STEP 4: VERIFICATION

After all five Phase A changes are applied, run two verifications:

(a) Re-read the codify-convention skill end-to-end. Does it still read
    as a coherent skill, or have the amendments produced internal
    inconsistencies? Phase A's changes touch multiple sections; the
    aggregate effect should be coherent.

(b) Re-graduate one of ARC 3's families *as a dry run* against the
    refined skill. Pick prediction-grounding.md (the cleaner of the
    two). Walk through the skill's instructions as if codifying from
    scratch. Verify that:
    - The Evaluation basis field is now part of the documented schema
    - The prescriptive/descriptive distinction is documented in the
      evaluation criteria
    - The upstream-artifacts step is in the output checklist
    - The substrate-vs-mechanism precedence is findable

If the dry run surfaces gaps (e.g., the refined skill still has a
section that doesn't reflect Phase A's changes), STOP and address
before considering Phase A complete.

---

STEP 5: PHASE A CLOSE OBSERVATIONS

Bank in the friction-journal under today's H2:

(a) Any unanticipated findings surfaced during Phase A application.
    Phase A is an audit-fix-verify arc shape — per
    audit-fix-verify-surfaces-banking (deferred, N=3), unanticipated
    banking is plausible. If it surfaces, this would be N=4 of that
    family. The deferral's re-evaluation trigger was "a diverse 4th
    instance from operationally distinct context." Phase A operates
    on the codify-convention skill (not the friction-pattern-
    detector), which is operationally distinct from the prior three
    instances. A Phase A unanticipated finding would satisfy the
    deferral's re-evaluation trigger.

    If this fires, surface the finding *and* the trigger satisfaction
    for review. Don't auto-act on the trigger; the re-evaluation is
    its own decision separate from Phase A's banking.

(b) The Phase A → Phase B split itself, banked as N=1 of "arcs split
    by scope into phased sub-arcs when surfaced findings exceed
    original arc's scope." This is a new pattern: ARC 2 surfaced
    ARC 2.5; ARC 3 surfaced ARC 3.5 (and 3.5 surfaces 3.6); each
    split followed the same principle of "audit-and-fix work earns
    its own arc rather than tacking onto another." Could be the
    same family as audit-fix-verify-surfaces-banking (the recursion
    *is* the surfacing) or a sibling pattern about arc-scoping
    discipline. Bank for future N counting; don't pre-commit to
    family taxonomy.

(c) ARC 3.6 explicit queue handoff: confirm Phase B's six observation
    remains queued, with a note that this session's work didn't
    touch it. The friction-journal serves as the durable substrate
    for the next session to pick up.

---

STEP 6: PHASE A COMMIT AND PUSH DECISION

Phase A commits accumulate per STEP 3. The Phase A close observations
(STEP 5) commit as a final batch.

Push decision:
  - If the dry run in STEP 4 passed cleanly and STEP 5 banking is
    coherent, push is reasonable. Phase A's changes are self-
    contained (skill refinements + spec cross-reference) and don't
    interact with origin/staging in ways requiring co-ordination.
  - If Phase A surfaced findings significant enough to warrant
    review distance (e.g., the audit-fix-verify-surfaces-banking
    trigger fired), DEFER push and surface for direction.

Same no-push-half-resolved-state rule applies. Don't push if any
queued state is incomplete or known-wrong.

---

STEP 7: ARC 3.6 PREPARATION (optional)

If Phase A closed early and you want to continue with Phase B in this
session, surface the decision explicitly:
  - "Phase A is complete. I'm choosing to fold Phase B (Stage A
    refinement) into this session because [reasoning]."

The default is don't. Phase B is a substantial audit-and-fix arc with
its own grounding requirement. Folding it in is reasonable only if:
  - Phase A took less time/context than expected, AND
  - The Stage A finding's context is fresh from ARC 3 close, AND
  - You explicitly choose to (not "we have time, may as well")

If you proceed to Phase B in this session, treat it as ARC 3.6
substantively — same audit-then-fix-then-verify discipline as ARC 2.5,
but applied to Stage A. The Stage A audit is non-trivial: examining
all existing convention files' footer formats, deciding the
discriminator shape, applying the fix, regression-testing against
ARC 3's three decided families.

If you don't proceed, write the ARC 3.6 prompt for the next session
as the final artifact of this one.

---

THROUGHOUT

- Verify-from-disk at every gate.
- Stop on sanity-check mismatches.
- Don't bundle decisions — surface each Phase A change for approval
  separately.
- Codify-while-deciding-not-while-implementing. Observations during
  ARC 3.5 get banked, not acted on inline.
- Phase B is deferred by default. Don't fold in without explicit
  choice and stated reasoning.

Start with STEP 0 (prompt reconciliation), then STEP 1 (substrate
reading).

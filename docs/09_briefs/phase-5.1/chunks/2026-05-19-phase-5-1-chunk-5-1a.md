# Phase 5.1 Chunk 5.1a Brief — INV-DOC-001 graduation + vendor_credits β substrate

- **Date:** 2026-05-19
- **Phase:** Phase 5.1 (amendment cycle per Phase 2.5 precedent; first of two chunks per Sub-Q1 1.γ-i decomposition)
- **Chunk:** 5.1a of 2 (5.1a substrate amendments; 5.1b paymentService greenfield introduction)
- **Status:** brief-drafting (Session 19) → implementation (Session 20+) per Sub-Q5 5.1a → 5.1b sequenced ordering
- **Session shape:** single chunk-brief drafting per Phase 6.5 chunk 1 brief precedent; chunk 5.1b brief drafts post-5.1a-implementation
- **Cycle-close ratification:** Phase 5.1 scope-lock cycle closed substantively at Round 4 (`8631a5d`); Rounds 1-4 artifacts at `docs/09_briefs/phase-5.1/` constitute cycle-close evidence basis. This brief operationalizes locked dispositions per Phase 6.5 chunk-brief precedent.
- **Baseline at Session 19 onset:** HEAD = `8631a5d` on staging (local; 4 commits ahead of `origin/staging = 3ca0314`); `pnpm agent:validate` 26/26 green; `pnpm test` 1148/1148 claimed at directive baseline.

## 1. Preamble

### 1.1 Brief context

This brief operationalizes Phase 5.1 chunk 5.1a at implementation-task grain. Phase 5.1 scope-lock cycle substantively closed at Round 4 (`8631a5d`) with 14 sub-questions + 8 sub-decisions LOCKED across Rounds 1-4. Round 4 §5.1 shipped per-chunk acceptance criteria; this brief converts those criteria into a numbered task list with per-task acceptance criteria, dependency annotations, and concrete partial-information value picks.

**Inheritance posture.** Round 4 artifact (`8631a5d`) §5.1 is authoritative for *what* chunk 5.1a ships (INV-DOC-001 graduation atomic single commit + vendor_credits β substrate-only tables + ADR-0016 third amendment cycle). This brief is authoritative for *how* chunk 5.1a builds it (task ordering; migration filename + numbering; schema extension shape; integration test fixture design; commit-body shape). The Round 4 artifact wins on scope conflicts; this brief wins on implementation-task ordering and partial-information value picks.

### 1.2 Session-onset divergence absorption

Two divergences from Session 19 directive surfaced at chunk 5.1a brief drafting verify-from-disk pass:

**Divergence (α) — Push state mismatch.** Directive claimed "Phase 5.1 scope-lock cycle 5 commits pushed to origin/staging post-Round-4." Actual: `origin/staging = 3ca0314`; local HEAD = `8631a5d` is 4 commits ahead. Push did not happen. Banked as **candidate (c) pattern instance N=20** at session-prompt-authoring grain. Brief drafting proceeds off local HEAD; push timing is orthogonal to brief-grade work.

**Divergence (β) — Migration path drift.** Directive §3 "Files created" cited `apps/web/db/migrations/[YYYYMMDD]_vendor_credits_substrate.sql`. Actual migration path: `supabase/migrations/` (recent files: `20240155000000_forwarded_mailbox_substrate.sql`). Banked as **candidate (c) pattern instance N=21** at session-prompt-authoring grain. Brief operationalizes correct path at §3 + §4.

**Updated candidate (c) catalog:** N=19 → **N=21** at Session 19 onset. Session-prompt-authoring sub-grain: N=9 Rounds 1-3 + N=0 Round 4 + N=2 Session 19 = N=11. Mechanism A "discipline internalization" conjecture weakens; Mechanism B "fresh-path-citation density" gains weight per Round 4 close observation. Continued tracking at Phase 5.1 retrospective drafting.

### 1.3 Path C invocation evaluation at brief-drafting grain

Per Phase 5.1 scope-lock cycle Round 4 §4 + F-J-14 third-instance discipline: **prospectively-negative** at chunk 5.1a brief drafting grain. Reactive availability preserved through chunk 5.1a implementation grain per F-J-14 mid-impl-reactive sub-grain.

Brief drafting volume forecast at ~600-900 LOC (per Phase 6.5 chunk 1 brief precedent 623 LOC); chunk 5.1a implementation volume forecast at ~765-1465 LOC per Round 4 §2.2 refined estimate. Both within RI-7 single-session ceiling.

**Named-future-trigger:** if chunk 5.1a implementation (Session 20) surfaces unexpected scope expansion (e.g., ADR-0011 §15 editorial cluster turns substantive instead of editorial-only; ledger_truth_model.md leaf registration surfaces undocumented cross-reference cascade; bill_evidence_override_applied audit backfill surfaces operational-blast-radius beyond ~10K bills), reactive Path C invocation fires per F-J-14 third-instance discipline.

### 1.4 Cross-references

- **Cycle-close artifacts:**
  - Round 4 (`8631a5d`): `docs/09_briefs/phase-5.1/2026-05-19-phase-5-1-scope-lock-cycle-round-4.md` (488 lines) — primary substrate (§5.1 chunk 5.1a acceptance criteria; §6.1 Two Laws scope).
  - Round 3 (`5121569`): sub-decisions + sub-shapes (§3 + §4).
  - Round 2 (`2560ef6`): leans + Refinements 1+2.
  - Round 1 (`72a40bf`): Sub-Q structure + 5 findings.
- **Chunk-brief structural precedent:**
  - Phase 6.5 chunk 1 brief: `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` (623 lines) — §1-§8 inheritance shape.
- **ADR substrates:**
  - ADR-0011 §15 (INV-DOC-001 reservation graduation reference).
  - ADR-0016 §1 + §3 + §5 (vendor_credit cell-validity third amendment cycle).
  - ADR-0015 §7 Scenario C (Sub-Q4-b receipt accepted set basis).
  - ADR-0022 §5 (additive provenance-preserving amendment-block convention).
- **CLAUDE.md codifications:** verify-forward-at-scope-lock cluster; substrate-mod test-staleness review; verify-from-disk-at-non-standard-grain sub-grain catalog; operational-flex collapse heuristic.

This brief seeds `docs/09_briefs/phase-5.1/chunks/` (subfolder created at brief drafting per Phase 6.5 `chunks/` pattern). Naming per Sub-Q6 lock: `YYYY-MM-DD-phase-5-1-chunk-5-1a.md` (phase-5.1 → phase-5-1 hyphenated for filename per Phase 2.5 commit-a/b precedent; chunk-5-1a sub-naming).

## 2. Scope

### 2.1 What chunk 5.1a ships

Single atomic commit per Sub-Q4 Option (i) shipping T3 + T2 + T1 surface-precedence:

**T3 deliverables — governance / spec authorities:**

- **INV-DOC-001 leaf addition** at `docs/02_specs/ledger_truth_model.md` (graduation from reserved-candidate per ADR-0011 §15 to canonical leaf).
- **ADR-0016 third amendment cycle** at `docs/07_governance/adr/0016-document-relationship-graph.md` (additive provenance-preserving amendment block after line 1983; cites Phase 2.5 Commit A reservation + Phase 5.1 current ratification per ADR-0022 §5).

**T2 deliverables — derived authorities:**

- **Rollup row** added to `docs/02_specs/invariants.md` (row 21; bidirectional reachability statement updates "20 distinct" → "21 distinct" + Layer 2 count 6 → 7).
- **Audit-row** added to `docs/06_audit/control_matrix.md` Layer 2 section (currently 6 rows → 7 rows).
- **Bidirectional reachability annotation** in `apps/web/src/services/spend/billService.ts` header comment block + at billService.post() function header.

**T1 deliverables — code substrate:**

- **billService.post() Layer 2 enforcement code** inline per Sub-Q4.5 4.5.α (no evidence-service introduction; `apps/web/src/services/evidence/` stays `.gitkeep`-only).
- **PostBillInputSchema extension** at `apps/web/src/shared/schemas/spend/bill.schema.ts`: `primary_document_id?: string` (optional uuid) + `override_evidence_completeness: z.boolean().optional().default(false)` per Sub-Q4-shape-2 lock (Zod-level default mirrors Layer 1 NOT NULL DEFAULT false from migration `20240138000000:172`).
- **`EVIDENCE_INCOMPLETE` ServiceErrorCode extension** at `apps/web/src/services/errors/ServiceError.ts` per Sub-Q4-shape-1 lock.
- **Integration test** at `apps/web/tests/integration/billEvidenceCompleteness.test.ts` (new flat-path file per Sub-Q4-shape-3 lock; 3 fixtures: positive / override / failure).
- **billService.post integration test extension** (adjacency-path coverage if existing `billPostBill.test.ts` requires Sub-Q4-shape-2 default-behavior regression test).

**vendor_credits β substrate (Sub-Q3 β):**

- **Migration** at `supabase/migrations/20240156000000_phase_5_1_vendor_credits_substrate.sql` (next sequential migration number per latest `20240155000000`). Ships:
  - `vendor_credits` table (mirror `vendor_prepayments` substrate shape per migration `20240138000000`).
  - `vendor_credit_applications` table (mirror `vendor_prepayment_applications` substrate shape).
  - RLS policies on both tables (mirror existing vendor_prepayments RLS).
  - `linked_entity_type` CHECK constraint broadened from 6-value v1-active to 8-value v1-active (adding `vendor_credit` + `vendor_credit_application`).
  - Zod schema widen at `LinkedEntityTypeSchema` boundary (per chunk-2-Phase-2 lesson: Layer 1 broadening implies Zod schema broadening).
  - TypeScript types regeneration via `pnpm db:types`.
  - Migration top-comment naming deferred-consumer disposition + named-future-trigger (post-v1 vendorCreditService consumer chunk activates T4 + T6 dispatcher slots).
- **Sub-Q4-d 4-d.γ backfill** folded into same migration:
  - `UPDATE bills SET override_evidence_completeness=true WHERE bill_id NOT IN (SELECT linked_entity_id FROM source_document_links WHERE linked_entity_type='bill' AND link_role IN ('primary_invoice', 'receipt') AND link_status='created')`.
  - `bill_evidence_override_applied` audit row insertion per UPDATEd bill (mechanism: batch INSERT into `audit_log`; brief operationalizes at Task 9).

### 2.2 What chunk 5.1a does NOT ship

Deferred to chunk 5.1b (paymentService):

- `paymentService.ts` greenfield introduction.
- `T2_new_payment` branch addition to `DispatchTriggerInputSchema` discriminated union (currently 5 branches; 5 → 6 at chunk 5.1b).
- `billService.recordPayment` refactor to delegate payment-flow primitive to paymentService.record() per Sub-Q2 2.β.

Deferred to post-v1:

- `vendorCreditService.ts` consumer surface (Sub-Q3 β; vendor_credits operational rollout post-v1 contingent per Phase 5 retro §6:404).
- T4 + T6 dispatcher activations (Refinement 2 from Round 2; activate at vendorCreditService consumer chunk post-v1).
- `controller_override_memo` link_role PDF attachment (ADR-0016 §2:421-427; v1 captures override via boolean flag + audit row only).

Banked for Phase 5.1 retrospective drafting (not chunk 5.1a scope):

- ADR-0011 §15 editorial cluster (2 candidates per Round 4 §7.2):
  - "Adjacent commit paths" framing over-broad (Round 2 Refinement B).
  - "primary_invoice (or 'primary')" cites non-existent enum value (Round 3 §3.b).
- Phase 5 retro §6:404 vendor_credits assertion correction (Round 1 §2.4 finding 1).
- Candidate (c) catalog evolution observations (N=21 at Session 19; retrospective-authoring fourth-grain + ADR-authoring fifth-grain candidates).

## 3. Substrate touchpoints

Verify-from-disk at Session 20 implementation before consuming any line/path reference below. All counts/paths confirmed at Session 19 brief-drafting against HEAD `8631a5d`.

### 3.1 Files modified

- **`apps/web/src/services/spend/billService.ts`** (946 lines pre-chunk-5.1a). Edits: insert Layer 2 enforcement check at post() function between Zod validation (line ~276) and bill row INSERT (line ~333). Insert optional documentLinkService.create() call after bill row INSERT, conditional on `primary_document_id` provided. Update header comment block to add `INV-DOC-001 Layer 2: bill commit requires primary_document_id OR override_evidence_completeness=true (see ledger_truth_model.md leaf + ADR-0011 §15 reservation graduation)` annotation. Update post() function header doc-comment to mention INV-DOC-001. Touch grain: ~30-60 LOC modified.

- **`apps/web/src/shared/schemas/spend/bill.schema.ts`** (132 lines pre-chunk-5.1a). Edit: extend `PostBillInputSchema` at line 72-89 with two optional fields (`primary_document_id` + `override_evidence_completeness`). Update file-top header comment if applicable. Touch grain: ~5-10 LOC added.

- **`apps/web/src/services/errors/ServiceError.ts`** (119 lines pre-chunk-5.1a). Edit: add `'EVIDENCE_INCOMPLETE'` to ServiceErrorCode union (insert before `'RATE_LIMITED'` at line ~108 OR after `'EXCEPTION_ALREADY_CANCELLED'` at line ~102 per Document core grouping convention). Touch grain: ~3-5 LOC added.

- **`docs/02_specs/ledger_truth_model.md`** (5087 lines pre-chunk-5.1a). Edit: add INV-DOC-001 leaf at end of Layer 2 section (template per INV-LEDGER-001 leaf at line 179-283 OR Layer 2 examples at line 2304/2444/3088). Touch grain: ~80-150 LOC added.

- **`docs/02_specs/invariants.md`** (163 lines pre-chunk-5.1a). Edit: add row 21 (INV-DOC-001) to the 20-invariants table at lines 58-79. Update bidirectional reachability statement at lines 33-35 ("20 distinct" → "21 distinct"; "6 Layer 2" → "7 Layer 2"). Touch grain: ~3-5 LOC modified.

- **`docs/06_audit/control_matrix.md`** (300 lines pre-chunk-5.1a). Edit: add INV-DOC-001 audit row at end of Layer 2 section (after line ~206 per existing structure). Touch grain: ~20-40 LOC added.

- **`docs/07_governance/adr/0016-document-relationship-graph.md`** (1983 lines pre-chunk-5.1a). Edit: append third amendment block after line 1983 (after existing Phase 2.5 Commit A amendment at line 1751 + Phase 4 retrospective amendment at line 1869). Amendment content includes §1 v1-active 6 → 8 + §3 pair-validity matrix cells + §5 cascade behavior amendments for vendor_credit + vendor_credit_application rows. Touch grain: ~100-200 LOC added.

- **`apps/web/src/shared/schemas/document-platform/sourceDocumentLink.schema.ts`** (verify path + line count at impl-onset). Edit: extend `LinkedEntityTypeSchema` Zod enum to include `vendor_credit` + `vendor_credit_application` (per chunk-2-Phase-2 lesson: Layer 1 broadening implies Zod schema broadening). Touch grain: ~3-5 LOC modified.

- **`apps/web/src/db/types.ts`** (auto-generated). Regenerated via `pnpm db:types` post-migration. Touch grain: ~50-150 LOC regenerated.

### 3.2 Files created

- **`supabase/migrations/20240156000000_phase_5_1_vendor_credits_substrate.sql`** (new; ~150-300 LOC estimated per Round 2 Finding 3). Ships:
  - `vendor_credits` table + `vendor_credit_applications` table (substrate-only).
  - RLS policies (mirror vendor_prepayments).
  - `linked_entity_type` CHECK constraint broadening 6 → 8.
  - Sub-Q4-d 4-d.γ backfill (UPDATE bills SET override_evidence_completeness=true + INSERT audit_log rows for backfilled bills).
  - Top-comment naming deferred-consumer disposition + named-future-trigger.

- **`apps/web/tests/integration/billEvidenceCompleteness.test.ts`** (new; ~150-300 LOC estimated). 3 fixtures per Sub-Q4-shape-3 lock:
  - Fixture 1 (positive path): bill post with `primary_document_id` succeeds + `source_document_links` row created with `link_role='primary_invoice'`.
  - Fixture 2 (override path): bill post with `override_evidence_completeness=true` (no `primary_document_id`) succeeds without source_document_links row + `bills.override_evidence_completeness=true` persisted.
  - Fixture 3 (failure path): bill post without `primary_document_id` AND without override throws `ServiceError('EVIDENCE_INCOMPLETE', ...)`; no rows committed.

### 3.3 Files NOT modified at chunk 5.1a

- **`apps/web/src/services/spend/billService.ts`** `recordPayment` function (lines ~515-744) — chunk 5.1b territory per Sub-Q2 2.β.
- **`apps/web/src/shared/schemas/document-platform/documentRelationshipCandidate.schema.ts`** `DispatchTriggerInputSchema` (line 418+) — chunk 5.1b T2 branch addition.
- **paymentService-related files** (all not-yet-existing; chunk 5.1b).
- **`apps/web/src/services/evidence/`** — `.gitkeep`-only; preserved at chunk 5.1a per Sub-Q4.5 4.5.α.
- **`docs/07_governance/adr/0011-document-platform.md`** §15 — editorial cluster (2 candidates) deferred to Phase 5.1 retrospective drafting.

### 3.4 Service layer scope

- **Layer 2 enforcement at billService.post() only** per Sub-Q4 Option (i) + Round 4 §3.b.v "adjacent commit paths" verify-from-disk reduction. No other service paths modified.

## 4. Tasks

Strict-sequence: Tasks 1-2 (T2 documentation) → Tasks 4-5 (schema/type substrate) → Task 3 (Layer 2 enforcement; depends on Tasks 4-5) → Tasks 6-7 (test authoring; depends on Task 3) → Task 8 (vendor_credits migration; substrate-independent; can ship anytime) → Task 9 (audit backfill; may fold into Task 8) → Task 10 (ADR-0016 amendment; substrate-independent) → Task 11 (cross-task verification + commit ceremony). See §9 for parallelization options.

### Task 1 — ledger_truth_model.md INV-DOC-001 leaf registration

Add INV-DOC-001 leaf at end of Layer 2 section in `docs/02_specs/ledger_truth_model.md`.

**Files:**

- Modify: `docs/02_specs/ledger_truth_model.md`

**Implementation pattern:**

- Insertion site: after INV-AUDIT-001 leaf (last existing Layer 2 leaf at line ~3088+), before Phase 2 Reserved Invariants subsection.
- Leaf format per INV-LEDGER-001 template (line 179-283) or Layer 2 examples (INV-AUTH-001 / INV-SERVICE-001 / INV-AUDIT-001):
  - `### INV-DOC-001 — Evidence completeness for committed bills (Layer 2)`
  - **Invariant.** Statement of the rule (per ADR-0011 §15:848-850).
  - **Enforcement.** Layer 2 service-layer check at billService.post(); citation of code site.
  - **Why Layer 2.** Substrate-now-enforcement-later rationale; substrate from ADR-0011 §15 Layer pin.
  - **Interaction with the service layer.** PostBillInputSchema extension + override flag interaction + same-transaction documentLinkService.create() composition.
  - **Service-layer backstop.** None at v1; Phase 2+ reporting backstop named-future-trigger.
  - **Category A floor test.** Not applicable at Phase 5.1.
  - **Referenced by.** ADR-0011 §15 (reservation graduation source); ADR-0015 (override mechanism owner); ADR-0016 §6 (primary_invoice link_role canonical pair).

**Acceptance criteria:**

- INV-DOC-001 leaf added at end of Layer 2 section per template.
- All template sections populated (Invariant + Enforcement + Why Layer 2 + Interaction + Service-layer backstop + Category A + Referenced by).
- Cross-references to ADR-0011 §15 + ADR-0016 §6 + ADR-0015 included.
- Leaf format conforms to existing Layer 2 examples (INV-AUTH-001 / INV-SERVICE-001 / INV-AUDIT-001 pattern matching).

**Dependency:** none (T2 documentation; substrate-independent).

**Parallelizable:** yes (with Task 2; both T2 documentation).

### Task 2 — invariants.md rollup row + count update + control_matrix.md audit row

Atomic update across both files. Bidirectional reachability count statement updates 20 → 21.

**Files:**

- Modify: `docs/02_specs/invariants.md` (row 21 + bidirectional reachability statement).
- Modify: `docs/06_audit/control_matrix.md` (Layer 2 audit row addition).

**Implementation pattern:**

- **`invariants.md` row 21 addition** (after row 20 INV-AUDIT-001 at line 79):
  - Pattern per existing rows (15-20 are Layer 2): `| 21 | INV-DOC-001 | 2 | Bills require attached primary document | TypeScript service function (Zod schema + business-logic check) | [leaf](ledger_truth_model.md#inv-doc-001--evidence-completeness-for-committed-bills) | `src/services/spend/billService.ts` (function `post`) |`
- **`invariants.md` bidirectional reachability statement update** at lines 33-35:
  - "20 distinct INV-IDs documented in `docs/02_specs/ledger_truth_model.md` (14 Layer 1a, 6 Layer 2, 0 Layer 1b)" → "21 distinct INV-IDs documented in `docs/02_specs/ledger_truth_model.md` (14 Layer 1a, **7 Layer 2**, 0 Layer 1b)".
  - Update `20 distinct INV-IDs annotated in code` to `21 distinct INV-IDs annotated in code`.
- **`control_matrix.md` Layer 2 row addition** (after existing 6 Layer 2 audit-row entries at line ~206+):
  - Audit-row structure per existing Layer 2 entries: section header (`### INV-DOC-001 — Evidence completeness for committed bills (Layer 2)`) + Spec leaf + Test coverage + Enforcement mechanism + Non-bypassability claim.
  - Test coverage cite: `apps/web/tests/integration/billEvidenceCompleteness.test.ts` (3 fixtures).

**Acceptance criteria:**

- `invariants.md` row 21 added with correct pattern.
- Bidirectional reachability statement updates to 21 distinct + 7 Layer 2.
- `control_matrix.md` Layer 2 audit-row added per existing structure pattern.
- All three references (leaf cite + test cite + code cite) consistent across the three files.
- Symmetric-difference verification command (lines 44-47 of invariants.md) returns empty when run after chunk 5.1a close.

**Dependency:** Task 1 (leaf exists; rollup references the leaf).

**Parallelizable:** no (sequential after Task 1; both ship in same commit).

### Task 3 — billService.post() Layer 2 enforcement code

Inline INV-DOC-001 enforcement at billService.post() per Sub-Q4.5 4.5.α. No evidence-service introduction.

**Files:**

- Modify: `apps/web/src/services/spend/billService.ts`

**Implementation pattern:**

- Insertion site for enforcement check: between Zod validation (post-`PostBillInputSchema.parse()`) at line ~276 and `loadVendorOrThrow` at line ~282. Specifically AFTER Zod parse + BEFORE the entity preload block (vendor + fiscal_period + accounts).
- Enforcement check shape:

```typescript
// INV-DOC-001 Layer 2: bill commit requires attached primary document
// OR override_evidence_completeness=true. Per ADR-0011 §15 reservation
// graduation; leaf at ledger_truth_model.md.
if (!parsed.override_evidence_completeness && !parsed.primary_document_id) {
  throw new ServiceError(
    'EVIDENCE_INCOMPLETE',
    `bill commit requires primary_document_id or override_evidence_completeness=true (INV-DOC-001)`,
  );
}
```

- Insertion site for source_document_links insert: after bill row INSERT (line ~333) + after bill_lines insert (line ~370) + before bill-grain audit emission (line ~377). Specifically AFTER bill rows committed + BEFORE recordMutation:

```typescript
// INV-DOC-001 Layer 2 atomic primary attachment: if primary_document_id
// provided, insert source_document_links row in same transaction
// per ADR-0016 §6 (documentLinkService.create() is the canonical
// attachment-creation surface) + ADR-0011 §15 (canonical primary).
if (parsed.primary_document_id) {
  await documentLinkService.create(
    {
      org_id: parsed.org_id,
      source_document_id: parsed.primary_document_id,
      linked_entity_type: 'bill',
      linked_entity_id: insertedBill.bill_id,
      link_role: 'primary_invoice',
    },
    ctx,
  );
}
```

- Bidirectional reachability annotation in file-top header comment block (per existing INV-AP-001 / INV-AP-002 / INV-SERVICE-002 annotation pattern):

```typescript
// INV-DOC-001 Layer 2: bill commit requires primary_document_id OR
// override_evidence_completeness=true (see ledger_truth_model.md leaf
// + ADR-0011 §15 reservation graduation).
```

- Update post() function header doc-comment to mention INV-DOC-001 enforcement.

**Acceptance criteria:**

- Enforcement check fires when neither `primary_document_id` nor `override_evidence_completeness=true` provided; throws `ServiceError('EVIDENCE_INCOMPLETE', ...)`.
- documentLinkService.create() called atomically in same transaction when `primary_document_id` provided.
- documentLinkService.create() NOT called when override path taken.
- Bidirectional reachability annotation added per existing pattern.
- File-top header comment + post() function header doc-comment updated.

**Dependency:** Tasks 4 + 5 (PostBillInputSchema extension + ServiceErrorCode union extension must be in place).

**Parallelizable:** no (sequential after Tasks 4-5).

### Task 4 — PostBillInputSchema extension

Add two optional fields per Sub-Q4-shape-2 lock.

**Files:**

- Modify: `apps/web/src/shared/schemas/spend/bill.schema.ts`

**Implementation pattern:**

- Edit `PostBillInputSchema` at lines 72-89 of `bill.schema.ts`. Add two fields after `ap_control_account_id` (line 88):

```typescript
// INV-DOC-001 enforcement inputs (Phase 5.1):
primary_document_id: z.string().uuid().optional(),
override_evidence_completeness: z.boolean().optional().default(false),
```

- Zod-level default for `override_evidence_completeness` mirrors Layer 1 column default at migration `20240138000000:172` (NOT NULL DEFAULT false).
- `primary_document_id` optional (no default; undefined when not provided).
- `PostBillInput` and `PostBillInputRaw` types regenerate automatically via `z.infer<>` and `z.input<>` at lines 90-91.

**Acceptance criteria:**

- Two optional fields added to PostBillInputSchema.
- `primary_document_id: z.string().uuid().optional()` (uuid format validation).
- `override_evidence_completeness: z.boolean().optional().default(false)` (Zod-level default).
- TypeScript types `PostBillInput` + `PostBillInputRaw` reflect the new shape.
- Existing 18-field schema preserved; no field renames or removals.

**Dependency:** none (substrate-independent; T2 schema work).

**Parallelizable:** yes (with Task 5; both schema/type substrate).

### Task 5 — ServiceErrorCode EVIDENCE_INCOMPLETE extension

Add typed code per Sub-Q4-shape-1 lock.

**Files:**

- Modify: `apps/web/src/services/errors/ServiceError.ts`

**Implementation pattern:**

- Add `'EVIDENCE_INCOMPLETE'` to ServiceErrorCode union. Section placement: after `'EXCEPTION_ALREADY_CANCELLED'` (line 102; Phase 4 chunk 3 Document core grouping) OR add new "Document core (Phase 5.1)" subsection before `'RATE_LIMITED'` (line 108).
- Recommended placement (cleaner per existing Document core grouping):

```typescript
  // Document core (Phase 5.1 chunk 5.1a) — INV-DOC-001 evidence-
  // completeness enforcement at billService.post(); fired when neither
  // primary_document_id nor override_evidence_completeness=true provided.
  | 'EVIDENCE_INCOMPLETE'
```

- Section comment naming Phase 5.1 chunk 5.1a + INV-DOC-001 source.

**Acceptance criteria:**

- `'EVIDENCE_INCOMPLETE'` added to ServiceErrorCode union.
- Section placement consistent with existing Document core grouping convention.
- Section comment cites Phase 5.1 chunk 5.1a + INV-DOC-001 source.
- TypeScript typecheck green post-change.

**Dependency:** none (substrate-independent; T2 type substrate).

**Parallelizable:** yes (with Task 4; both schema/type substrate).

### Task 6 — billEvidenceCompleteness.test.ts (3 fixtures)

New integration test file per Sub-Q4-shape-3 lock at flat path.

**Files:**

- Create: `apps/web/tests/integration/billEvidenceCompleteness.test.ts`

**Implementation pattern:**

- File structure per existing bill[Action].test.ts convention (e.g., `billPostBill.test.ts` as closest analog).
- Test file header per existing bill integration test convention:
  - License/attribution comment.
  - `describe('INV-DOC-001 enforcement at billService.post()', () => { ... })` outer block.
- Three test fixtures:

```typescript
it('positive path: bill post with primary_document_id succeeds + creates source_document_links row', async () => {
  // Setup: org + vendor + fiscal_period + accounts + source_document
  // Act: billService.post({ ...inputs, primary_document_id: <uuid> })
  // Assert:
  //   - bill row created with override_evidence_completeness=false (Layer 1 default)
  //   - bill_lines created
  //   - JE posted via journalEntryService
  //   - source_document_links row created with linked_entity_type='bill' +
  //     link_role='primary_invoice' + linked_entity_id=<bill_id>
  //   - bill_evidence_override_applied audit row NOT created
});

it('override path: bill post with override_evidence_completeness=true succeeds without primary_document_id', async () => {
  // Setup: org + vendor + fiscal_period + accounts
  // Act: billService.post({ ...inputs, override_evidence_completeness: true })
  // Assert:
  //   - bill row created with override_evidence_completeness=true
  //   - bill_lines created
  //   - JE posted
  //   - source_document_links row NOT created
});

it('failure path: bill post without primary_document_id and without override throws EVIDENCE_INCOMPLETE', async () => {
  // Setup: org + vendor + fiscal_period + accounts
  // Act: billService.post({ ...inputs })  // neither primary_document_id nor override
  // Assert:
  //   - ServiceError thrown with code='EVIDENCE_INCOMPLETE'
  //   - bills table: NO new row committed
  //   - source_document_links: NO row committed
  //   - audit_log: NO row created
});
```

- Test setup utilities: mirror existing `billPostBill.test.ts` fixture conventions (org seed; vendor seed; fiscal_period seed; account seeds; trace_id + ctx assembly per service-architecture skill canonical pattern).

**Acceptance criteria:**

- 3 fixtures shipped: positive path + override path + failure path.
- File at `apps/web/tests/integration/billEvidenceCompleteness.test.ts` (flat path; matches existing bill[Action].test.ts convention).
- Each fixture asserts state on all relevant tables (bills, bill_lines, source_document_links, audit_log).
- Failure path asserts atomicity: no rows committed when enforcement throws.
- Tests integrate with existing test infrastructure (no new env vars; no new DB setup; no mock layer).

**Dependency:** Tasks 3 + 4 + 5 (enforcement code + schema + ServiceErrorCode must be in place).

**Parallelizable:** yes (with Task 7; both test authoring).

### Task 7 — billService.post.integration.test.ts adjacency-path additions

Existing test file extension if regression coverage required.

**Files:**

- Verify-from-disk at impl-onset: `apps/web/tests/integration/billPostBill.test.ts` (or analogous existing test file).
- Modify (if existing tests rely on old PostBillInputSchema 18-field shape without override-flag consideration).

**Implementation pattern:**

- Verify-from-disk at impl-onset: existing billPostBill.test.ts may already cover default-behavior cases without override flag. Sub-Q4-shape-2 Zod-level default mirrors Layer 1 default; downstream behavior should be unchanged for existing tests (override_evidence_completeness defaults to false; existing tests pass primary attachment OR are intentionally testing failure paths).
- Adjacency additions if needed:
  - Verify no regression on existing positive paths (bills with primary attachments pass through).
  - Add coverage for override flag interaction if not already covered.
- May be NO-OP if existing tests are robust to schema extension.

**Acceptance criteria:**

- Verify-from-disk on existing billPostBill.test.ts (or analogous) at impl-onset.
- No regression on existing tests (1148/1148 vitest baseline preserved).
- Adjacency-path coverage added if existing tests don't cover override flag interaction.

**Dependency:** Task 3 (enforcement code in place).

**Parallelizable:** yes (with Task 6; both test authoring).

### Task 8 — vendor_credits + vendor_credit_applications migration

Substrate-only tables per Sub-Q3 β; ~150-300 LOC per Round 2 Finding 3.

**Files:**

- Create: `supabase/migrations/20240156000000_phase_5_1_vendor_credits_substrate.sql`

**Implementation pattern:**

- Migration number `20240156000000` (next sequential after latest `20240155000000_forwarded_mailbox_substrate.sql`).
- Filename per existing convention: `[number]_[descriptive_name].sql`.
- Mirror `vendor_prepayments` substrate shape from `20240138000000_phase5_vendor_prepayment_substrate.sql` (272 lines reference):
  - `vendor_credits` table with columns mirroring `vendor_prepayments` (id, org_id, legal_entity_id, vendor_id, ...).
  - `vendor_credit_applications` table mirroring `vendor_prepayment_applications`.
  - RLS policies on both tables (org_id-derived per existing pattern).
  - Indices on (org_id, vendor_id) and (org_id, status) for query patterns.
- Add `linked_entity_type` CHECK constraint broadening from 6-value v1-active to 8-value v1-active. Locate current CHECK constraint at substrate (likely migration `20240147000000_source_document_links_substrate.sql` or analogous; verify-from-disk at impl-onset).
- Top-comment block naming deferred-consumer disposition:

```sql
-- ============================================================
-- Phase 5.1 chunk 5.1a — vendor_credits substrate ratification (β)
-- ============================================================
-- Sub-Q3 β disposition (Phase 5.1 scope-lock cycle Round 2 onset):
-- substrate-tables-only. NO service-layer surface (vendorCreditService.ts
-- not introduced at Phase 5.1); NO consumer wiring. Tables ship with
-- comments naming named-future-trigger.
--
-- Named-future-activation: vendorCreditService consumer chunk post-v1
-- contingent on founder + two real users hitting operational need
-- (Phase 5 retro §6:404 framing). Activates T4 + T6 dispatcher slots
-- reserved at Phase 4 chunk 3 (Refinement 2 from Phase 5.1 Round 2).
--
-- ADR-0010 substrate-now-enforcement-later N=5 instance (Phase 6.5
-- retro ratified N=4; chunk 5.1a adds N=5).
-- ============================================================
```

**Acceptance criteria:**

- Migration filename `20240156000000_phase_5_1_vendor_credits_substrate.sql`.
- `vendor_credits` + `vendor_credit_applications` tables ship with full substrate (columns + RLS + indices).
- Tables mirror `vendor_prepayments` shape per existing precedent.
- `linked_entity_type` CHECK constraint broadens 6 → 8 v1-active.
- Top-comment block names deferred-consumer disposition + ADR-0010 N=5 + named-future-trigger.
- Migration runs cleanly against fresh DB reset (`pnpm db:reset && pnpm db:seed:all`).
- `pnpm db:types` regenerates `apps/web/src/db/types.ts` post-migration.

**Dependency:** none (substrate-independent).

**Parallelizable:** yes (with Tasks 1-2 + Task 10; all substrate-independent).

### Task 9 — bill_evidence_override_applied audit row backfill

Sub-Q4-d 4-d.γ forward + audit row posture; folds into Task 8 migration at impl-onset adjudication.

**Files:**

- Modify: `supabase/migrations/20240156000000_phase_5_1_vendor_credits_substrate.sql` (fold backfill into Task 8 migration).

**Implementation pattern:**

- Backfill SQL appended to Task 8 migration:

```sql
-- ============================================================
-- INV-DOC-001 forward-only enforcement: auto-override flag flip
-- for pre-Phase-5.1 posted bills (Sub-Q4-d 4-d.γ).
-- ============================================================
-- Existing posted bills (pre-Phase-5.1) may lack primary_invoice
-- link rows. Forward-only enforcement applies to chunks post-
-- Phase-5.1; existing bills get auto-override via this migration.
--
-- Per Phase 5.1 scope-lock cycle Round 3 Sub-Q4-d 4-d.γ:
-- forward + audit row backfill.

-- Step 1: auto-override flag flip for pre-Phase-5.1 bills without
-- primary_invoice link.
UPDATE bills
SET override_evidence_completeness = true
WHERE bill_id NOT IN (
  SELECT linked_entity_id
  FROM source_document_links
  WHERE linked_entity_type = 'bill'
    AND link_role IN ('primary_invoice', 'receipt')
    AND link_status = 'created'
);

-- Step 2: audit row insertion per UPDATEd bill.
-- Pattern: batch INSERT into audit_log via SELECT against
-- override-set bills.
INSERT INTO audit_log (
  org_id,
  action,
  entity_type,
  entity_id,
  before_state,
  after_state,
  performed_by,
  trace_id
)
SELECT
  org_id,
  'bill_evidence_override_applied',
  'bill',
  bill_id,
  jsonb_build_object('override_evidence_completeness', false),
  jsonb_build_object('override_evidence_completeness', true),
  '00000000-0000-0000-0000-000000000000'::uuid, -- system user for migration
  gen_random_uuid()::text -- migration trace_id
FROM bills
WHERE override_evidence_completeness = true
  AND bill_id IN (...);  -- subquery to bound to backfilled bills only
```

- Note: `bill_evidence_override_applied` audit action enum value may require enum-extension migration if `audit_action` is a closed enum at DB grain; verify-from-disk at impl-onset. If audit_log uses TEXT column, no enum extension needed.
- Adjudication at impl-onset: split into separate migration `20240157000000_phase_5_1_inv_doc_001_backfill.sql` IF Task 8 migration scope exceeds ~300 LOC OR vendor_credits + backfill have logical separation.

**Acceptance criteria:**

- UPDATE statement auto-overrides pre-Phase-5.1 bills without primary_invoice/receipt link.
- INSERT statement creates audit_log row per UPDATEd bill.
- audit_action enum extended if required (verify-from-disk at impl-onset).
- Migration test fixture asserts backfill correctness:
  - Pre-migration: N existing posted bills without primary links.
  - Post-migration: N bills have `override_evidence_completeness=true` + N audit rows with action='bill_evidence_override_applied'.

**Dependency:** Task 8 (folded into same migration).

**Parallelizable:** N/A (folded into Task 8).

### Task 10 — ADR-0016 third amendment cycle block

Additive provenance-preserving per ADR-0022 §5 + Phase 2.5 Commit B convention.

**Files:**

- Modify: `docs/07_governance/adr/0016-document-relationship-graph.md` (append amendment block after line 1983).

**Implementation pattern:**

- Append new amendment block at file end (after existing Phase 2.5 Commit A amendment at line 1751 + Phase 4 retrospective amendment at line 1869).
- Amendment block structure mirrors existing amendments:

```markdown
## Amendment — Phase 5.1 chunk 5.1a ratification (2026-05-19)

### Substance

[4-6 sub-findings numbered 1-N enumerating:
 1. §1 v1-active subset 6 → 8 (re-add vendor_credit + vendor_credit_application per Phase 5.1 substrate landing per Sub-Q3 β)
 2. §3 pair-validity matrix cell amendments for vendor_credit row (specific cells transition from "R" reserved-post-v1 to "A" active v1)
 3. §3 pair-validity matrix cell amendments for vendor_credit_application row
 4. §5 cascade behavior amendments for vendor_credit + vendor_credit_application rows
 5. Closes/updates field updates per substrate landing]

### Why this amendment

[Phase 5.1 chunk 5.1a substrate ratification per Sub-Q3 β disposition; locked at Phase 5.1 scope-lock cycle Round 2 onset; founder + brainstorming-side ratified post-Round-1 per substrate-side validation]

### Bundling

[Phase 5.1 chunk 5.1a single atomic commit per Sub-Q4 Option (i); ships alongside INV-DOC-001 graduation deliverables]

### Cross-references

- Phase 2.5 Commit A amendment (2026-05-13) at line 1751 — original reservation moving vendor_credit + vendor_credit_application to reserved-post-v1.
- Phase 4 retrospective amendment (2026-05-14) at line 1869 — sibling amendment.
- Phase 5.1 scope-lock cycle artifacts at `docs/09_briefs/phase-5.1/` (Rounds 1-4).

### Note on amendment shape

Additive provenance-preserving per ADR-0022 §5 + Phase 2.5 Commit B convention. NOT restructure-in-place; both Phase 2.5 Commit A amendment AND this Phase 5.1 amendment cite each other.
```

**Acceptance criteria:**

- Amendment block appended at file end per existing amendment block shape.
- Cites both Phase 2.5 Commit A amendment AND Phase 5.1 amendment (additive provenance-preserving).
- All four §1 + §3 + §5 amendments enumerated.
- ADR-0022 §5 + Phase 2.5 Commit B convention citations included.

**Dependency:** none (substrate-independent).

**Parallelizable:** yes (with Tasks 1-2 + Task 8; all substrate-independent).

### Task 11 — Cross-task verification + commit ceremony

Final integration check + validation gates + commit ceremony.

**Files:**

- (No files; verification + commit).

**Pre-commit validation gates:**

- `pnpm test` — expected 1148 (baseline) + new tests (3 fixtures from Task 6 + adjacency from Task 7 + migration test from Task 9) = ~1151-1155.
- `pnpm agent:validate` — expected 26/26 baseline preserved.
- `pnpm typecheck` — expected green.
- `pnpm test:no-hardcoded-urls` — expected green.
- Bidirectional reachability check (per `invariants.md` lines 44-47 reproducible command): expected empty diff (21 distinct INV-IDs leaf-and-annotation symmetric).
- `pnpm db:reset && pnpm db:seed:all` — fresh DB run cleanly; migration applies without errors.

**Commit shape:**

Single commit on staging per Sub-Q4 Option (i) atomic INV-DOC-001 graduation:

```
feat(spend): Phase 5.1 chunk 5.1a — INV-DOC-001 graduation + vendor_credits β substrate

[3-5 paragraphs covering:
 - chunk 5.1a scope (INV-DOC-001 graduation + vendor_credits β substrate)
 - T3 deliverables (leaf + ADR-0016 third amendment)
 - T2 deliverables (rollup + audit row + reachability count update)
 - T1 deliverables (Layer 2 enforcement code + schema extension + ServiceErrorCode + integration tests)
 - vendor_credits β substrate (substrate-only; no consumer wiring; ADR-0010 N=5)
 - rollback posture (Layer 2 reversible; substrate migration reversible; ADR amendments additive)]

Cross-references:
- Phase 5.1 scope-lock cycle Round 4 artifact: docs/09_briefs/phase-5.1/2026-05-19-phase-5-1-scope-lock-cycle-round-4.md (8631a5d)
- Chunk 5.1a brief: docs/09_briefs/phase-5.1/chunks/2026-05-19-phase-5-1-chunk-5-1a.md (this brief's commit SHA — captured at Session 20)
- ADR-0011 §15 (INV-DOC-001 reservation graduation reference)
- ADR-0016 amendment (third amendment cycle; vendor_credit cell-validity)
- ADR-0022 §5 (additive provenance-preserving amendment convention)
```

**Acceptance criteria:**

- All 6 validation gates green.
- Bidirectional reachability check empty.
- Single commit lands on staging at Session 20 close.
- Push readiness three-condition gate met:
  - Condition 1 (test-suite health): 1148/1148 + new tests green.
  - Condition 2 (doc-sync reconciled): invariants.md ↔ control_matrix.md ↔ ledger_truth_model.md ↔ billService.ts annotation all consistent.
  - Condition 3 (governance closeout): chunk-5.1a-close friction-journal entry if any deviations surfaced; no retrospective at chunk close (Phase 5.1 retrospective per Sub-Q7).

**Dependency:** Tasks 1-10 complete.

**Parallelizable:** no (terminal task).

## 5. Operationalization

Per chunk 5.1a Round 3 + Round 4 sub-decision/sub-shape locks (no re-litigation at brief-drafting grain).

### Sub-Q4-a per-bill granularity (Round 3 lock)

INV-DOC-001 fires at billService.post() boundary at per-bill grain. Per-case grain out-of-scope per Layer 2 narrowing (document_case not in v1-active linked_entity_type per ADR-0016 §1).

### Sub-Q4-b primary_invoice + receipt accepted set (Round 3 lock)

Layer 2 enforcement check accepts both `link_role='primary_invoice'` AND `link_role='receipt'` for primary attachment. ADR-0015 §7 Scenario C born-paid honored (receipt qualifies as primary document for born-paid scenarios). ADR-0016 §3 pair-validity matrix `(bill, receipt)` v1-active cell consistent.

**Note on implementation:** Task 3 enforcement check fires when `primary_document_id` provided OR override flag set; the link_role check happens at the source_document_links insert (link_role='primary_invoice' is the default). For the backfill query at Task 9, the WHERE clause accepts BOTH `primary_invoice` AND `receipt` link_roles per Sub-Q4-b.

### Sub-Q4-c post-time-only enforcement (Round 3 lock)

One-shot check at billService.post() boundary; post-time mutations do NOT re-fire enforcement. ADR-0016 §6 post-commit immutability handles cascade (link_status flip on bill reversal; evidence preserved per audit). Named-future-trigger for lifetime-check substrate per ADR-0010.

### Sub-Q4-d 4-d.γ forward + audit row backfill (Round 3 lock)

Backfill at Task 9 (folded into Task 8 migration):
- Forward enforcement at billService.post() for new bills.
- Auto-override flag flip for pre-Phase-5.1 posted bills.
- bill_evidence_override_applied audit row insertion at migration time.
- INV-AUDIT-002 preserved (audit_log append-only; backfilled rows queryable).

### ADR-0016 third amendment additive (Refinement 1 from Round 2)

Task 10 ships additive amendment block citing both Phase 2.5 Commit A AND Phase 5.1 per ADR-0022 §5 + Phase 2.5 Commit B convention. NOT restructure-in-place.

## 6. Cross-task verification + integration testing

### 6.1 Unit test scope

No new unit tests per chunk 5.1a (test coverage concentrated at integration grain per Sub-Q4-shape-3 lock). PostBillInputSchema Zod-level default behavior verified at integration test grain (Task 6 fixtures).

### 6.2 Integration test scope

New integration test file per Task 6:

- `apps/web/tests/integration/billEvidenceCompleteness.test.ts` (3 fixtures: positive / override / failure).

Existing integration test extensions per Task 7 (verify-from-disk at impl-onset).

### 6.3 Migration test scope

Migration tests verify substrate + backfill correctness:

- `vendor_credits` + `vendor_credit_applications` tables created with correct schema.
- RLS policies effective on both tables.
- `linked_entity_type` CHECK constraint broadened correctly.
- Backfill UPDATE + INSERT statements produce expected audit trail.

### 6.4 Floor test preservation

- 1148/1148 vitest preserved.
- 26/26 agent:validate preserved.
- `pnpm typecheck` green.
- `pnpm test:no-hardcoded-urls` green.

### 6.5 Bidirectional reachability verification

Symmetric-difference verification command at chunk 5.1a close per `invariants.md` lines 44-47:

```bash
diff <(grep -oE 'INV-[A-Z]+-[0-9]{3}' docs/02_specs/ledger_truth_model.md | sort -u) \
     <(grep -rho 'INV-[A-Z]\+-[0-9]\+' src/ supabase/migrations/ | sort -u)
```

Expected output: empty (21 distinct INV-IDs leaf-and-annotation symmetric).

### 6.6 Substrate-mod test-staleness review

Per `.claude/rules/migrations.md` discipline (migration broadens CHECK constraints / ENUM membership):

- `linked_entity_type` CHECK 6 → 8 broadening. Audit dependent tests via grep `linked_entity_type` + `LinkedEntityTypeSchema`. Brief drafter surfaces at impl-onset.
- ENUM membership broadening implies Zod schema broadening (chunk-2-Phase-2 lesson). Task 8 includes LinkedEntityTypeSchema widening.

## 7. Two Laws verification scope

Per Round 4 §6.1 inheritance.

### 7.1 INV-SERVICE-001 (every mutating service function wrapped in withInvariants)

billService.post() already wrapped via existing route handler at `apps/web/src/app/api/orgs/[orgId]/bills/route.ts` (verify path + wrap site at impl-onset). Chunk 5.1a changes within wrap boundary; no new wrap site introduced.

vendor_credits migration: substrate-only; no service-layer code; no INV-SERVICE-001 surface introduced.

### 7.2 INV-SERVICE-002 (adminClient discipline)

billService.post() uses adminClient per existing implementation. documentLinkService.create() call inherits caller's transaction context (adminClient-bound). No userClient introduction.

### 7.3 INV-AUTH-001 (every mutating service call authorized via withInvariants(action))

billService.post() route handler invokes `withInvariants(action: 'bill.post')`. Chunk 5.1a changes within authorized boundary.

Migration-level backfill runs at deploy time (not service-layer mutation); migration discipline applies per `.claude/rules/migrations.md`, not service-layer Two Laws. Backfill audit rows attribute to system user `'00000000-0000-0000-0000-000000000000'::uuid` per existing migration pattern.

### 7.4 vendor_credits RLS

Per Task 8 acceptance criteria: vendor_credits + vendor_credit_applications RLS policies mirror existing vendor_prepayments table per existing pattern. Brief drafter verifies-from-disk at impl-onset (vendor_prepayments RLS policy enumeration as reference).

### 7.5 Two Laws floor tests preserved

- `serviceMiddlewareAuthorization.test.ts` (Category A floor — INV-AUTH-001) preserved.
- `crossOrgRlsIsolation.test.ts` (Category A floor — INV-RLS-001) preserved.
- All 5 Category A floor tests green at 26/26 baseline.

## 8. Substrate touchpoint risks + mitigations

### Risk 1 — INV-DOC-001 leaf format conformance

**Risk:** ledger_truth_model.md existing leaves' format may have implicit conventions; chunk 5.1a leaf MUST conform.

**Mitigation:** Task 1 acceptance criteria requires leaf format to inherit canonical shape from existing Layer 2 examples (INV-AUTH-001 / INV-SERVICE-001 / INV-AUDIT-001). Brief drafter verifies-from-disk at session-onset + enumerates existing leaves' format pattern; chunk 5.1a leaf conforms.

### Risk 2 — ADR-0016 amendment block additive shape

**Risk:** Restructure-in-place accidentally absorbs Phase 2.5 Commit A amendment.

**Mitigation:** Task 10 acceptance criteria requires explicit additive amendment block per ADR-0022 §5 + Phase 2.5 Commit B convention; brief drafter cites both amendment dates + provenance-preserving discipline at Task 10 implementation.

### Risk 3 — bill_evidence_override_applied audit backfill blast radius

**Risk:** Existing posted bills count substantively high; backfill migration becomes operationally heavy.

**Mitigation:** Brief drafter verifies-from-disk at impl-onset: estimated bill count post-Phase-5 close. If ≤10K rows, single-pass backfill. If >10K, batched backfill at migration shape. Task 9 acceptance criteria includes migration runtime expectations.

### Risk 4 — PostBillInputSchema Zod-level default vs Layer 1 NOT NULL DEFAULT consistency

**Risk:** Zod-level default for `override_evidence_completeness` (default false) interacts with Layer 1 NOT NULL DEFAULT; inconsistency causes Layer 1 vs Layer 2 enforcement drift.

**Mitigation:** Task 4 acceptance criteria requires Zod-level default mirrors Layer 1 NOT NULL DEFAULT per Sub-Q4-shape-2 disposition; brief drafter verifies-from-disk Phase 5 migration `20240138000000:172` default value at Task 4 implementation.

### Risk 5 — Test infrastructure inheritance

**Risk:** Bill integration tests use camelCase + flat path convention per Finding 4; chunk 5.1a brief drafter MUST inherit convention (NOT impose alternative).

**Mitigation:** Task 6 acceptance criteria requires `apps/web/tests/integration/billEvidenceCompleteness.test.ts` flat path per existing convention. Brief drafter verifies-from-disk `apps/web/tests/integration/bill*.test.ts` existing pattern at session-onset.

### Risk 6 — Volume forecast band breakage

**Risk:** Round 4 Phase 5.1 cycle-shape observation: late-round preamble-inheritance attenuation may apply at chunk-brief grain too. Chunk 5.1a brief forecast ~600-900 LOC; brief drafter banks volume realization for Phase 5.1 retrospective drafting.

**Mitigation:** Brief drafter banks volume realization at chunk close for retrospective drafting. If brief volume breaks below 600 LOC (chunk-brief lower-bound), bank as second instance of late-round preamble-inheritance attenuation pattern (Round 4 N=1 → Session 19 N=2).

### Risk 7 — audit_log action enum extension

**Risk:** `bill_evidence_override_applied` action value may require enum-extension migration if `audit_action` is closed enum at DB grain.

**Mitigation:** Task 9 acceptance criteria includes verify-from-disk at impl-onset on audit_log action column type. If closed enum, Task 9 includes enum-extension; if TEXT column, no extension needed.

## 9. Implementation order + parallelization

### 9.1 Strict-sequence dependencies

```
Tasks 1 + 2 (T2 docs: leaf + rollup/matrix)     [parallel-eligible]
  ↓
Tasks 4 + 5 (schema + ServiceErrorCode)         [parallel-eligible]
  ↓
Task 3 (Layer 2 enforcement code)               [depends on 4+5]
  ↓
Tasks 6 + 7 (integration tests)                 [parallel-eligible after Task 3]
  ↓
Task 8 + 9 (vendor_credits + backfill migration) [parallel-eligible with Tasks 1-7; substrate-independent]
  ↓
Task 10 (ADR-0016 amendment)                    [parallel-eligible with Tasks 1-9; substrate-independent]
  ↓
Task 11 (cross-task verification + commit)      [terminal]
```

### 9.2 Subagent dispatch shape (if Session 20 chooses parallel dispatch)

- **Subagent A** — Tasks 1 + 2 (T2 documentation cluster).
- **Subagent B** — Tasks 4 + 5 (schema/type substrate cluster).
- **Subagent C** — Task 3 (Layer 2 enforcement code; depends on Subagent B).
- **Subagent D** — Tasks 6 + 7 (integration tests; depends on Subagent C).
- **Subagent E** — Tasks 8 + 9 (vendor_credits + backfill migration; substrate-independent).
- **Subagent F** — Task 10 (ADR-0016 amendment; substrate-independent).
- **Final orchestrator pass** — Task 11 (cross-task verification + commit ceremony).

### 9.3 Alternative: sequential single-subagent dispatch

Session 20 may elect sequential single-subagent (or direct WSL-side implementation) instead of parallel subagent dispatch. Chunk 5.1a total LOC forecast (~765-1465 LOC per Round 4 §2.2) is within single-session budget per RI-7 thresholds and comparable to Phase 6.5 chunk-1 (~500-700 LOC modified/created realized).

**Recommendation at brief-drafting grain:** sequential single-subagent OR direct WSL-side implementation. Chunk 5.1a substantive complexity concentrates at the T3 ADR-0016 third amendment cycle (additive provenance-preserving) + T1 Layer 2 enforcement code (atomic transaction discipline); splitting these across subagents adds coordination overhead disproportionate to parallelization win. Session 20 onset adjudicates.

## 10. Cross-references

### 10.1 Cycle predecessors

- **Round 4 artifact** (`8631a5d`): `docs/09_briefs/phase-5.1/2026-05-19-phase-5-1-scope-lock-cycle-round-4.md` (488 lines) — primary substrate for chunk 5.1a operationalization. §5.1 chunk 5.1a acceptance criteria; §6.1 Two Laws scope; §7 friction-journal cycle inventory.
- **Round 3 artifact** (`5121569`): §3 Sub-Q4 sub-decision walks (4-a/b/c/d); §4 Sub-Q4 sub-shape walks (1/2/3).
- **Round 2 artifact** (`2560ef6`): §2 Sub-Q2 walk; §3 Sub-Q4 walk; §4 Sub-Q4.5 walk; Refinements 1+2.
- **Round 1 artifact** (`72a40bf`): §2 verify-from-disk pass with 5 Grain 1 substrate-shape findings.

### 10.2 ADR substrates

- **ADR-0011 §15** at `docs/07_governance/adr/0011-document-platform.md` — INV-DOC-001 reservation graduation reference + editorial cluster context (2 candidates banked for Phase 5.1 retrospective drafting).
- **ADR-0016** at `docs/07_governance/adr/0016-document-relationship-graph.md` — Task 10 third amendment cycle target; §1 v1-active + §3 pair-validity matrix + §5 cascade behavior amendments.
- **ADR-0015 §7 Scenario C** at `docs/07_governance/adr/0015-ap-spend-subdomain.md` — Sub-Q4-b receipt accepted set basis (born-paid scenario).
- **ADR-0022 §5** at `docs/07_governance/adr/0022-adr-lifecycle-workflows.md` — additive provenance-preserving amendment-block convention (Task 10 shape).

### 10.3 Structural precedent

- **Phase 6.5 chunk 1 brief** (`1c65cd1`): `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-1.md` (623 lines) — 10-section structural inheritance shape (§1-§8 + §9-§10 extension).
- **Phase 6.5 chunk 3 brief** (`60d5993`): `docs/09_briefs/phase-6.5/chunks/2026-05-16-phase-6-5-chunk-3.md` (1091 lines) — two-commit chunk shape reference (NOT applicable to chunk 5.1a per Sub-Q4 Option (i) atomic single commit).

### 10.4 Substrate references

- **`apps/web/src/services/spend/billService.ts`** (946 lines) — billService.post() Layer 2 enforcement insertion site.
- **`apps/web/src/shared/schemas/spend/bill.schema.ts`** (132 lines) — PostBillInputSchema extension site.
- **`apps/web/src/services/errors/ServiceError.ts`** (119 lines) — ServiceErrorCode union extension site.
- **`supabase/migrations/20240138000000_phase5_vendor_prepayment_substrate.sql`** (272 lines) — vendor_credits substrate shape mirror reference; `bills.override_evidence_completeness` Layer 1 substrate at line 172.

### 10.5 CLAUDE.md codifications

- **Verify-forward-at-scope-lock cluster** (RI-1 + RI-6 four-grain + Grain 5 + RI-7 + RI-10) — applied at chunk 5.1a brief-drafting grain.
- **Substrate-mod test-staleness review** — applied at Task 8 migration broadening (linked_entity_type CHECK 6 → 8 + Zod widen).
- **Verify-from-disk-at-non-standard-grain sub-grain catalog** (Phase 6.5 retro codification) — applied at session-onset divergence detection (N=20 + N=21 instances banked).
- **Operational-flex collapse heuristic** (Phase 6.5 retro codification) — applied at chunk 5.1a impl-session collapse decision (Session 20 single-session implementation OR collapse adjudication).

### 10.6 Phase 5.1 retrospective drafting candidates (cycle banking)

Not chunk 5.1a scope; banked for Phase 5.1 retrospective drafting per Round 4 §7:

- **ADR-0011 §15 editorial cluster** (2 candidates):
  - "Adjacent commit paths" framing over-broad.
  - "primary_invoice (or 'primary')" cites non-existent enum value.
- **Phase 5 retro §6:404 vendor_credits assertion correction** (retrospective-authoring fourth-grain candidate N=1).
- **Candidate (c) catalog observations** (N=21 at Session 19 onset; session-prompt-authoring sub-grain N=11; Mechanism A vs B discrimination at Phase 5.1 retrospective drafting).
- **controller_override_memo + T4/T6 dispatcher slots** named-future-activation cluster (post-v1 trigger context).
- **Late-round preamble-inheritance attenuation** observation (Round 4 N=1 evidence; Session 19 N=2 candidate if chunk 5.1a brief breaks below 600 LOC lower bound).

---

**Brief drafting status:** complete. Single-prompt-execute-and-close per directive. Next operational fire: Session 20 chunk 5.1a implementation per brief OR Phase A reactive Path C adjudication per F-J-14 mid-impl-reactive sub-grain if implementation surfaces unforeseen substrate density.

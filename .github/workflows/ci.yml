name: CI

# PR/branch gating for the monorepo. Runs typecheck, lint, adr
# checks, the intent-producers coverage check (INV-WORKFLOW-001,
# Wave 6 D6 — blocking), and build across all workspaces.
#
# History note: the @chounting/web app previously carried lint
# errors that blocked both `pnpm lint` and `next build` for the
# web workspace. Those errors were @/db/adminClient violations
# under apps/web/src/agent/*. The narrow `src/agent/**` eslint
# exemption shipped in `e719d02` (Q33 partial-resolution arc,
# 2026-04-30) cleared the path. The lint job was subsequently
# narrowed to web + demo at 6c4ce42 because @chounting/ui had
# a "lint": "eslint" script declared but no eslint.config.*
# file wired (scaffolded empty at 000d6f7). Item 47 closed that
# gap by mirroring apps/demo's flat-config in packages/ui;
# lint and build now run unfiltered across all workspaces.
#
# Security note: this workflow does not consume any user-controlled
# context (no github.event.issue.*, github.event.pull_request.title,
# commit message bodies, etc.). Only github.workflow, github.ref,
# github.sha, and runner.os are referenced — all platform-trusted.
#
# The daily verify-audit-coverage workflow runs separately (see
# .github/workflows/verify-audit-coverage.yml). It needs Supabase
# and is not duplicated here.

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck:
    name: typecheck (all workspaces)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-typecheck-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-typecheck-

      - name: Type-check
        run: pnpm exec turbo run type-check

  lint:
    name: lint (all workspaces)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-lint-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-lint-

      - name: Lint
        run: pnpm exec turbo run lint

  adr-check:
    name: adr (lint + index drift)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: ADR lint + index drift check
        run: pnpm adr:check

  intent-producers:
    # Wave 6 D6 — INV-WORKFLOW-001 teeth (ADR-0031 D-0031.2/.3): the
    # producer-coverage check exits 1 on any Intent lacking a non-AI
    # producer (the ratified Q2 `query` carve-out excepted). Blocking by
    # job-failure semantics. Consumes no github.event.* context (the
    # workflow's security-note property preserved). adr-check-isomorphic;
    # no turbo cache (a two-file tsx traversal).
    name: intent-producers (INV-WORKFLOW-001)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Intent producer-coverage check (blocking)
        run: pnpm intent-producers:check

  build:
    name: build (all workspaces)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-build-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-build-

      - name: Build
        run: pnpm exec turbo run build
        env:
          # Build-only placeholders. `next build` collects page data, which
          # imports apps/web/src/shared/env.ts — its assertEnv() checks only
          # that each var is NON-EMPTY (presence), and the build never calls
          # Supabase / Anthropic / Redis / Postmark. So fake non-empty values
          # are sufficient to pass assertEnv. NONE of these are real
          # credentials, and the CI build artifact is a check that is never
          # deployed (Vercel does the real build with real values). If a
          # future real build-time data fetch is added, this will fail past
          # assertEnv and these must become real values via `${{ secrets.* }}`.
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-build-placeholder
          NEXT_PUBLIC_APP_URL: https://placeholder.local
          SUPABASE_SERVICE_ROLE_KEY: ci-build-placeholder
          ANTHROPIC_API_KEY: ci-build-placeholder
          UPSTASH_REDIS_KV_REST_API_URL: https://placeholder.upstash.io
          UPSTASH_REDIS_KV_REST_API_TOKEN: ci-build-placeholder
          POSTMARK_INBOUND_BASIC_AUTH_PASSWORD: ci-build-placeholder
